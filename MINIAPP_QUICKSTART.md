# 🚀 Быстрый старт: Настройка Telegram Mini App

## 📋 Что уже сделано

✅ Backend работает на `http://localhost:8000`  
✅ ngrok туннель для backend: `https://galleylike-nydia-however.ngrok-free.dev`  
✅ Frontend работает на `http://localhost:5173`  
✅ Интеграция с Telegram Web App SDK добавлена  
✅ Автоматическая авторизация через Telegram реализована

## 🎯 Что нужно сделать сейчас

### ⚠️ ВАЖНО: Используем один ngrok туннель!

На бесплатном плане ngrok можно иметь только **один активный туннель**. Поэтому:
- ✅ Используем ngrok для **frontend** (порт 5173)
- ✅ API запросы проксируются через Vite dev server на backend

### Шаг 1: Остановите ngrok для backend (если запущен)

Если у вас запущен ngrok для backend (порт 8000), остановите его (**Ctrl+C**).

### Шаг 2: Запустите ngrok для фронтенда

Откройте терминал в корне проекта и запустите:

```powershell
cd C:\finance-manager
.\ngrok.exe http 5173
```

Вы получите новый URL, например:
```
https://abc123-xyz-456.ngrok-free.app
```

**Сохраните этот URL** - он понадобится для настройки Mini App!

### Шаг 3: Убедитесь, что backend запущен локально

Backend должен работать на `http://localhost:8000`:

```powershell
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Vite автоматически проксирует все `/api/*` запросы на backend.

### Шаг 4: Настроить Mini App в @BotFather

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"**
5. Выберите **"Menu Button"**
6. Введите URL фронтенда (тот, который вы получили в Шаге 2):
   ```
   https://abc123-xyz-456.ngrok-free.app
   ```
7. Готово! ✅

### Шаг 5: Проверить работу

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (обычно внизу экрана)
3. Должно открыться ваше приложение!
4. При первом открытии произойдет автоматическая авторизация

## 📝 Текущая конфигурация

### Терминалы, которые должны быть запущены:

1. **Backend** (localhost:8000) - должен быть запущен локально:
   ```powershell
   cd backend
   python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

2. **Frontend** (localhost:5173) - должен быть запущен локально:
   ```powershell
   cd frontend
   npm run dev
   ```

3. **ngrok для frontend** (один туннель!) - туннелирует на порт 5173:
   ```powershell
   .\ngrok.exe http 5173
   ```
   URL: `https://abc123-xyz-456.ngrok-free.app` (ваш новый URL)

4. **Telegram Bot** (опционально):
   ```powershell
   cd telegram-bot
   python bot.py
   ```

**Примечание:** API запросы автоматически проксируются через Vite dev server на backend (localhost:8000).

## ⚠️ Важные замечания

### URL меняется при перезапуске ngrok

Если вы перезапустили ngrok для frontend:

1. Скопируйте новый URL
2. Обновите в @BotFather:
   ```
   /mybots → Ваш бот → Bot Settings → Menu Button → Введите новый URL
   ```

### Как это работает

1. Пользователь открывает Mini App через ngrok URL фронтенда
2. Frontend загружается через ngrok туннель
3. Все API запросы (`/api/*`) идут на тот же ngrok URL
4. Vite dev server проксирует эти запросы на `http://localhost:8000`
5. Backend обрабатывает запросы и возвращает ответы
6. Всё работает через один ngrok туннель! 🎉

### Проверка работы

Перед настройкой в Telegram, проверьте в браузере:

1. **Frontend через ngrok:**
   ```
   https://abc123-xyz-456.ngrok-free.app
   ```
   Должно открыться ваше приложение.

2. **Backend через ngrok:**
   ```
   https://galleylike-nydia-however.ngrok-free.dev/docs
   ```
   Должна открыться Swagger документация.

## 🎉 Готово!

После выполнения этих шагов Mini App будет работать в Telegram!

При открытии приложения:
- Автоматически определится, что запущено в Telegram
- Произойдет авторизация через Telegram Web App API
- Пользователь сразу попадет на дашборд

Удачи! 🚀

