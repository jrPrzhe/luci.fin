# Финальная настройка проекта для работы с удаленной БД

База данных настроена и доступна! Теперь нужно настроить проект.

## Шаг 1: Создайте файл .env в директории backend/

Перейдите в директорию проекта и создайте файл `.env`:

```bash
cd finance-manager/backend
```

Создайте файл `.env` со следующим содержимым:

```env
# Database - подключение к удаленной PostgreSQL БД
DATABASE_URL=postgresql://finance_user:ваш_пароль@195.43.142.121:5432/finance_db

# Security - ОБЯЗАТЕЛЬНО измените на случайную строку!
SECRET_KEY=ваш-секретный-ключ-для-production-минимум-32-символа

# CORS (если нужно добавить дополнительные домены)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Другие настройки (опционально)
# REDIS_URL=
# GOOGLE_AI_API_KEY=
# TELEGRAM_BOT_TOKEN=
```

⚠️ **ВАЖНО:** 
- Замените `ваш_пароль` на реальный пароль, который вы установили для `finance_user`
- Замените `ваш-секретный-ключ-для-production-минимум-32-символа` на случайную строку (можно сгенерировать командой: `python -c "import secrets; print(secrets.token_urlsafe(32))"`)

## Шаг 2: Проверьте подключение к БД

```bash
cd finance-manager/backend
python test_db_connection.py
```

Должно вывести:
```
✅ Подключение установлено успешно!
✅ Версия PostgreSQL: ...
✅ Текущая база данных: finance_db
✅ Текущий пользователь: finance_user
```

## Шаг 3: Примените миграции базы данных

```bash
python run_migrations.py
```

Или используйте Alembic напрямую:

```bash
alembic upgrade head
```

Это создаст все необходимые таблицы в базе данных.

## Шаг 4: (Опционально) Создайте тестового пользователя

```bash
python create_test_user.py
```

Или создайте администратора:

```bash
python create_admin_user.py
```

## Шаг 5: Запустите приложение

```bash
# Обычный запуск
python -m uvicorn app.main:app --reload

# Или с HTTPS (если нужно)
python run_https.py
```

Приложение будет доступно по адресу:
- HTTP: http://localhost:8000
- HTTPS: https://localhost:8443 (если используете run_https.py)
- API документация: http://localhost:8000/docs

## Шаг 6: Проверьте работу приложения

1. Откройте браузер: http://localhost:8000/docs
2. Проверьте, что API доступно
3. Попробуйте выполнить тестовый запрос

## Устранение проблем

### Ошибка: "ModuleNotFoundError"

Установите зависимости:

```bash
cd finance-manager/backend
pip install -r requirements.txt
```

### Ошибка подключения к БД

1. Проверьте файл `.env` - правильный ли пароль и IP-адрес
2. Проверьте подключение вручную: `psql -h 195.43.142.121 -U finance_user -d finance_db`
3. Проверьте файрвол на сервере (если используется)

### Ошибка миграций

```bash
# Проверьте текущее состояние миграций
alembic current

# Примените миграции заново
alembic upgrade head

# Если нужно, создайте новую миграцию
alembic revision --autogenerate -m "Initial migration"
```

## Генерация SECRET_KEY

Для генерации безопасного SECRET_KEY выполните:

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

Или используйте онлайн-генератор случайных строк.

## Проверка всех настроек

Выполните полную проверку:

```bash
cd finance-manager/backend

# 1. Проверка подключения к БД
echo "=== Проверка подключения к БД ==="
python test_db_connection.py

# 2. Проверка миграций
echo ""
echo "=== Проверка миграций ==="
alembic current

# 3. Проверка переменных окружения
echo ""
echo "=== Проверка переменных окружения ==="
python -c "from app.core.config import settings; print(f'DATABASE_URL: {settings.DATABASE_URL[:50]}...')"
```

## Готово! 🎉

Теперь ваш проект подключен к удаленной базе данных PostgreSQL и готов к работе!














