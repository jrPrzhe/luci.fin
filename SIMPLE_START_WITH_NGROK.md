# Упрощенный запуск проекта с NGROK

Инструкция для запуска проекта в упрощенном режиме:
- **Фронтенд** → запускается через NGROK (публичный URL)
- **Бэкенд** → запускается локально на порту 8000
- **Телеграм бот** → запускается локально

## 📋 Предварительные требования

1. Установлен Python 3.8+
2. Установлен Node.js и npm
3. Установлен ngrok (см. [install-ngrok.bat](install-ngrok.bat) или [install-ngrok.ps1](install-ngrok.ps1))
4. Создан файл `.env` в корне проекта:

```env
SECRET_KEY=your-random-secret-key-here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
```

## 🚀 Порядок запуска

### Терминал 1: Бэкенд (локально)

**Windows (CMD):**
```cmd
cd backend
call venv\Scripts\activate
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Windows (PowerShell):**
```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Или используйте готовый скрипт:**
- `start-backend-simple.bat` (CMD)
- `start-backend-simple.ps1` (PowerShell)

✅ Бэкенд будет доступен на: `http://localhost:8000`

---

### Терминал 2: Фронтенд (локально, но через ngrok будет доступен публично)

**Windows (CMD):**
```cmd
cd frontend
npm install
npm run dev
```

**Windows (PowerShell):**
```powershell
cd frontend
npm install
npm run dev
```

**Или используйте готовый скрипт:**
- `start-frontend.bat` (CMD)
- `start-frontend.ps1` (PowerShell)

✅ Фронтенд будет доступен локально на: `http://localhost:5173`

---

### Терминал 3: NGROK (туннель для фронтенда)

**Windows (CMD):**
```cmd
ngrok http 5173
```

**Windows (PowerShell):**
```powershell
ngrok http 5173
```

**Или используйте готовый скрипт:**
- `start-ngrok-frontend.bat` (CMD)
- `start-ngrok-frontend.ps1` (PowerShell)

✅ После запуска ngrok покажет публичный URL, например:
- `https://xxxx-xxxx-xxxx.ngrok-free.app`

**Этот URL нужно использовать для доступа к фронтенду извне!**

---

### Терминал 4: Телеграм бот (локально)

**Windows (CMD):**
```cmd
cd telegram-bot
call venv\Scripts\activate
pip install -r requirements.txt
python bot.py
```

**Windows (PowerShell):**
```powershell
cd telegram-bot
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python bot.py
```

**Или используйте готовый скрипт:**
- `start-bot.bat` (CMD)
- `start-bot.ps1` (PowerShell)

✅ Бот будет работать в Telegram

---

## 🔗 Как это работает

1. **Фронтенд** (порт 5173) → запускается локально, но доступен через ngrok по публичному URL
2. **Бэкенд** (порт 8000) → работает локально, фронтенд обращается к нему через Vite proxy (`/api` → `http://localhost:8000`)
   - Когда пользователь открывает фронтенд через ngrok URL, все запросы к `/api` проксируются Vite на `http://localhost:8000` на вашем компьютере
3. **NGROK** создает туннель к порту 5173, делая фронтенд доступным публично
4. **Телеграм бот** работает локально и обращается к локальному бэкенду

**Важно:** Vite proxy работает только если бэкенд запущен на той же машине, где запущен фронтенд. Если вы открываете фронтенд через ngrok URL, все API запросы будут проксироваться на `localhost:8000` на вашем компьютере.

## ⚙️ Настройка API URL (опционально)

По умолчанию фронтенд использует относительные пути для API (через Vite proxy).

Если нужно указать явный API URL, создайте файл `frontend/.env`:
```env
VITE_API_URL=http://localhost:8000
```

Или для работы через ngrok бэкенда:
```env
VITE_API_URL=https://your-ngrok-backend-url.ngrok-free.app
```

## 📝 Примечания

- **NGROK Free tier** требует регистрацию и имеет ограничения
- После перезапуска ngrok URL изменится (для постоянного URL нужен платный план)
- Бэкенд работает только локально - убедитесь, что фронтенд может к нему подключиться
- Если используете Telegram Mini App, укажите ngrok URL фронтенда в настройках бота

## 🛠️ Устранение проблем

- Если ngrok не найден → запустите `install-ngrok.bat` или `install-ngrok.ps1`
- Если фронтенд не подключается к бэкенду → проверьте, что бэкенд запущен на порту 8000
- Если ngrok показывает страницу с предупреждением → это нормально для free tier, просто нажмите "Visit Site"
- Если возникает ошибка "Request timeout - сервер не отвечает" → проверьте, что бэкенд запущен и доступен на `http://localhost:8000`

## 📧 Тестовые учетные данные

Для тестирования входа через Email+Password:

1. Создайте тестового пользователя:
   ```bash
   create-test-user.bat      # CMD
   .\create-test-user.ps1    # PowerShell
   ```

2. Используйте следующие учетные данные:
   - **Email:** `test@example.com`
   - **Password:** `test123456`

Подробнее: [TEST_CREDENTIALS.md](TEST_CREDENTIALS.md)

