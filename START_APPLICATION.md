# Запуск приложения

Тестовый пользователь создан! Теперь можно запустить приложение.

## Шаг 1: Запуск backend сервера

```bash
python -m uvicorn app.main:app --reload
```

Приложение будет доступно по адресам:
- **HTTP**: http://localhost:8000
- **API документация (Swagger)**: http://localhost:8000/docs
- **Альтернативная документация (ReDoc)**: http://localhost:8000/redoc

## Шаг 2: Проверка работы API

1. Откройте в браузере: http://localhost:8000/docs
2. Вы увидите интерактивную документацию API
3. Попробуйте выполнить тестовый запрос:
   - Проверьте endpoint `/` или `/health` (если есть)
   - Попробуйте авторизоваться через `/api/v1/auth/login`

## Шаг 3: Авторизация через API

### Способ 1: Через Swagger UI

1. Откройте http://localhost:8000/docs
2. Найдите endpoint `/api/v1/auth/login`
3. Нажмите "Try it out"
4. Введите данные:
   ```json
   {
     "email": "test@example.com",
     "password": "test123456"
   }
   ```
5. Нажмите "Execute"
6. Скопируйте полученный `access_token`

### Способ 2: Через curl

```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "password": "test123456"}'
```

### Способ 3: Через Python

```python
import requests

response = requests.post(
    "http://localhost:8000/api/v1/auth/login",
    json={"email": "test@example.com", "password": "test123456"}
)
print(response.json())
```

## Шаг 4: (Опционально) Создание администратора

Если нужно создать пользователя с правами администратора:

```bash
python create_admin_user.py
```

Или с параметрами:

```bash
python create_admin_user.py --email admin@example.com --password admin123456
```

## Шаг 5: Проверка работы с БД

После авторизации попробуйте создать:
- Счет (Account)
- Транзакцию (Transaction)
- Категорию (Category)

Все данные будут сохраняться в удаленной PostgreSQL БД на сервере 195.43.142.121.

## Полезные команды

### Проверка подключения к БД
```bash
python test_db_connection.py
```

### Просмотр таблиц в БД
```bash
psql -h 195.43.142.121 -U finance_user -d finance_db -c "\dt"
```

### Просмотр пользователей
```bash
psql -h 195.43.142.121 -U finance_user -d finance_db -c "SELECT id, email, username, is_active, is_admin FROM users;"
```

### Запуск с HTTPS (для Telegram Mini App)
```bash
python run_https.py
```

## Готово! 🎉

Теперь ваш проект полностью настроен и работает:
- ✅ База данных PostgreSQL на удаленном сервере
- ✅ Таблицы созданы
- ✅ Тестовый пользователь создан
- ✅ Приложение готово к запуску

## Следующие шаги

1. Запустите приложение: `python -m uvicorn app.main:app --reload`
2. Откройте http://localhost:8000/docs
3. Протестируйте API
4. Начните разработку новых функций!











