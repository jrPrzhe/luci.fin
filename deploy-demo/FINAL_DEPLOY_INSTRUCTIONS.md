# 🚀 Финальная инструкция по деплою в продакшн

## ✅ Что уже готово:

1. ✅ `index.html` обновлен с правильными путями к изображениям (`./1.png` вместо `./images/1.png`)
2. ✅ Изображения должны быть скопированы в корень `deploy-demo`
3. ✅ Все файлы готовы к деплою

## 📋 Шаги для деплоя:

### Вариант 1: Использовать готовый скрипт

Откройте PowerShell или CMD и выполните:

```cmd
cd E:\finance-manager
.\finance-manager\deploy-demo\push-to-prod.bat
```

### Вариант 2: Ручной деплой

Выполните команды по порядку:

```bash
# 1. Перейдите в корень проекта
cd E:\finance-manager

# 2. Клонируйте репозиторий (если еще не клонирован)
git clone https://github.com/jrPrzhe/demo0811.git

# 3. Перейдите в репозиторий
cd demo0811

# 4. Обновите репозиторий
git fetch origin
git checkout main
git pull origin main

# 5. Скопируйте файлы из deploy-demo
copy ..\finance-manager\deploy-demo\index.html index.html
copy ..\finance-manager\deploy-demo\1.png 1.png
copy ..\finance-manager\deploy-demo\2.png 2.png
copy ..\finance-manager\deploy-demo\3.png 3.png
copy ..\finance-manager\deploy-demo\4.png 4.png
copy ..\finance-manager\deploy-demo\5.png 5.png

# 6. Добавьте файлы в git
git add .

# 7. Создайте коммит
git commit -m "Update demo: assistant on each screen with different positions"

# 8. Отправьте изменения
git push origin main
```

## ⚠️ Важно:

- Убедитесь, что изображения `1.png` - `5.png` находятся в корне папки `deploy-demo`
- Если изображений нет в корне, скопируйте их из папки `images`:
  ```cmd
  cd E:\finance-manager\finance-manager\deploy-demo
  copy images\*.png .
  ```

## 🌐 После деплоя:

Демо будет доступно через 1-2 минуты на:
- https://jrprzhe.github.io/demo0811/
- https://demo0811.vercel.app/

## 🔍 Проверка:

После пуша проверьте:
1. Что файлы появились в репозитории на GitHub
2. Что изображения загружаются корректно
3. Что демо работает на GitHub Pages













