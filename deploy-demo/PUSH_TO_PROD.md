# 🚀 Инструкция по деплою в продакшн

## Быстрый деплой

### Вариант 1: Использовать готовый скрипт (РЕКОМЕНДУЕТСЯ)

**Для Windows (PowerShell):**
```powershell
cd E:\finance-manager\finance-manager\deploy-demo
.\deploy.ps1
```

**Для Windows (CMD):**
```cmd
cd E:\finance-manager\finance-manager\deploy-demo
deploy.bat
```

### Вариант 2: Ручной деплой через Git

```bash
# 1. Клонируйте репозиторий (если еще не клонирован)
cd E:\finance-manager
git clone https://github.com/jrPrzhe/demo0811.git
cd demo0811

# 2. Обновите репозиторий
git fetch origin
git checkout main
git pull origin main

# 3. Скопируйте файлы из deploy-demo в корень репозитория
# Скопируйте:
# - index.html
# - 1.png, 2.png, 3.png, 4.png, 5.png (изображения должны быть в корне!)

# 4. Добавьте файлы
git add .

# 5. Создайте коммит
git commit -m "Update demo: add assistant on each screen with different positions"

# 6. Отправьте изменения
git push origin main
```

## 📁 Структура файлов в репозитории

После деплоя структура должна быть:
```
demo0811/
├── index.html      # Главная страница
├── 1.png           # Изображения ассистента
├── 2.png
├── 3.png
├── 4.png
├── 5.png
├── README.md
└── ...
```

**ВАЖНО:** Изображения должны быть в корне репозитория, не в подпапке!

## ✅ Проверка после деплоя

После пуша подождите 1-2 минуты и проверьте:
- https://jrprzhe.github.io/demo0811/
- https://demo0811.vercel.app/

## 🔄 Что изменилось в этой версии

- ✅ Ассистент размещен на каждом экране в разных местах
- ✅ Темная тема применена ко всем элементам
- ✅ Ассистент использует изображения (1.png - 5.png) вместо эмодзи
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Интерактивные комментарии ассистента на каждом слайде













