# 🎯 Решение проблемы: Railway не находит requirements.txt

## Проблема

Railway загружает Dockerfile, но не может найти `requirements.txt` при выполнении `COPY requirements.txt .`

## Причина

Railway использует **Root Directory** как build context. Если Root Directory не установлен или установлен неправильно, Railway не может найти файлы.

## ✅ Решение

### Вариант 1: Установить Root Directory (РЕКОМЕНДУЕТСЯ)

**В Railway UI:**

1. Settings → **Source** (в Quick Links справа)
2. Поле **Root Directory** → установите: `backend`
3. Сохраните изменения
4. Railway автоматически перезапустит деплой

**После этого:**
- Railway будет использовать `backend/` как build context
- Dockerfile найдет `requirements.txt` в `backend/requirements.txt`
- Все команды COPY будут работать относительно `backend/`

### Вариант 2: Пересоздать сервис с Root Directory

Если Root Directory не видно в настройках:

1. **Удалите сервис:**
   - Settings → Danger Zone → Delete Service

2. **Создайте новый:**
   - + New → GitHub Repo
   - Репозиторий: `luci.fin`
   - **ВАЖНО:** При настройке в поле **Root Directory** укажите: `backend`
   - Нажмите Deploy

### Вариант 3: Проверить что файлы в репозитории

Убедитесь что файлы закоммичены:

```bash
# Проверьте что файлы в git
git ls-files backend/requirements.txt
git ls-files backend/Dockerfile

# Если нет - добавьте
git add backend/requirements.txt backend/Dockerfile
git commit -m "Ensure backend files are in repository"
git push origin main
```

## 🔍 Проверка

После установки Root Directory:

1. **В Railway Settings → Source:**
   - Root Directory должен быть: `backend`

2. **В логах сборки:**
   - Railway должен найти все файлы
   - `COPY requirements.txt .` должен работать

## 📝 Структура

```
luci.fin/ (корень репозитория)
└── backend/
    ├── Dockerfile
    ├── requirements.txt  ← должен быть здесь
    ├── railway.json
    └── app/
```

Если Root Directory = `backend`, Railway использует `backend/` как build context.

## ⚠️ Важно

**Root Directory ОБЯЗАТЕЛЬНО должен быть установлен в `backend`!**

Без этого Railway не сможет найти файлы и сборка будет падать с ошибкой "file not found".

