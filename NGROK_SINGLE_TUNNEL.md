# Настройка ngrok с одним туннелем (решение проблемы)

## 🔴 Проблема

На бесплатном плане ngrok можно иметь только **один активный туннель** одновременно. При попытке запустить второй туннель возникает ошибка:
```
ERROR: The endpoint 'https://...' is already online
```

## ✅ Решение: Один туннель для frontend

Используем один ngrok туннель для frontend (порт 5173), а API запросы проксируются через Vite dev server на backend.

### Шаг 1: Остановите ngrok для backend

В терминале, где запущен ngrok для backend (порт 8000), нажмите **Ctrl+C**

### Шаг 2: Запустите ngrok для frontend

В корне проекта:

```powershell
.\ngrok.exe http 5173
```

Вы получите URL, например:
```
https://abc123-xyz-456.ngrok-free.app
```

**Этот URL будет использоваться для Mini App!**

### Шаг 3: Убедитесь, что backend запущен локально

Backend должен быть запущен на `http://localhost:8000`:

```powershell
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Шаг 4: Убедитесь, что frontend запущен локально

Frontend должен быть запущен на `http://localhost:5173`:

```powershell
cd frontend
npm run dev
```

### Шаг 5: Проверьте конфигурацию

Vite уже настроен для проксирования API запросов (`frontend/vite.config.ts`):

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8000',
    changeOrigin: true,
  },
}
```

Это означает, что все запросы к `/api/*` будут автоматически проксироваться на backend.

### Шаг 6: Настройте Mini App в @BotFather

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Введите URL фронтенда (тот, который получили в Шаге 2):
   ```
   https://abc123-xyz-456.ngrok-free.app
   ```

## 🎯 Как это работает

1. Пользователь открывает Mini App через ngrok URL фронтенда
2. Frontend загружается через ngrok туннель
3. Все API запросы (`/api/*`) идут на тот же ngrok URL
4. Vite dev server проксирует эти запросы на `http://localhost:8000`
5. Backend обрабатывает запросы и возвращает ответы
6. Всё работает через один ngrok туннель!

## ⚠️ Важные замечания

### Vite proxy работает только в dev режиме

Если вы запускаете production build, нужно будет:
1. Либо использовать два ngrok туннеля (потребуется платный план)
2. Либо настроить nginx для проксирования
3. Либо запускать frontend и backend на одном порту

Для разработки текущее решение идеально!

### Проверка работы

1. **Frontend через ngrok:**
   ```
   https://abc123-xyz-456.ngrok-free.app
   ```
   Должно открыться приложение.

2. **API запросы:**
   Откройте DevTools (F12) в браузере → Network → проверьте, что API запросы идут на тот же ngrok URL (не на localhost:8000)

3. **В Telegram:**
   Откройте бота → нажмите кнопку меню → должно открыться приложение с автоматической авторизацией

## 📋 Итоговая конфигурация

### Терминалы:

1. **Backend** (localhost:8000):
   ```powershell
   cd backend
   python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

2. **Frontend** (localhost:5173):
   ```powershell
   cd frontend
   npm run dev
   ```

3. **ngrok для frontend** (один туннель!):
   ```powershell
   .\ngrok.exe http 5173
   ```

4. **Telegram Bot** (опционально):
   ```powershell
   cd telegram-bot
   python bot.py
   ```

## 🎉 Готово!

Теперь всё работает через один ngrok туннель, и Mini App должен корректно работать в Telegram!

