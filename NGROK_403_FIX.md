# Решение ошибки 403 Forbidden от ngrok

## 🔴 Проблема

При открытии ngrok URL возникает ошибка:
```
ERR_NGROK_3200
403 Forbidden
```

Это происходит из-за страницы предупреждения ngrok (browser warning page) на бесплатном плане.

## ✅ Решение

Добавлен Vite плагин для автоматического добавления заголовка `ngrok-skip-browser-warning` во все ответы.

### Что было сделано

1. ✅ Создан Vite плагин `ngrokBypassPlugin` 
2. ✅ Плагин добавляет заголовок `ngrok-skip-browser-warning: true` во все ответы
3. ✅ Заголовок также добавляется к проксированным API запросам

### Что нужно сделать

**Перезапустите frontend dev server:**

```powershell
# Остановите текущий (Ctrl+C)
# Затем запустите заново:
cd frontend
npm run dev
```

После перезапуска ошибка 403 должна исчезнуть!

## 🔍 Проверка

1. **Убедитесь, что frontend запущен:**
   ```powershell
   cd frontend
   npm run dev
   ```
   Должно показать: `Local: http://localhost:5173/`

2. **Убедитесь, что ngrok туннелирует на правильный порт:**
   ```powershell
   .\ngrok.exe http 5173
   ```

3. **Откройте ngrok URL в браузере:**
   ```
   https://galleylike-nydia-however.ngrok-free.dev
   ```
   
   Теперь должно работать без ошибки 403!

## 📝 Альтернативное решение

Если проблема сохраняется, можно также запустить ngrok с дополнительными опциями:

```powershell
.\ngrok.exe http 5173 --host-header=rewrite
```

Или использовать ngrok с обходом предупреждения через конфигурацию:

```yaml
# ~/.ngrok2/ngrok.yml или %LOCALAPPDATA%/ngrok/ngrok.yml
version: "2"
authtoken: YOUR_TOKEN
tunnels:
  frontend:
    proto: http
    addr: 5173
    inspect: false
    bind_tls: true
```

Затем запустите:
```powershell
.\ngrok.exe start frontend
```

## 🎯 Для Telegram Mini App

Важно: Telegram Mini App обычно обходит страницу предупреждения ngrok автоматически, так как запросы идут от Telegram, а не из браузера пользователя.

Но для тестирования в браузере обязательно перезапустите frontend после изменений в `vite.config.ts`!

