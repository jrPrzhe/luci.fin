# ⚡ Быстрый деплой исправлений enum

## ✅ Шаг 1: Изменения отправлены в GitHub

Изменения уже закоммичены и отправлены:
- ✅ `backend/app/models/category.py` - добавлен TypeDecorator
- ✅ `backend/app/models/transaction.py` - улучшен TypeDecorator
- ✅ `backend/app/api/v1/categories.py` - исправлены запросы
- ✅ `backend/app/api/v1/transactions.py` - исправлено создание транзакций

**Коммит:** `bed26926`

## 🚂 Шаг 2: Дождаться деплоя в Railway

Railway автоматически задеплоит изменения (обычно 2-5 минут).

Проверьте статус в [Railway Dashboard](https://railway.app):
- Откройте ваш проект
- Перейдите в **Backend Service** → **Deployments**
- Дождитесь зеленого статуса последнего деплоя

## 🔧 Шаг 3: Выполнить миграции на проде

### Способ 1: Через Railway CLI (РЕКОМЕНДУЕТСЯ)

```bash
# Если Railway CLI не установлен
npm i -g @railway/cli

# Авторизуйтесь и подключитесь
railway login
railway link

# Выполните миграции
railway run python backend/run_migrations.py
```

### Способ 2: Через Railway Dashboard

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект → **Backend Service**
3. Перейдите в **Deployments** → последний деплой
4. Откройте **Shell/Console**
5. Выполните:
   ```bash
   cd backend
   python run_migrations.py
   ```

## ✅ Шаг 4: Проверить что миграции применены

```bash
# Проверка через Railway CLI
railway run python backend/check_migrations.py
```

Или через API:
```bash
curl https://ВАШ_БЭКЕНД_URL/api/v1/check-migrations
```

**Ожидаемый результат:**
- `alembic_version`: `9fae3a73c9e6`
- `enum_values`: `['income', 'expense', 'transfer', 'both']` (все в lowercase)

## 🧪 Шаг 5: Быстрая проверка

1. **Откройте приложение** и попробуйте:
   - Добавить доход → должно работать без ошибок
   - Добавить расход → должно работать без ошибок
   - Открыть категории → должно загружаться без ошибок
   - Открыть отчеты → должны показывать данные
   - Открыть транзакции → должны показывать данные

## ⚠️ Если миграции не применяются автоматически

Если при старте backend миграции не применились (проверьте логи Railway), выполните их вручную:

```bash
railway run python backend/run_migrations.py
```

## 📋 Чек-лист

- [ ] Изменения отправлены в GitHub (✅ сделано)
- [ ] Railway задеплоил (проверить в Dashboard)
- [ ] Миграции применены (выполнить вручную)
- [ ] Enum значения в lowercase (проверить через `check_migrations.py`)
- [ ] Добавление доходов работает
- [ ] Добавление расходов работает
- [ ] Категории загружаются
- [ ] Отчеты работают
- [ ] Транзакции отображаются

---

**Время выполнения:** ~5-10 минут (включая ожидание деплоя)








