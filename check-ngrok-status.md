# Проверка статуса ngrok

## ✅ ngrok запущен

ngrok показывает: "Session Status: online" - это хорошо!

## 🔍 Что нужно проверить

В выводе ngrok должна быть строка:
```
Forwarding    https://xxxx-xx-xxx-xxx-xx.ngrok-free.app -> http://localhost:XXXX
```

Если её нет:
1. Подождите несколько секунд - ngrok может еще подключаться
2. Прокрутите вывод вверх - URL может быть выше
3. Проверьте, на какой порт туннелируется

## 📋 Полный процесс

### 1. Проверьте, что backend запущен

**Если используете HTTP backend (порт 8000):**
```powershell
# В другом терминале проверьте:
curl http://localhost:8000/health
```

**Если используете HTTPS backend (порт 8443):**
```powershell
# В другом терминале проверьте:
curl -k https://localhost:8443/health
```

### 2. Убедитесь, что ngrok туннелирует на правильный порт

**Для HTTP backend (порт 8000):**
```powershell
.\ngrok.exe http 8000
```

**Для HTTPS backend (порт 8443):**
```powershell
.\ngrok.exe http https://localhost:8443 --host-header=localhost:8443
```

### 3. Проверьте веб-интерфейс ngrok

Откройте: http://127.0.0.1:4040

Здесь вы увидите:
- Все запросы к туннелю
- URL туннеля
- Статус подключения

## 🎯 Быстрая проверка

1. Откройте http://127.0.0.1:4040 в браузере
2. На главной странице будет показан ваш ngrok URL
3. Скопируйте его

## ⚠️ Если URL не появляется

Возможные причины:
1. Backend не запущен на указанном порту
2. ngrok не может подключиться к backend
3. Неправильный порт в команде ngrok

### Решение:

1. **Остановите ngrok** (Ctrl+C)
2. **Убедитесь, что backend запущен** и работает
3. **Перезапустите ngrok** с правильным портом:
   ```powershell
   # Для HTTP backend:
   .\ngrok.exe http 8000
   
   # Для HTTPS backend:
   .\ngrok.exe http https://localhost:8443 --host-header=localhost:8443
   ```

