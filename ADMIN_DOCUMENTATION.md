# Документация приложения Finance Manager для системного аналитика

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Архитектура приложения](#архитектура-приложения)
3. [Модули системы](#модули-системы)
4. [Модели данных](#модели-данных)
5. [Потоки данных](#потоки-данных)
6. [API эндпоинты](#api-эндпоинты)
7. [Интеграции](#интеграции)
8. [Безопасность и авторизация](#безопасность-и-авторизация)

---

## 🎯 Обзор системы

**Finance Manager** — это комплексная система управления личными и совместными финансами с поддержкой нескольких платформ и ИИ-ассистентом.

### Основные компоненты

1. **Backend API** (FastAPI) — основной сервер приложения
2. **Frontend** (React + TypeScript) — веб-интерфейс
3. **Telegram Bot** — бот для Telegram
4. **VK Bot** — бот для VKontakte
5. **База данных** (PostgreSQL/SQLite) — хранение данных
6. **AI Assistant** (Google Gemini) — интеллектуальный помощник

---

## 🏗️ Архитектура приложения

### Общая схема

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Frontend  │     │ Telegram Bot│     │   VK Bot    │
│  (React)    │     │  (Python)   │     │  (Python)   │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       │  HTTP/REST API     │  HTTP/REST API    │
       │  JWT Auth          │  JWT Auth          │
       └───────────────────┼───────────────────┘
                           │
                  ┌────────▼────────┐
                  │  Backend API    │
                  │   (FastAPI)     │
                  └────────┬────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                   │
┌───────▼──────┐  ┌────────▼────────┐  ┌──────▼──────┐
│  PostgreSQL  │  │  AI Assistant   │  │   Redis     │
│   Database   │  │  (Gemini API)   │  │  (Cache)    │
└──────────────┘  └─────────────────┘  └─────────────┘
```

### Технологический стек

**Backend:**
- Python 3.10+
- FastAPI — веб-фреймворк
- SQLAlchemy — ORM
- Alembic — миграции БД
- Pydantic — валидация данных
- JWT — аутентификация

**Frontend:**
- React 18+
- TypeScript
- Vite — сборщик
- React Router — маршрутизация

**База данных:**
- PostgreSQL (production)
- SQLite (development)

**Боты:**
- python-telegram-bot (Telegram)
- vkbottle (VK)

**AI:**
- Google Gemini API

---

## 📦 Модули системы

### 1. Backend API (`backend/app/`)

#### 1.1. Модуль аутентификации (`api/v1/auth.py`)

**Назначение:** Управление пользователями и аутентификацией

**Функции:**
- Регистрация пользователей (email/password, Telegram, VK)
- Авторизация и выдача JWT токенов
- Обновление токенов (refresh token)
- Получение информации о текущем пользователе
- Связывание аккаунтов (Telegram + VK + Email)
- Проверка прав администратора

**Ключевые эндпоинты:**
- `POST /api/v1/auth/register` — регистрация
- `POST /api/v1/auth/login` — вход
- `POST /api/v1/auth/telegram` — вход через Telegram
- `POST /api/v1/auth/vk` — вход через VK
- `GET /api/v1/auth/me` — информация о пользователе
- `POST /api/v1/auth/refresh` — обновление токена

**Потоки данных:**
```
Telegram/VK → initData → Backend → User (создание/поиск) → JWT Token → Client
```

#### 1.2. Модуль транзакций (`api/v1/transactions.py`)

**Назначение:** Управление финансовыми операциями

**Функции:**
- Создание транзакций (доходы, расходы, переводы)
- Получение списка транзакций с фильтрацией
- Обновление и удаление транзакций
- Поддержка повторяющихся транзакций
- Распределение транзакций в совместных бюджетах
- Привязка транзакций к целям

**Ключевые эндпоинты:**
- `GET /api/v1/transactions/` — список транзакций
- `POST /api/v1/transactions/` — создание транзакции
- `GET /api/v1/transactions/{id}` — получение транзакции
- `PUT /api/v1/transactions/{id}` — обновление транзакции
- `DELETE /api/v1/transactions/{id}` — удаление транзакции

**Потоки данных:**
```
Client → Transaction Data → Validation → Database → Account Balance Update → Response
```

#### 1.3. Модуль счетов (`api/v1/accounts.py`)

**Назначение:** Управление финансовыми счетами пользователей

**Функции:**
- Создание и управление счетами (наличные, карты, кошельки)
- Получение баланса по всем счетам
- Обновление баланса при транзакциях
- Поддержка мультивалютности
- Привязка счетов к совместным бюджетам

**Ключевые эндпоинты:**
- `GET /api/v1/accounts/` — список счетов
- `POST /api/v1/accounts/` — создание счета
- `GET /api/v1/accounts/balance` — общий баланс
- `PUT /api/v1/accounts/{id}` — обновление счета
- `DELETE /api/v1/accounts/{id}` — удаление счета

#### 1.4. Модуль категорий (`api/v1/categories.py`)

**Назначение:** Управление категориями транзакций

**Функции:**
- Создание категорий доходов и расходов
- Настройка иконок и цветов
- Избранные категории
- Категории для совместных бюджетов

**Ключевые эндпоинты:**
- `GET /api/v1/categories/` — список категорий
- `POST /api/v1/categories/` — создание категории
- `PUT /api/v1/categories/{id}` — обновление категории
- `DELETE /api/v1/categories/{id}` — удаление категории

#### 1.5. Модуль совместных бюджетов (`api/v1/shared_budgets.py`)

**Назначение:** Управление совместными бюджетами (семья, пары, группы)

**Функции:**
- Создание совместных бюджетов
- Приглашение участников по коду
- Управление ролями (администратор, участник)
- Распределение транзакций между участниками
- Общие счета и категории

**Ключевые эндпоинты:**
- `GET /api/v1/shared-budgets/` — список бюджетов
- `POST /api/v1/shared-budgets/` — создание бюджета
- `POST /api/v1/shared-budgets/{id}/join` — присоединение по коду
- `GET /api/v1/shared-budgets/{id}/members` — участники
- `DELETE /api/v1/shared-budgets/{id}/members/{user_id}` — удаление участника

**Потоки данных:**
```
User A → Create Budget → Generate Invite Code → User B → Join by Code → 
→ Shared Budget Member → Access to Shared Accounts/Categories
```

#### 1.6. Модуль целей (`api/v1/goals.py`)

**Назначение:** Управление финансовыми целями (накопления, экономия)

**Функции:**
- Создание целей (накопить, потратить меньше, инвестировать)
- Отслеживание прогресса
- Генерация плана достижения цели (roadmap) через ИИ
- Привязка транзакций к целям
- Автоматическое создание счета для цели

**Ключевые эндпоинты:**
- `GET /api/v1/goals/` — список целей
- `POST /api/v1/goals/` — создание цели
- `POST /api/v1/goals/generate-roadmap` — генерация плана через ИИ
- `PUT /api/v1/goals/{id}` — обновление цели
- `DELETE /api/v1/goals/{id}` — удаление цели

**Потоки данных:**
```
User → Goal Creation → AI Analysis (transactions, income, expenses) → 
→ Roadmap Generation → Goal Account Creation → Progress Tracking
```

#### 1.7. Модуль отчетов (`api/v1/reports.py`)

**Назначение:** Генерация финансовых отчетов и аналитики

**Функции:**
- Отчеты по периодам
- Отчеты по категориям
- Отчеты по счетам
- Экспорт в CSV/Excel
- Визуализация данных

**Ключевые эндпоинты:**
- `GET /api/v1/reports/summary` — сводный отчет
- `GET /api/v1/reports/by-category` — по категориям
- `GET /api/v1/reports/by-period` — по периодам
- `GET /api/v1/reports/export` — экспорт данных

#### 1.8. Модуль ИИ-ассистента (`api/v1/ai.py`)

**Назначение:** Интеграция с ИИ для финансовых рекомендаций

**Функции:**
- Ответы на вопросы пользователей о финансах
- Анализ транзакций
- Рекомендации по оптимизации расходов
- Генерация планов достижения целей

**Ключевые эндпоинты:**
- `POST /api/v1/ai/ask` — задать вопрос ИИ
- `POST /api/v1/ai/analyze` — анализ финансов

**Потоки данных:**
```
User Question → Backend → AI Assistant (Gemini) → 
→ Context (transactions, accounts, goals) → AI Response → User
```

#### 1.9. Модуль администрирования (`api/v1/admin.py`)

**Назначение:** Административные функции

**Функции:**
- Просмотр всех пользователей со статистикой
- Сброс настроек пользователя
- Синхронизация статуса администратора

**Ключевые эндпоинты:**
- `GET /api/v1/admin/users` — список всех пользователей
- `POST /api/v1/admin/users/{user_id}/reset` — сброс пользователя
- `GET /api/v1/admin/sync-admin-status` — синхронизация админ-статуса

**Доступ:** Только для пользователей с `is_admin = True`

#### 1.10. Модуль импорта данных (`api/v1/import_data.py`)

**Назначение:** Импорт транзакций из внешних источников

**Функции:**
- Импорт из CSV/Excel
- Парсинг банковских выписок
- Массовое создание транзакций

**Ключевые эндпоинты:**
- `POST /api/v1/import/csv` — импорт из CSV
- `POST /api/v1/import/excel` — импорт из Excel

### 2. Frontend (`frontend/src/`)

#### 2.1. Страницы приложения

- **Dashboard** — главная страница с общей статистикой
- **Transactions** — список и управление транзакциями
- **Accounts** — управление счетами
- **Categories** — управление категориями
- **Goals** — финансовые цели
- **Reports** — отчеты и аналитика
- **SharedBudgets** — совместные бюджеты
- **Profile** — профиль пользователя
- **Login/Register** — аутентификация

#### 2.2. Сервисы

- **api.ts** — клиент для работы с Backend API
- **telegram.ts** — интеграция с Telegram Mini App
- **vk.ts** — интеграция с VK Mini App

#### 2.3. Контексты

- **I18nContext** — интернационализация (русский/английский)
- **NewYearContext** — тематические эффекты

### 3. Telegram Bot (`telegram-bot/`)

**Назначение:** Управление финансами через Telegram

**Функции:**
- Команды для быстрого добавления транзакций
- Просмотр баланса
- Просмотр транзакций
- Создание целей
- Многоязычная поддержка

**Команды:**
- `/start` — приветствие и список команд
- `/balance` — баланс по счетам
- `/transactions` — последние транзакции
- `/add` — добавление транзакции (диалог)
- `/goal` — создание цели
- `/help` — справка

**Потоки данных:**
```
Telegram Message → Bot → Backend API (JWT Auth) → Database → Response → Telegram
```

**Аутентификация:**
1. Пользователь отправляет команду
2. Бот получает `telegram_id` из сообщения
3. Бот запрашивает токен: `POST /api/v1/auth/bot-token-telegram`
4. Backend создает/находит пользователя по `telegram_id`
5. Backend возвращает JWT токен
6. Бот использует токен для всех последующих запросов

### 4. VK Bot (`vk-bot/`)

**Назначение:** Управление финансами через VK

**Функции:** Аналогичны Telegram Bot

**Команды:**
- `/start` — приветствие
- `/balance` — баланс
- `/transactions` — транзакции
- `/help` — справка

**Аутентификация:**
Аналогично Telegram Bot, но использует `vk_id` вместо `telegram_id`

---

## 💾 Модели данных

### Основные сущности

#### 1. User (Пользователь)

```python
- id: Integer (PK)
- email: String (unique)
- username: String (unique, nullable)
- hashed_password: String (nullable)
- telegram_id: String (index, nullable)
- telegram_username: String (nullable)
- vk_id: String (index, nullable)
- first_name: String (nullable)
- last_name: String (nullable)
- timezone: String (default: "UTC")
- default_currency: String (default: "USD")
- language: String (default: "en")
- is_2fa_enabled: Boolean
- is_active: Boolean
- is_verified: Boolean
- is_admin: Boolean
- created_at: DateTime
- updated_at: DateTime
- last_login: DateTime (nullable)
```

**Связи:**
- Один ко многим: Accounts, Transactions, Categories, Goals, Reports
- Многие ко многим: SharedBudgets (через SharedBudgetMember)

#### 2. Account (Счет)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- shared_budget_id: Integer (FK → shared_budgets.id, nullable)
- name: String
- account_type: Enum (CASH, BANK_CARD, BANK_ACCOUNT, E_WALLET, CREDIT_CARD, INVESTMENT, OTHER)
- currency: String (3 chars)
- initial_balance: Numeric
- is_active: Boolean
- is_archived: Boolean
- description: String (nullable)
- created_at: DateTime
- updated_at: DateTime
```

**Связи:**
- Многие к одному: User, SharedBudget
- Один ко многим: Transactions

#### 3. Transaction (Транзакция)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- account_id: Integer (FK → accounts.id)
- transaction_type: Enum (INCOME, EXPENSE, TRANSFER)
- amount: Numeric
- currency: String (3 chars)
- amount_in_default_currency: Numeric (nullable)
- exchange_rate: Numeric (nullable)
- category_id: Integer (FK → categories.id, nullable)
- description: Text (nullable)
- transaction_date: DateTime
- is_recurring: Boolean
- recurring_frequency: Enum (nullable)
- recurring_end_date: DateTime (nullable)
- parent_transaction_id: Integer (FK → transactions.id, nullable)
- to_account_id: Integer (FK → accounts.id, nullable) # для переводов
- shared_budget_id: Integer (FK → shared_budgets.id, nullable)
- distribution_method: Enum (nullable)
- distribution_data: Text (JSON, nullable)
- goal_id: Integer (FK → goals.id, nullable)
- created_at: DateTime
- updated_at: DateTime
```

**Связи:**
- Многие к одному: User, Account, Category, SharedBudget, Goal
- Многие ко многим: Tags (через transaction_tags)

#### 4. Category (Категория)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- shared_budget_id: Integer (FK → shared_budgets.id, nullable)
- name: String
- transaction_type: Enum (INCOME, EXPENSE)
- icon: String (nullable)
- color: String (nullable)
- is_default: Boolean
- is_favorite: Boolean
- created_at: DateTime
- updated_at: DateTime
```

#### 5. SharedBudget (Совместный бюджет)

```python
- id: Integer (PK)
- created_by: Integer (FK → users.id)
- name: String
- description: Text (nullable)
- currency: String (3 chars)
- invite_code: String (unique, index)
- is_active: Boolean
- created_at: DateTime
- updated_at: DateTime
```

**Связи:**
- Один ко многим: SharedBudgetMember, Transaction, Account, Category

#### 6. SharedBudgetMember (Участник бюджета)

```python
- id: Integer (PK)
- shared_budget_id: Integer (FK → shared_budgets.id)
- user_id: Integer (FK → users.id)
- role: Enum (ADMIN, MEMBER)
- can_view_private_transactions: Boolean
- joined_at: DateTime
- updated_at: DateTime
```

#### 7. Goal (Цель)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- goal_type: Enum (SAVE, SPEND_LESS, INVEST)
- name: String
- description: Text (nullable)
- target_amount: Numeric
- current_amount: Numeric
- currency: String (3 chars)
- start_date: DateTime
- target_date: DateTime (nullable)
- status: Enum (ACTIVE, COMPLETED, FAILED, PAUSED)
- progress_percentage: Integer
- category_id: Integer (FK → categories.id, nullable)
- account_id: Integer (FK → accounts.id, nullable)
- roadmap: Text (JSON, nullable)
- created_at: DateTime
- updated_at: DateTime
```

#### 8. Tag (Тег)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- name: String
- color: String (nullable)
- created_at: DateTime
```

**Связи:**
- Многие ко многим: Transactions (через transaction_tags)

#### 9. Report (Отчет)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- report_type: String
- parameters: Text (JSON)
- generated_at: DateTime
```

#### 10. Notification (Уведомление)

```python
- id: Integer (PK)
- user_id: Integer (FK → users.id)
- title: String
- message: Text
- notification_type: String
- is_read: Boolean
- created_at: DateTime
```

---

## 🔄 Потоки данных

### 1. Регистрация и авторизация

#### 1.1. Регистрация через Email

```
Frontend → POST /api/v1/auth/register
  ↓
Backend: Validation → User Creation → JWT Token Generation
  ↓
Response: { access_token, refresh_token, user }
  ↓
Frontend: Save Token → Redirect to Dashboard
```

#### 1.2. Авторизация через Telegram

```
Telegram Mini App → window.Telegram.WebApp.initData
  ↓
Frontend → POST /api/v1/auth/telegram { init_data }
  ↓
Backend: Parse initData → Extract telegram_id → 
  → Find/Create User → Generate JWT Token
  ↓
Response: { access_token, refresh_token, user }
  ↓
Frontend: Save Token → Authenticated State
```

#### 1.3. Авторизация через VK

Аналогично Telegram, но использует VK initData

### 2. Создание транзакции

```
Frontend/Bot → POST /api/v1/transactions/
  {
    account_id, amount, currency, transaction_type,
    category_id, description, transaction_date
  }
  ↓
Backend: Validation → Transaction Creation → 
  → Account Balance Update → Response
  ↓
Response: Transaction Object
  ↓
Frontend: Update UI (Balance, Transactions List)
```

**Обновление баланса счета:**
```
Transaction Created → Calculate New Balance:
  - INCOME: balance += amount
  - EXPENSE: balance -= amount
  - TRANSFER: from_account -= amount, to_account += amount
  ↓
Update Account.initial_balance (или вычислять динамически)
```

### 3. Работа с совместными бюджетами

#### 3.1. Создание бюджета

```
User A → POST /api/v1/shared-budgets/
  { name, currency, description }
  ↓
Backend: Create SharedBudget → Generate Invite Code → 
  → Create SharedBudgetMember (role: ADMIN) → Response
  ↓
Response: { id, invite_code, ... }
```

#### 3.2. Присоединение к бюджету

```
User B → POST /api/v1/shared-budgets/join
  { invite_code }
  ↓
Backend: Find Budget by Code → 
  → Create SharedBudgetMember (role: MEMBER) → Response
  ↓
Response: SharedBudget Object
  ↓
User B: Access to Shared Accounts/Categories
```

#### 3.3. Транзакция в совместном бюджете

```
User → POST /api/v1/transactions/
  {
    account_id (shared), amount, shared_budget_id,
    distribution_method: "equal"
  }
  ↓
Backend: Create Transaction → 
  → Distribute Amount Between Members → 
  → Update Shared Account Balance
  ↓
Response: Transaction with Distribution Data
```

### 4. Создание цели с ИИ-планом

```
User → POST /api/v1/goals/
  { name, target_amount, currency, target_date }
  ↓
Backend: Create Goal → Create Goal Account → 
  → POST /api/v1/goals/generate-roadmap
  ↓
AI Assistant: Analyze User Data (transactions, income, expenses) → 
  → Generate Roadmap (JSON) → Save to Goal.roadmap
  ↓
Response: Goal with Roadmap
```

### 5. Запрос к ИИ-ассистенту

```
User → POST /api/v1/ai/ask
  { question: "На что я трачу больше всего?" }
  ↓
Backend: Get User Context (transactions, accounts, goals) → 
  → AI Assistant (Gemini API) → Generate Response
  ↓
Response: { answer: "..." }
```

### 6. Синхронизация между платформами

Все платформы (Frontend, Telegram Bot, VK Bot) работают через единый Backend API:

```
Platform A → Backend API → Database
                    ↓
Platform B ← Backend API ← Database (same data)
```

**Пример:**
1. Пользователь добавляет транзакцию через Telegram Bot
2. Транзакция сохраняется в БД
3. Пользователь открывает Frontend → видит новую транзакцию
4. Все данные синхронизированы через единый API

---

## 🔌 API эндпоинты

### Базовая структура

Все API эндпоинты находятся под префиксом `/api/v1/`

### Аутентификация

Все защищенные эндпоинты требуют JWT токен в заголовке:
```
Authorization: Bearer <token>
```

### Основные группы эндпоинтов

#### Authentication (`/api/v1/auth/`)
- `POST /register` — регистрация
- `POST /login` — вход
- `POST /telegram` — вход через Telegram
- `POST /vk` — вход через VK
- `GET /me` — текущий пользователь
- `POST /refresh` — обновление токена
- `POST /bot-token-telegram` — токен для Telegram бота
- `POST /bot-token-vk` — токен для VK бота

#### Transactions (`/api/v1/transactions/`)
- `GET /` — список транзакций (с фильтрами)
- `POST /` — создание транзакции
- `GET /{id}` — получение транзакции
- `PUT /{id}` — обновление транзакции
- `DELETE /{id}` — удаление транзакции

#### Accounts (`/api/v1/accounts/`)
- `GET /` — список счетов
- `POST /` — создание счета
- `GET /balance` — общий баланс
- `GET /{id}` — получение счета
- `PUT /{id}` — обновление счета
- `DELETE /{id}` — удаление счета

#### Categories (`/api/v1/categories/`)
- `GET /` — список категорий
- `POST /` — создание категории
- `PUT /{id}` — обновление категории
- `DELETE /{id}` — удаление категории

#### Shared Budgets (`/api/v1/shared-budgets/`)
- `GET /` — список бюджетов
- `POST /` — создание бюджета
- `GET /{id}` — получение бюджета
- `POST /{id}/join` — присоединение по коду
- `GET /{id}/members` — участники
- `DELETE /{id}/members/{user_id}` — удаление участника

#### Goals (`/api/v1/goals/`)
- `GET /` — список целей
- `POST /` — создание цели
- `POST /generate-roadmap` — генерация плана через ИИ
- `PUT /{id}` — обновление цели
- `DELETE /{id}` — удаление цели

#### Reports (`/api/v1/reports/`)
- `GET /summary` — сводный отчет
- `GET /by-category` — по категориям
- `GET /by-period` — по периодам
- `GET /export` — экспорт данных

#### AI (`/api/v1/ai/`)
- `POST /ask` — вопрос к ИИ
- `POST /analyze` — анализ финансов

#### Admin (`/api/v1/admin/`)
- `GET /users` — все пользователи
- `POST /users/{user_id}/reset` — сброс пользователя
- `GET /sync-admin-status` — синхронизация админ-статуса

---

## 🔗 Интеграции

### 1. Telegram Mini App

**Инициализация:**
```javascript
// Frontend получает initData от Telegram
const initData = window.Telegram.WebApp.initData;
// Отправка на Backend для авторизации
```

**Особенности:**
- Автоматическая авторизация при открытии Mini App
- Доступ к данным пользователя через Telegram API
- Поддержка тем (светлая/темная)

### 2. VK Mini App

Аналогично Telegram Mini App, но использует VK API

### 3. Google Gemini AI

**Конфигурация:**
- API Key в переменной окружения `GOOGLE_AI_API_KEY`
- Используется модель `gemini-pro`

**Использование:**
- Генерация планов достижения целей
- Ответы на вопросы пользователей
- Анализ финансовых данных

### 4. База данных

**PostgreSQL (Production):**
- Основная БД для production
- Поддержка транзакций
- Индексы для оптимизации

**SQLite (Development):**
- Используется для локальной разработки
- Файл БД: `backend/finance.db`

**Миграции:**
- Используется Alembic
- Автоматическое применение при старте (в production)

---

## 🔐 Безопасность и авторизация

### 1. JWT Токены

**Структура токена:**
```json
{
  "sub": "user_id",
  "exp": timestamp,
  "iat": timestamp
}
```

**Жизненный цикл:**
- Access Token: короткоживущий (15 минут)
- Refresh Token: долгоживущий (7 дней)
- Автоматическое обновление при истечении

### 2. Изоляция данных

**Принцип:** Каждый пользователь видит только свои данные

**Реализация:**
```python
# Все запросы фильтруются по user_id
transactions = db.query(Transaction).filter(
    Transaction.user_id == current_user.id
).all()
```

**Исключения:**
- Совместные бюджеты: участники видят общие данные
- Администраторы: доступ ко всем данным через `/api/v1/admin/`

### 3. Административный доступ

**Назначение администратора:**
- Через переменную окружения `ADMIN_TELEGRAM_IDS`
- Список Telegram ID администраторов
- Автоматическая синхронизация при входе

**Проверка прав:**
```python
def get_current_admin(current_user: User = Depends(get_current_user)):
    if not current_user.is_admin:
        raise HTTPException(403, "Admin access required")
    return current_user
```

### 4. CORS

**Настройки:**
- Разрешенные домены в `CORS_ORIGINS`
- Поддержка ngrok, Vercel, VK доменов через regex
- Credentials: разрешены

### 5. Rate Limiting

**Использование:** slowapi
**Ограничения:** По IP адресу

---

## 📊 Диаграммы потоков данных

### Создание транзакции (полный поток)

```
┌──────────┐
│  Client  │
│(Frontend)│
└────┬─────┘
     │ POST /api/v1/transactions/
     │ { account_id, amount, type, ... }
     ▼
┌──────────┐
│  Backend │
│  (FastAPI)│
└────┬─────┘
     │ 1. Validate Data
     │ 2. Check User Permissions
     │ 3. Create Transaction
     ▼
┌──────────┐
│Database  │
│(PostgreSQL)│
└────┬─────┘
     │ INSERT INTO transactions
     │ UPDATE accounts SET balance
     ▼
┌──────────┐
│ Response │
│Transaction│
└────┬─────┘
     │
     ▼
┌──────────┐
│  Client  │
│  Update  │
│    UI    │
└──────────┘
```

### Авторизация через Telegram

```
┌──────────┐
│Telegram  │
│ Mini App │
└────┬─────┘
     │ window.Telegram.WebApp.initData
     ▼
┌──────────┐
│ Frontend │
└────┬─────┘
     │ POST /api/v1/auth/telegram
     │ { init_data }
     ▼
┌──────────┐
│  Backend │
└────┬─────┘
     │ 1. Parse initData
     │ 2. Extract telegram_id
     │ 3. Find/Create User
     │ 4. Generate JWT Token
     ▼
┌──────────┐
│Database  │
└────┬─────┘
     │ SELECT/INSERT users
     ▼
┌──────────┐
│ Response │
│{token,   │
│ user}    │
└────┬─────┘
     │
     ▼
┌──────────┐
│ Frontend │
│ Save Token│
│Authenticated│
└──────────┘
```

### Работа с совместным бюджетом

```
User A                    User B
   │                         │
   │ Create Budget           │
   │ POST /shared-budgets/   │
   │                         │
   ▼                         │
┌──────────┐                 │
│ Backend  │                 │
│ Generate │                 │
│InviteCode│                 │
└────┬─────┘                 │
     │                       │
     │ Share Code            │
     ├───────────────────────►│
     │                       │
     │                       │ Join by Code
     │                       │ POST /join
     │                       ▼
     │                  ┌──────────┐
     │                  │ Backend  │
     │                  │ Add User │
     │                  │ as Member│
     │                  └────┬─────┘
     │                       │
     │                       │
     │  Both users can now:  │
     │  - See shared accounts│
     │  - Create transactions│
     │  - View shared data   │
     └───────────────────────┘
```

---

## 🛠️ Конфигурация

### Переменные окружения

**Backend:**
- `DATABASE_URL` — строка подключения к БД
- `SECRET_KEY` — секретный ключ для JWT
- `CORS_ORIGINS` — разрешенные домены для CORS
- `GOOGLE_AI_API_KEY` — ключ для Gemini API
- `ADMIN_TELEGRAM_IDS` — список Telegram ID администраторов

**Telegram Bot:**
- `TELEGRAM_BOT_TOKEN` — токен бота
- `BACKEND_URL` — URL Backend API

**VK Bot:**
- `VK_BOT_TOKEN` — токен сообщества VK
- `VK_GROUP_ID` — ID группы VK
- `BACKEND_URL` — URL Backend API

---

## 📝 Заключение

Данная документация описывает архитектуру, модули, потоки данных и интеграции системы Finance Manager. 

**Ключевые особенности системы:**
1. **Многоплатформенность** — единый Backend для Frontend, Telegram и VK
2. **Изоляция данных** — каждый пользователь видит только свои данные
3. **Совместные бюджеты** — поддержка группового управления финансами
4. **ИИ-интеграция** — интеллектуальные рекомендации и планирование
5. **Мультивалютность** — поддержка нескольких валют с конвертацией
6. **Расширяемость** — модульная архитектура для добавления новых функций

**Для дальнейшего изучения:**
- Исходный код модулей в `backend/app/`
- API документация: `http://backend-url/docs` (Swagger UI)
- Модели данных: `backend/app/models/`
- API эндпоинты: `backend/app/api/v1/`


