# Деплой на Vercel

Инструкция по деплою фронтенда на Vercel.

## 📋 Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Репозиторий на GitHub (https://github.com/jrPrzhe/luci.fin.git)
3. Backend должен быть задеплоен отдельно (например, на Railway, Render, или другом сервисе)

## 🚀 Быстрый деплой через Vercel CLI

### 1. Установите Vercel CLI

```bash
npm install -g vercel
```

### 2. Войдите в Vercel

```bash
vercel login
```

### 3. Деплой

Перейдите в папку frontend:

```bash
cd frontend
vercel
```

Следуйте инструкциям:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N** (для первого деплоя)
- Project name? **luci-fin** (или любое другое имя)
- Override settings? **N**

### 4. Продакшн деплой

После успешного деплоя, выполните:

```bash
vercel --prod
```

## 🌐 Деплой через веб-интерфейс Vercel

### 1. Подключите репозиторий

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **Add New Project**
3. Импортируйте репозиторий: `https://github.com/jrPrzhe/luci.fin.git`

### 2. Настройте проект

В настройках проекта:

- **Framework Preset**: Vite
- **Root Directory**: `frontend` ⚠️ **ВАЖНО**: Укажите `frontend` как корневую директорию
- **Build Command**: `npm run build` (будет автоматически определен)
- **Output Directory**: `dist` (будет автоматически определен)
- **Install Command**: `npm install` (будет автоматически определен)

**Примечание**: `vercel.json` находится в папке `frontend/`, поэтому в настройках Vercel обязательно укажите Root Directory как `frontend`.

### 3. Настройте переменные окружения

В разделе **Environment Variables** добавьте:

```
VITE_API_URL=https://your-backend-url.com
```

**Важно:** Замените `https://your-backend-url.com` на реальный URL вашего backend API.

### 4. Деплой

Нажмите **Deploy** и дождитесь завершения сборки.

## ⚙️ Переменные окружения

### Обязательные переменные

- `VITE_API_URL` - URL вашего backend API (например, `https://api.example.com`)

### Пример настройки

Если ваш backend развернут на `https://my-backend.railway.app`, то:

```
VITE_API_URL=https://my-backend.railway.app
```

Если backend находится на том же домене (через rewrites), можно оставить пустым или использовать относительный путь.

## 📁 Структура файлов

Проект использует следующую структуру:

```
finance-manager/
├── vercel.json          # Конфигурация Vercel
├── frontend/            # Фронтенд приложение
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
└── backend/             # Backend (деплоится отдельно)
```

## 🔧 Настройка CORS на Backend

Убедитесь, что ваш backend разрешает запросы с домена Vercel:

```python
# В вашем FastAPI приложении
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-vercel-app.vercel.app",
        "https://your-custom-domain.com",
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## 🌍 Кастомный домен

1. В настройках проекта Vercel перейдите в **Domains**
2. Добавьте ваш домен
3. Следуйте инструкциям для настройки DNS

## 📝 Проверка деплоя

После деплоя проверьте:

1. ✅ Приложение открывается
2. ✅ API запросы работают (проверьте Network в DevTools)
3. ✅ Авторизация работает
4. ✅ Все страницы загружаются

## 🐛 Устранение проблем

### Ошибка сборки

- Проверьте логи в Vercel Dashboard
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что TypeScript компилируется без ошибок

### API запросы не работают

- Проверьте переменную окружения `VITE_API_URL`
- Убедитесь, что backend доступен и CORS настроен правильно
- Проверьте Network tab в браузере для деталей ошибок

### 404 на страницах после обновления

- Убедитесь, что `vercel.json` содержит правильные rewrites
- Проверьте настройки роутинга в React Router

## 📚 Полезные ссылки

- [Документация Vercel](https://vercel.com/docs)
- [Vercel CLI](https://vercel.com/docs/cli)
- [Vite на Vercel](https://vercel.com/docs/frameworks/vite)

## 🔄 Обновление

После каждого push в `main` ветку, Vercel автоматически создаст новый деплой. Для продакшн обновлений используйте:

```bash
git push origin main
```

Или через Vercel Dashboard → Deployments → Redeploy

