# 🚂 Установка и подключение Railway CLI

## Шаг 1: Установка Railway CLI

### Windows (PowerShell или CMD):

```powershell
# Через npm (нужен Node.js)
npm install -g @railway/cli

# Или через winget (Windows 10/11)
winget install Railway.CLI

# Или через Scoop
scoop install railway
```

### Альтернативные способы установки:

**Через установщик:**
1. Перейдите на https://railway.app/cli
2. Скачайте установщик для Windows
3. Запустите установщик

**Через Chocolatey:**
```powershell
choco install railway
```

## Шаг 2: Проверка установки

```bash
railway --version
```

Должно показать версию, например: `railway version 3.x.x`

## Шаг 3: Авторизация

```bash
railway login
```

Это откроет браузер для авторизации через Railway аккаунт.

**Альтернатива (если браузер не открывается):**
```bash
railway login --browserless
```
Скопируйте ссылку из терминала и откройте в браузере вручную.

## Шаг 4: Подключение к проекту

### Вариант 1: Автоматическое подключение (если вы в директории проекта)

```bash
cd finance-manager
railway link
```

Railway спросит:
- Выберите проект (если у вас несколько)
- Выберите сервис (например, "BACKEND")

### Вариант 2: Указать проект вручную

```bash
railway link --project <project-id>
```

Project ID можно найти в Railway Dashboard → Settings → General

## Шаг 5: Проверка подключения

```bash
railway status
```

Должно показать информацию о текущем проекте и сервисе.

## Шаг 6: Выполнение команд

Теперь вы можете выполнять команды:

```bash
# Создать админа
railway run python backend/create_admin_user.py --email admin@test.com --password admin123456

# Проверить статус админа
railway run python backend/check_admin_status.py 7295487724

# Применить миграции
railway run alembic upgrade head

# Запустить любую Python команду
railway run python -c "print('Hello from Railway!')"
```

## 🔍 Полезные команды Railway CLI

```bash
# Просмотр переменных окружения
railway variables

# Добавить переменную
railway variables set ADMIN_TELEGRAM_IDS=7295487724

# Просмотр логов
railway logs

# Просмотр логов в реальном времени
railway logs --follow

# Просмотр информации о проекте
railway status

# Открыть проект в браузере
railway open

# Просмотр деплоев
railway deployments
```

## ⚠️ Troubleshooting

### Проблема: "railway: command not found"

**Решение:**
- Убедитесь, что Node.js установлен: `node --version`
- Переустановите CLI: `npm install -g @railway/cli`
- Перезапустите терминал

### Проблема: "Not logged in"

**Решение:**
```bash
railway login
```

### Проблема: "No project linked"

**Решение:**
```bash
railway link
```

### Проблема: Не могу найти проект

**Решение:**
1. Откройте Railway Dashboard
2. Найдите ваш проект
3. Settings → General → скопируйте Project ID
4. Выполните: `railway link --project <project-id>`

## 📝 Быстрый старт для создания админа

```bash
# 1. Установите Railway CLI (если еще не установлен)
npm install -g @railway/cli

# 2. Авторизуйтесь
railway login

# 3. Перейдите в директорию проекта
cd finance-manager

# 4. Подключитесь к проекту
railway link

# 5. Создайте админа
railway run python backend/create_admin_user.py --email admin@test.com --password admin123456
```

## 🎯 После создания админа

1. Откройте Swagger: `https://lucifin-production.up.railway.app/docs`
2. Войдите через `POST /api/v1/auth/login` с email и паролем
3. Скопируйте токен
4. Авторизуйтесь в Swagger через кнопку "Authorize"
5. Проверьте админские эндпоинты












