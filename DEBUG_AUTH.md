# 🔍 Диагностика ошибки авторизации

## Быстрая диагностика

### 1. Проверьте в браузере (DevTools)

1. Откройте `https://luci-fin-kb8p.vercel.app`
2. Нажмите **F12** для открытия DevTools
3. Перейдите на вкладку **Network**
4. Попробуйте авторизоваться
5. Найдите запрос к `/api/v1/auth/login`
6. Проверьте:
   - **Request URL**: должен быть `https://lucifin-production.up.railway.app/api/v1/auth/login`
   - **Status Code**: какой код ответа? (200, 401, 500, CORS error?)
   - **Request Headers**: должен быть `Content-Type: application/json`
   - **Request Payload**: должен содержать `{"email": "...", "password": "..."}`
   - **Response**: что приходит в ответе?

### 2. Типичные проблемы и решения

#### Проблема: CORS ошибка
**Симптомы**: В консоли браузера ошибка типа:
- `Access to fetch at '...' from origin '...' has been blocked by CORS policy`
- `No 'Access-Control-Allow-Origin' header is present`

**Решение**: 
1. Убедитесь, что на Railway установлена переменная `CORS_ORIGINS` со значением `https://luci-fin-kb8p.vercel.app`
2. Перезапустите backend сервис

#### Проблема: 404 Not Found
**Симптомы**: Status Code 404

**Решение**:
1. Проверьте, что `VITE_API_URL` на Vercel установлен в `https://lucifin-production.up.railway.app`
2. Перезапустите frontend деплой

#### Проблема: 401 Unauthorized
**Симптомы**: Status Code 401, сообщение "Incorrect email or password"

**Решение**:
1. Проверьте, что email и password правильные
2. Попробуйте зарегистрировать нового пользователя
3. Проверьте, что пользователь активен в базе данных

#### Проблема: 500 Internal Server Error
**Симптомы**: Status Code 500, ошибка на сервере

**Решение**:
1. Проверьте логи на Railway
2. Убедитесь, что база данных подключена
3. Проверьте переменные окружения на Railway

#### Проблема: Request failed (без деталей)
**Симптомы**: В консоли просто "Request failed"

**Решение**:
1. Откройте DevTools → Network
2. Найдите запрос к `/api/v1/auth/login`
3. Посмотрите на **Response** tab - там должна быть детальная ошибка
4. Проверьте, что URL правильный: `https://lucifin-production.up.railway.app/api/v1/auth/login`

### 3. Проверка конфигурации

#### Frontend (Vercel)
```bash
VITE_API_URL=https://lucifin-production.up.railway.app
```

#### Backend (Railway)
```bash
CORS_ORIGINS=https://luci-fin-kb8p.vercel.app
DATABASE_URL=postgresql://... (должна быть установлена)
SECRET_KEY=... (должен быть установлен)
```

### 4. Проверка запроса

Правильный запрос должен выглядеть так:

**URL**: `https://lucifin-production.up.railway.app/api/v1/auth/login`

**Headers**:
```
Content-Type: application/json
```

**Body**:
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**Response** (успешный):
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "user": {
    "id": 1,
    "email": "user@example.com",
    ...
  }
}
```

### 5. Логирование на backend

Если нужно добавить больше логов, проверьте файл `backend/app/api/v1/auth.py` - там уже есть логирование.

## Что сделать сейчас

1. **Откройте DevTools** (F12) → **Network**
2. **Попробуйте авторизоваться**
3. **Скопируйте**:
   - URL запроса
   - Status Code
   - Response (если есть)
   - Ошибку из консоли (Console tab)

Это поможет точно определить проблему!









