# Как настроить Root Directory в Railway

## 🎯 Проблема

Railway не может найти настройку **Root Directory** в интерфейсе.

## ✅ Решение

### Вариант 1: При создании нового сервиса (Рекомендуется)

1. **Создайте новый сервис:**
   - В вашем проекте Railway нажмите **+ New**
   - Выберите **GitHub Repo**
   - Выберите репозиторий `luci.fin`

2. **Настройте Root Directory:**
   - После выбора репозитория Railway покажет форму настройки
   - Найдите поле **Root Directory** или **Root Path**
   - Введите: `backend`
   - Нажмите **Deploy**

### Вариант 2: Через настройки существующего сервиса

1. **Откройте настройки сервиса:**
   - В вашем проекте Railway кликните на сервис `luci.fin`
   - Перейдите на вкладку **Settings**

2. **Найдите раздел Source:**
   - В правом меню **Quick Links** найдите и нажмите **Source**
   - Или прокрутите вниз до раздела **Source**

3. **Настройте Root Directory:**
   - В разделе **Source** должно быть поле **Root Directory**
   - Установите значение: `backend`
   - Нажмите **Save** или **Apply changes**

### Вариант 3: Если Root Directory не видно

Если в настройках нет поля Root Directory, это может означать:

1. **Railway определяет проект автоматически:**
   - Проверьте логи деплоя
   - Если ошибка "Error creating build plan", возможно Railway не может найти файлы в корне
   - В этом случае лучше пересоздать сервис с правильным Root Directory

2. **Используйте railway.json:**
   - Файл `railway.json` уже создан в `backend/`
   - Railway должен использовать его для конфигурации
   - Убедитесь что файл закоммичен в репозиторий

3. **Пересоздайте сервис:**
   - Удалите текущий сервис (Settings → Danger Zone → Delete Service)
   - Создайте новый через **+ New → GitHub Repo**
   - При настройке обязательно укажите **Root Directory: `backend`**

## 🔍 Проверка

После настройки Root Directory:

1. Убедитесь что в Settings → Source указано: `backend`
2. Нажмите **Deploy** для перезапуска деплоя
3. Проверьте логи деплоя - Railway должен найти файлы в папке `backend/`

## 📝 Альтернатива: Использование Dockerfile

Если проблема с Root Directory не решается:

1. В Settings → Build → Builder
2. Измените Builder с **Railpack Default** на **Dockerfile**
3. Railway будет использовать `backend/Dockerfile` для сборки
4. Dockerfile автоматически правильно настроен для вашего проекта

## 🆘 Если ничего не помогает

1. **Проверьте структуру репозитория:**
   ```
   luci.fin/
   └── backend/
       ├── runtime.txt
       ├── Procfile
       ├── railway.json
       ├── requirements.txt
       └── app/
   ```

2. **Убедитесь что файлы закоммичены:**
   ```bash
   git add backend/runtime.txt backend/Procfile backend/railway.json
   git commit -m "Add Railway config"
   git push origin main
   ```

3. **Создайте сервис заново:**
   - Удалите старый сервис
   - Создайте новый с Root Directory: `backend`
   - Railway автоматически определит проект

















