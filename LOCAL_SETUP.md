# Инструкция по локальному запуску проекта

Этот документ описывает, как запустить бэкенд, фронтенд и Telegram-бота локально (без Docker).

> **💡 Хотите запустить БЕЗ PostgreSQL и Redis?** Используйте [LOCAL_SETUP_SIMPLE.md](LOCAL_SETUP_SIMPLE.md) - там используется SQLite и не нужны дополнительные сервисы!

## 📋 Требования

- **Python 3.11+** - для бэкенда и бота
- **Node.js 18+** - для фронтенда
- **PostgreSQL 15+** - база данных (можно через Docker)
- **Redis** - для кэширования и очередей (можно через Docker)

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка базы данных и Redis

#### Вариант A: Через Docker (рекомендуется)

```bash
# Запустить только PostgreSQL и Redis
docker run -d \
  --name postgres \
  -e POSTGRES_USER=finance_user \
  -e POSTGRES_PASSWORD=finance_password \
  -e POSTGRES_DB=finance_db \
  -p 5432:5432 \
  postgres:15

docker run -d \
  --name redis \
  -p 6379:6379 \
  redis:7-alpine
```

#### Вариант B: Локальная установка

Установите PostgreSQL и Redis локально на вашей системе.

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Безопасность (ОБЯЗАТЕЛЬНО сгенерируйте свой ключ!)
SECRET_KEY=your-very-secret-key-here-please-change-me-generate-random-string

# База данных (для локального запуска)
DATABASE_URL=postgresql://finance_user:finance_password@localhost:5432/finance_db

# Redis
REDIS_URL=redis://localhost:6379/0

# Google AI Studio API Key (для ИИ-функций, опционально)
GOOGLE_AI_API_KEY=your-google-ai-api-key-here

# Telegram Bot Token (ОБЯЗАТЕЛЬНО для бота)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# CORS - адреса фронтенда
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend URL
FRONTEND_URL=http://localhost:5173
```

**Как получить Telegram Bot Token:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### Шаг 3: Настройка и запуск Backend

```bash
# Перейдите в директорию backend
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Примените миграции базы данных
alembic upgrade head

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: **http://localhost:8000**

API документация: **http://localhost:8000/docs**

### Шаг 4: Настройка и запуск Frontend

Откройте **новый терминал**:

```bash
# Перейдите в директорию frontend
cd frontend

# Установите зависимости
npm install

# Запустите dev-сервер
npm run dev
```

Frontend будет доступен по адресу: **http://localhost:5173**

### Шаг 5: Настройка и запуск Telegram Bot

Откройте **еще один терминал**:

```bash
# Перейдите в директорию telegram-bot
cd telegram-bot

# Создайте виртуальное окружение (или используйте то же, что для backend)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Убедитесь, что в корне проекта есть .env файл с TELEGRAM_BOT_TOKEN
# Или создайте .env в директории telegram-bot:
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
# BACKEND_URL=http://localhost:8000

# Запустите бота
python bot.py
```

Бот запустится и будет слушать команды в Telegram.

## ✅ Проверка работы

### Backend
Откройте в браузере:
- http://localhost:8000/health - должен вернуть `{"status": "healthy"}`
- http://localhost:8000/docs - Swagger UI документация

### Frontend
Откройте в браузере:
- http://localhost:5173 - должна открыться страница входа

### Telegram Bot
1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветствием

## 📝 Полезные команды

### Backend

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "описание изменений"

# Применить миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Остановить сервер
Ctrl+C
```

### Frontend

```bash
# Остановить dev-сервер
Ctrl+C

# Сборка для продакшена
npm run build

# Просмотр продакшен сборки
npm run preview
```

### Telegram Bot

```bash
# Остановить бота
Ctrl+C
```

## 🔧 Решение проблем

### Backend не запускается

**Проблема:** Ошибка подключения к базе данных
```bash
# Проверьте, что PostgreSQL запущен
# Windows (если через Docker):
docker ps
# Должен быть виден контейнер postgres

# Проверьте DATABASE_URL в .env файле
# Должен быть: postgresql://finance_user:finance_password@localhost:5432/finance_db
```

**Проблема:** Ошибка импорта модулей
```bash
# Убедитесь, что вы активировали виртуальное окружение
# И установили все зависимости:
pip install -r requirements.txt
```

### Frontend не запускается

**Проблема:** Ошибка `npm install`
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
```

**Проблема:** Ошибка подключения к API
- Убедитесь, что backend запущен на порту 8000
- Проверьте настройки CORS в backend/.env

### Telegram Bot не отвечает

**Проблема:** Бот не запускается
```bash
# Проверьте, что TELEGRAM_BOT_TOKEN указан в .env
# Токен должен начинаться с цифр, например: 1234567890:ABCdef...

# Проверьте логи бота на наличие ошибок
```

**Проблема:** Бот не подключается к backend
- Убедитесь, что backend запущен
- Проверьте BACKEND_URL в .env (должен быть http://localhost:8000)

## 📂 Структура терминалов

Для удобной работы откройте **3 терминала**:

**Терминал 1 - Backend:**
```bash
cd backend
venv\Scripts\activate  # Windows
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm install
npm run dev
```

**Терминал 3 - Telegram Bot:**
```bash
cd telegram-bot
venv\Scripts\activate  # Windows
pip install -r requirements.txt
python bot.py
```

## 🎉 Готово!

Теперь все три компонента запущены локально:
- ✅ Backend: http://localhost:8000
- ✅ Frontend: http://localhost:5173
- ✅ Telegram Bot: работает и отвечает в Telegram

Удачи в разработке! 🚀

