# Деплой Telegram бота на Railway

## 🎯 Обзор

Telegram бот работает в режиме **polling** - это означает:
- ✅ Бот сам запрашивает обновления от Telegram API
- ✅ Не требует HTTPS или webhook
- ✅ Простая настройка и деплой
- ✅ Идеально подходит для production

## 📋 Предварительные требования

1. ✅ Backend уже задеплоен на Railway (см. `BACKEND_DEPLOY.md`)
2. ✅ У вас есть Telegram Bot Token от @BotFather
3. ✅ Backend URL доступен (например: `https://your-backend.railway.app`)

## 🚀 Деплой на Railway

### Шаг 1: Создайте новый сервис для бота

1. Откройте ваш проект на Railway
2. В проекте нажмите **+ New** или **New Service**
3. Выберите **Deploy from GitHub repo**
4. Выберите тот же репозиторий (`luci.fin`)
5. **ВАЖНО:** После выбора репозитория:
   - Railway может предложить настройки сразу
   - Если видите опцию **Root Directory**, установите: `telegram-bot`
   - Если не видите опцию сразу, продолжите - настроим позже

### Шаг 1.5: Настройте Root Directory (КРИТИЧЕСКИ ВАЖНО!)

**Если Root Directory не установлен, вы получите ошибку:**
```
ERROR: "/requirements.txt": not found
```

**Как установить Root Directory:**

**Способ 1: Через Settings → Source**
1. Откройте созданный сервис для бота
2. Перейдите в **Settings** (Настройки)
3. В правом меню найдите **Source** (или **Quick Links** → **Source**)
4. В разделе **Source** найдите поле **Root Directory**
5. Установите значение: `telegram-bot`
6. Нажмите **Save** или **Apply changes**

**Способ 2: Если Root Directory не видно**
1. В Settings → **Build** проверьте настройки Builder
2. Root Directory может быть в других настройках
3. Если не можете найти, попробуйте пересоздать сервис и указать Root Directory при создании

**Важно:** После установки Root Directory Railway автоматически перезапустит деплой

### Шаг 2: Настройте Builder

1. Откройте созданный сервис
2. Перейдите в **Settings** → **Build**
3. В разделе **Builder** выберите **Dockerfile**
4. Railway автоматически найдет `telegram-bot/Dockerfile`

### Шаг 3: Настройте переменные окружения

В разделе **Variables** добавьте следующие переменные:

#### Обязательные переменные:

```env
# Telegram Bot Token (от @BotFather)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# URL вашего backend на Railway
BACKEND_URL=https://your-backend.railway.app
```

**Как получить TELEGRAM_BOT_TOKEN:**
1. Найдите @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Перейдите в **API Token**
5. Скопируйте токен

**Важно:** 
- Замените `your-backend.railway.app` на реальный URL вашего backend
- Убедитесь что URL начинается с `https://` (не `http://`)
- Не добавляйте слэш в конце (`/`)

### Шаг 4: Проверьте Dockerfile

Убедитесь что `telegram-bot/Dockerfile` существует и содержит:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application
COPY . .

# Run bot
CMD ["python", "bot.py"]
```

### Шаг 5: Деплой

1. Railway автоматически начнет деплой после создания сервиса
2. Или нажмите **Deploy** вручную
3. Дождитесь завершения сборки (обычно 2-3 минуты)

### Шаг 6: Проверьте логи

1. Откройте **Deployments** → **View Logs**
2. Вы должны увидеть:
   ```
   Starting Telegram bot in polling mode...
   Polling mode works on HTTP (no HTTPS required)
   ```
3. Если бот запустился успешно, вы увидите сообщение о начале работы

### Шаг 7: Проверьте работу бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить

## 🔧 Альтернативный вариант: Использование Nixpacks (если Dockerfile не работает)

Если у вас проблемы с Dockerfile, можно использовать Nixpacks:

### 1. Создайте Procfile

Создайте файл `telegram-bot/Procfile`:

```
worker: python bot.py
```

### 2. Создайте runtime.txt

Создайте файл `telegram-bot/runtime.txt`:

```
python-3.11.0
```

### 3. Настройте Railway

1. В Settings → Build → Builder выберите **Nixpacks** (или **Railpack Default**)
2. Railway автоматически определит Python проект

## 🐛 Решение проблем

### Ошибка: "TELEGRAM_BOT_TOKEN is not set!"

**Решение:**
1. Проверьте что переменная `TELEGRAM_BOT_TOKEN` установлена в Variables
2. Убедитесь что значение не содержит пробелов
3. Перезапустите деплой после добавления переменной

### Ошибка: "Failed to connect to backend"

**Решение:**
1. Проверьте что `BACKEND_URL` правильный (должен быть `https://your-backend.railway.app`)
2. Убедитесь что backend запущен и доступен
3. Проверьте что backend не требует авторизации для health check (эндпоинт `/health`)

### Ошибка: "requirements.txt not found" / "/requirements.txt": not found

**Симптомы:**
```
ERROR: failed to build: failed to solve: failed to compute cache key: 
failed to calculate checksum of ref ... "/requirements.txt": not found
```

**Причина:** Root Directory не установлен или установлен неправильно.

**Решение:**
1. ✅ **Проверьте Root Directory:**
   - Settings → Source → Root Directory должен быть: `telegram-bot`
   - Если пусто или другое значение - установите `telegram-bot`

2. ✅ **Проверьте что файлы существуют:**
   - Убедитесь что файл `telegram-bot/requirements.txt` существует в репозитории
   - Проверьте что файл закоммичен и запушен в GitHub

3. ✅ **Перезапустите деплой:**
   - После изменения Root Directory нажмите **Redeploy**
   - Или Railway автоматически перезапустит после сохранения

4. ✅ **Проверьте настройки Builder:**
   - Settings → Build → Builder должен быть: **Dockerfile**
   - Dockerfile Path должен быть: `Dockerfile` (или пусто, если Root Directory = `telegram-bot`)

**Если проблема сохраняется:**
- Попробуйте пересоздать сервис
- При создании сразу укажите Root Directory: `telegram-bot`

### Ошибка: "Dockerfile not found"

**Решение:**
1. Проверьте что **Root Directory** установлен в `telegram-bot`
2. Убедитесь что файл `telegram-bot/Dockerfile` существует
3. Если используете Dockerfile, убедитесь что Builder установлен как **Dockerfile**

### Бот не отвечает

**Проверьте:**
1. Логи Railway - нет ли ошибок
2. Telegram Bot Token правильный
3. Бот запущен (см. логи)
4. Backend доступен (проверьте URL в браузере)

### Бот работает, но не может подключиться к backend

**Проверьте:**
1. `BACKEND_URL` правильный (с `https://`)
2. Backend доступен (откройте `https://your-backend.railway.app/health` в браузере)
3. CORS на backend разрешает запросы от бота (не требуется, но проверьте если есть проблемы)

## 📊 Мониторинг

### Логи

Railway автоматически сохраняет логи бота:
- Откройте **Deployments** → **View Logs**
- Вы увидите все сообщения от бота

### Проверка работы

Чтобы проверить что бот работает:
1. Отправьте боту `/help` в Telegram
2. Бот должен ответить списком команд
3. Если бот не отвечает, проверьте логи

## 🔄 Обновление бота

После изменений в коде:

1. Закоммитьте и запушьте изменения в GitHub
2. Railway автоматически перезапустит бота
3. Или нажмите **Redeploy** вручную

## 💰 Стоимость

Railway бесплатный тариф включает:
- ✅ До $5 кредитов в месяц
- ✅ Достаточно для одного Telegram бота (бот потребляет мало ресурсов)

## 🎯 Структура проекта

После деплоя у вас будет 3 сервиса на Railway:

```
Railway Project
├── Backend Service (FastAPI)
│   ├── Root Directory: backend
│   ├── URL: https://your-backend.railway.app
│   └── Database: PostgreSQL
│
└── Telegram Bot Service
    ├── Root Directory: telegram-bot
    ├── URL: не требуется (polling режим)
    └── Variables: TELEGRAM_BOT_TOKEN, BACKEND_URL
```

## 📝 Важные замечания

1. **Polling vs Webhook:**
   - Текущий бот использует **polling** - это нормально для production
   - Polling работает на HTTP и не требует HTTPS
   - Webhook требует HTTPS, но не нужен для работы бота

2. **Backend URL:**
   - Бот должен иметь доступ к backend
   - Используйте публичный URL backend (Railway предоставляет автоматически)
   - Убедитесь что backend доступен из интернета

3. **Telegram Bot Token:**
   - Храните токен в секрете (в Variables, не в коде)
   - Не коммитьте токен в Git
   - Если токен скомпрометирован, создайте новый через @BotFather

## 🎉 Готово!

После завершения деплоя:
- ✅ Backend работает на Railway
- ✅ Frontend работает на Vercel
- ✅ Telegram бот работает на Railway
- ✅ Все компоненты связаны и работают вместе

**Проверьте работу:**
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить и работать корректно

---

**Нужна помощь?** Проверьте логи в Railway и убедитесь что все переменные окружения установлены правильно.

