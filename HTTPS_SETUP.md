# Настройка HTTPS для Telegram Mini App

Инструкция по настройке HTTPS на localhost для разработки Telegram Mini App.

## 🎯 Зачем это нужно?

Telegram Mini App требует HTTPS для работы. Для локальной разработки мы используем самоподписанный сертификат.

## 📋 Быстрая настройка

### Шаг 1: Создайте SSL сертификат

**Вариант A: Используя OpenSSL (рекомендуется)**

Если у вас установлен OpenSSL:
```bash
cd backend
create_cert_openssl.bat
```

Или вручную:
```bash
openssl req -x509 -newkey rsa:2048 -keyout localhost.key -out localhost.crt -days 365 -nodes -subj "/CN=localhost"
```

**Вариант B: Используя PowerShell**

```powershell
# Запустите PowerShell от имени администратора
cd backend
.\create_cert.ps1
```

**Вариант C: Автоматический скрипт**

```bash
cd backend
create_cert_simple.bat
```

### Шаг 2: Проверьте созданные файлы

После выполнения скрипта должны появиться файлы:
- `localhost.crt` - сертификат
- `localhost.key` - приватный ключ

### Шаг 3: Запустите backend с HTTPS

```bash
cd backend
python run_https.py
```

Backend будет доступен по адресу: **https://localhost:8443**

### Шаг 4: Разрешите сертификат в браузере

При первом открытии браузер покажет предупреждение о безопасности:
1. Нажмите **"Дополнительно"** / **"Advanced"**
2. Нажмите **"Перейти на localhost (небезопасно)"** / **"Proceed to localhost"**

Это нормально для самоподписанных сертификатов.

## 🔧 Настройка Telegram Mini App

### Шаг 1: Создайте HTTPS туннель (ngrok)

Так как Telegram Mini App должен быть доступен из интернета, используйте ngrok:

```bash
# Установите ngrok: https://ngrok.com/download
# Запустите туннель:
ngrok http 8443
```

Вы получите HTTPS URL вида: `https://xxxx-xx-xxx-xxx-xx.ngrok-free.app`

### Шаг 2: Настройте Mini App в Telegram

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** -> **"Menu Button"**
5. Введите URL вашего фронтенда через ngrok:
   ```
   https://xxxx-xx-xxx-xxx-xx.ngrok-free.app
   ```

Или создайте новую Mini App:
```
/newapp
```

### Шаг 3: Обновите фронтенд

Убедитесь, что фронтенд может работать по HTTPS URL от ngrok.

## 📝 Структура файлов

```
backend/
├── localhost.crt      # SSL сертификат (не коммитится)
├── localhost.key      # Приватный ключ (не коммитится)
├── run_https.py       # Скрипт запуска с HTTPS
├── create_cert.ps1    # PowerShell скрипт создания сертификата
└── create_cert_openssl.bat  # Batch скрипт для OpenSSL
```

## 🔐 Безопасность

⚠️ **Важно:**
- Самоподписанные сертификаты **НЕ подходят для продакшена**
- Файлы `.crt`, `.key`, `.pfx` добавлены в `.gitignore` - не коммитьте их!
- Используйте только для локальной разработки

## 🚀 Полный процесс запуска

### Терминал 1: Backend (HTTPS)
```bash
cd backend
python run_https.py
```

### Терминал 2: ngrok
```bash
ngrok http 8443
```

### Терминал 3: Frontend
```bash
cd frontend
npm run dev
```

### Терминал 4: Telegram Bot
```bash
cd telegram-bot
python bot.py
```

## 🐛 Решение проблем

### Ошибка "SSL certificate not found"

Убедитесь, что файлы `localhost.crt` и `localhost.key` находятся в директории `backend/`.

### Ошибка "Port 8443 already in use"

Измените порт в `run_https.py`:
```python
uvicorn.run(..., port=8444, ...)  # Используйте другой порт
```

И обновите ngrok:
```bash
ngrok http 8444
```

### OpenSSL не найден

**Windows:**
1. Скачайте: https://slproweb.com/products/Win32OpenSSL.html
2. Установите
3. Добавьте в PATH

**Или используйте PowerShell метод** (см. `create_cert.ps1`)

### Браузер не доверяет сертификату

Это нормально! Самоподписанные сертификаты вызывают предупреждение. Просто нажмите "Продолжить" в браузере.

## 📚 Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [ngrok Documentation](https://ngrok.com/docs)
- [OpenSSL Windows](https://slproweb.com/products/Win32OpenSSL.html)

## ✅ Проверка работы

1. Backend доступен: https://localhost:8443/docs
2. ngrok туннель активен: проверьте в терминале ngrok
3. Frontend доступен через ngrok URL
4. В Telegram бот имеет настроенную Mini App

Удачи! 🚀

