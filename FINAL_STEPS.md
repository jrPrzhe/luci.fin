# Финальные шаги после настройки БД

База данных настроена, миграции применены! Теперь нужно запустить приложение и проверить работу.

## Шаг 1: Проверка созданных таблиц

Проверьте, что все таблицы созданы:

```bash
# Подключитесь к БД и проверьте таблицы
psql -h 195.43.142.121 -U finance_user -d finance_db -c "\dt"
```

Или используйте Python:

```bash
python -c "from app.core.database import engine; from sqlalchemy import inspect; inspector = inspect(engine); print('Таблицы:', inspector.get_table_names())"
```

## Шаг 2: Создание тестового пользователя (опционально)

Создайте тестового пользователя для проверки работы:

```bash
python create_test_user.py
```

Или создайте администратора:

```bash
python create_admin_user.py
```

## Шаг 3: Запуск приложения

Запустите backend сервер:

```bash
python -m uvicorn app.main:app --reload
```

Приложение будет доступно по адресу:
- **HTTP**: http://localhost:8000
- **API документация**: http://localhost:8000/docs
- **Альтернативная документация**: http://localhost:8000/redoc

## Шаг 4: Проверка работы API

1. Откройте в браузере: http://localhost:8000/docs
2. Попробуйте выполнить тестовый запрос:
   - Проверьте endpoint `/health` или `/` (если есть)
   - Попробуйте зарегистрировать пользователя через `/api/v1/auth/register`

## Шаг 5: Проверка подключения к БД из приложения

После запуска приложения проверьте логи - не должно быть ошибок подключения к БД.

## Готово! 🎉

Теперь ваш проект полностью настроен и работает с удаленной базой данных PostgreSQL!

## Полезные команды

### Проверка статуса БД
```bash
python test_db_connection.py
```

### Просмотр таблиц
```bash
psql -h 195.43.142.121 -U finance_user -d finance_db -c "\dt"
```

### Просмотр структуры таблицы
```bash
psql -h 195.43.142.121 -U finance_user -d finance_db -c "\d users"
```

### Создание нового пользователя через API
```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "password": "test123456", "username": "testuser"}'
```

## Устранение проблем

### Ошибка при запуске приложения

1. Проверьте, что все зависимости установлены:
   ```bash
   pip install -r requirements.txt
   ```

2. Проверьте файл `.env` - правильный ли пароль

3. Проверьте подключение к БД:
   ```bash
   python test_db_connection.py
   ```

### Ошибка "Table does not exist"

Если таблицы не созданы, примените миграции заново:
```bash
python run_migrations.py
```

### Ошибка подключения к БД

1. Проверьте, что PostgreSQL запущен на сервере
2. Проверьте файрвол (если используется)
3. Проверьте правильность пароля в `.env`






