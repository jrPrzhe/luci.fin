# Изоляция данных пользователей по Telegram ID

## ✅ Реализовано

### 1. Автоматическое создание пользователя

При первом открытии Mini App:
- Пользователь автоматически создается по `telegram_id` из Telegram Web App API
- Создается уникальный email: `tg_{telegram_id}@telegram.local`
- Создается username на основе Telegram username или `tg_{telegram_id}`
- Пользователь автоматически считается верифицированным (`is_verified=True`)
- Создается дефолтный счет "Основной счёт" с балансом 0

### 2. Изоляция данных

**Все API эндпоинты фильтруют данные по текущему пользователю:**

#### Accounts (Счета)
- ✅ `GET /api/v1/accounts/` - возвращает только счета текущего пользователя
- ✅ `GET /api/v1/accounts/balance` - возвращает баланс только текущего пользователя
- ✅ `POST /api/v1/accounts/` - создает счет только для текущего пользователя
- ✅ Баланс рассчитывается только из транзакций текущего пользователя

#### Transactions (Транзакции)
- ✅ `GET /api/v1/transactions/` - возвращает только транзакции текущего пользователя
- ✅ `POST /api/v1/transactions/` - создает транзакцию только для текущего пользователя
- ✅ При создании транзакции проверяется, что счет принадлежит текущему пользователю

### 3. Безопасность

- Все запросы требуют JWT токен (через `get_current_user`)
- Токен содержит `user.id` и проверяется при каждом запросе
- Данные фильтруются по `user_id` на уровне базы данных
- Невозможно получить доступ к данным другого пользователя

## 🔧 Как это работает

### Авторизация через Telegram

1. Пользователь открывает Mini App в Telegram
2. Frontend получает `initData` от Telegram Web App API
3. Frontend отправляет `initData` на `/api/v1/auth/telegram`
4. Backend:
   - Парсит JSON данные из `initData`
   - Извлекает `telegram_id`, `username`, `first_name`, `last_name`
   - Ищет пользователя по `telegram_id`
   - Если не найден - создает нового пользователя
   - Если найден - обновляет информацию (если изменилась)
   - Создает дефолтный счет для новых пользователей
   - Генерирует JWT токен
5. Frontend сохраняет токен и использует для всех последующих запросов

### Изоляция данных

При каждом запросе к API:
1. Из JWT токена извлекается `user.id`
2. Запросы к базе данных фильтруются по `user_id == current_user.id`
3. Пользователь видит только свои данные

## 📊 Структура данных

### User (Пользователь)
- `id` - внутренний ID пользователя
- `telegram_id` - уникальный ID из Telegram (используется для поиска/создания)
- `email` - `tg_{telegram_id}@telegram.local`
- `username` - Telegram username или `tg_{telegram_id}`
- `first_name`, `last_name` - из Telegram

### Account (Счет)
- `user_id` - привязка к пользователю (Foreign Key)
- Фильтруется по `user_id == current_user.id`

### Transaction (Транзакция)
- `user_id` - привязка к пользователю (Foreign Key)
- `account_id` - привязка к счету (проверяется принадлежность текущему пользователю)
- Фильтруется по `user_id == current_user.id`

## ✅ Тестирование

Для проверки изоляции:
1. Откройте Mini App с одного Telegram аккаунта
2. Создайте транзакцию
3. Откройте Mini App с другого Telegram аккаунта
4. Убедитесь, что данные не пересекаются

## 🎯 Результат

- ✅ Каждый пользователь имеет свой изолированный набор данных
- ✅ Автоматическое создание пользователя при первом входе
- ✅ Невозможность доступа к данным других пользователей
- ✅ Все запросы автоматически фильтруются по текущему пользователю

