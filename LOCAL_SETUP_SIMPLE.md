# Инструкция по локальному запуску БЕЗ PostgreSQL и Redis

Эта инструкция описывает запуск проекта с использованием SQLite (встроенная база данных) без необходимости устанавливать PostgreSQL и Redis.

## ✅ Преимущества

- **Не требует установки PostgreSQL** - используется SQLite
- **Не требует установки Redis** - не нужен для базовой функциональности
- **Быстрый старт** - минимальная настройка
- **Идеально для разработки** - все данные в одном файле

## 📋 Требования

- **Python 3.11+** - для бэкенда и бота
- **Node.js 18+** - для фронтенда

**Ничего больше не требуется!** SQLite входит в стандартную поставку Python.

## 🚀 Пошаговая инструкция

### Шаг 1: Настройка переменных окружения

Создайте файл `.env` в корне проекта (или просто запустите проект - будут использованы значения по умолчанию):

```env
# Безопасность (ОБЯЗАТЕЛЬНО измените для продакшена!)
SECRET_KEY=your-secret-key-here-generate-random-string

# База данных - SQLite (файл создастся автоматически)
# Можно не указывать - будет использовано значение по умолчанию
DATABASE_URL=sqlite:///./finance.db

# Telegram Bot Token (ОБЯЗАТЕЛЬНО для бота)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Google AI Studio API Key (опционально, для ИИ-функций)
GOOGLE_AI_API_KEY=your-google-ai-api-key-here

# CORS - адреса фронтенда
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend URL
FRONTEND_URL=http://localhost:5173
```

**Как получить Telegram Bot Token:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### Шаг 2: Настройка и запуск Backend

```bash
# Перейдите в директорию backend
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows PowerShell:
.\venv\Scripts\Activate.ps1
# Если ошибка о политике выполнения, сначала выполните:
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# 
# Windows CMD:
venv\Scripts\activate.bat
#
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Примените миграции базы данных (создаст файл finance.db)
alembic upgrade head

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: **http://localhost:8000**

API документация: **http://localhost:8000/docs**

**База данных SQLite** будет создана автоматически в файле `backend/finance.db`

### Шаг 3: Настройка и запуск Frontend

Откройте **новый терминал**:

```bash
# Перейдите в директорию frontend
cd frontend

# Установите зависимости
npm install

# Запустите dev-сервер
npm run dev
```

Frontend будет доступен по адресу: **http://localhost:5173**

### Шаг 4: Настройка и запуск Telegram Bot

Откройте **еще один терминал**:

```bash
# Перейдите в директорию telegram-bot
cd telegram-bot

# Создайте виртуальное окружение (или используйте то же, что для backend)
python -m venv venv

# Активируйте виртуальное окружение
# Windows PowerShell:
.\venv\Scripts\Activate.ps1
# Если ошибка о политике выполнения, сначала выполните:
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# 
# Windows CMD:
venv\Scripts\activate.bat
#
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Убедитесь, что в корне проекта есть .env файл с TELEGRAM_BOT_TOKEN
# Или создайте .env в директории telegram-bot:
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
# BACKEND_URL=http://localhost:8000

# Запустите бота
python bot.py
```

Бот запустится и будет слушать команды в Telegram.

## ✅ Проверка работы

### Backend
Откройте в браузере:
- http://localhost:8000/health - должен вернуть `{"status": "healthy"}`
- http://localhost:8000/docs - Swagger UI документация

Проверьте, что файл базы данных создан:
- `backend/finance.db` - должен существовать

### Frontend
Откройте в браузере:
- http://localhost:5173 - должна открыться страница входа

### Telegram Bot
1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветствием

## 📝 Полезные команды

### Backend

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "описание изменений"

# Применить миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Просмотреть содержимое базы данных SQLite (опционально)
sqlite3 finance.db
.tables  # показать таблицы
.quit    # выйти

# Остановить сервер
Ctrl+C
```

### Frontend

```bash
# Остановить dev-сервер
Ctrl+C

# Сборка для продакшена
npm run build
```

### Telegram Bot

```bash
# Остановить бота
Ctrl+C
```

## 🔧 Решение проблем

### Backend не запускается

**Проблема:** Ошибка импорта модулей
```bash
# Убедитесь, что вы активировали виртуальное окружение
# И установили все зависимости:
pip install -r requirements.txt
```

**Проблема:** Ошибка с базой данных
```bash
# Удалите файл базы данных и пересоздайте:
rm finance.db  # или на Windows: del finance.db
alembic upgrade head
```

**Проблема:** SECRET_KEY не указан
```bash
# Добавьте в .env файл:
SECRET_KEY=любой-случайный-строковый-ключ
# Или используйте значение по умолчанию для разработки
```

### Frontend не запускается

**Проблема:** Ошибка `npm install`
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
```

**Проблема:** Ошибка подключения к API
- Убедитесь, что backend запущен на порту 8000
- Проверьте настройки CORS в backend (по умолчанию разрешен localhost:5173)

### Telegram Bot не отвечает

**Проблема:** Бот не запускается
```bash
# Проверьте, что TELEGRAM_BOT_TOKEN указан в .env
# Токен должен начинаться с цифр, например: 1234567890:ABCdef...
```

**Проблема:** Бот не подключается к backend
- Убедитесь, что backend запущен
- Проверьте BACKEND_URL в .env (должен быть http://localhost:8000)

## 📂 Структура терминалов

Для удобной работы откройте **3 терминала**:

**Терминал 1 - Backend:**
```bash
cd backend
venv\Scripts\activate  # Windows
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm install
npm run dev
```

**Терминал 3 - Telegram Bot:**
```bash
cd telegram-bot
venv\Scripts\activate  # Windows
pip install -r requirements.txt
python bot.py
```

## 📁 Файловая структура после запуска

После запуска у вас будет:
```
finance-manager/
├── backend/
│   ├── finance.db          # ← База данных SQLite (создается автоматически)
│   └── ...
├── frontend/
│   └── ...
├── telegram-bot/
│   └── ...
└── .env                    # ← Файл с настройками
```

## ⚠️ Ограничения SQLite

SQLite отлично подходит для разработки, но имеет некоторые ограничения:

- **Нет одновременной записи** - несколько процессов не могут писать одновременно
- **Меньше функций** - некоторые функции PostgreSQL недоступны
- **Медленнее для больших данных** - PostgreSQL лучше для больших объемов

**Для продакшена рекомендуется использовать PostgreSQL.**

## 🎉 Готово!

Теперь все три компонента запущены локально **без необходимости устанавливать PostgreSQL и Redis**:
- ✅ Backend: http://localhost:8000 (с SQLite)
- ✅ Frontend: http://localhost:5173
- ✅ Telegram Bot: работает и отвечает в Telegram

Удачи в разработке! 🚀

