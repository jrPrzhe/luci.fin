# 🔴 КРИТИЧНО: Исправление BACKEND_URL для Telegram бота

## Проблема

В логах бота видно:
```
⚠️ BACKEND_URL seems to be a placeholder: https://your-backend.railway.app
POST https://your-backend.railway.app/api/v1/auth/bot-token "HTTP/1.1 404 Not Found"
```

## Решение (ШАГ ЗА ШАГОМ)

### Шаг 1: Найдите URL вашего Backend на Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Откройте ваш проект
3. Найдите **Backend Service** (не Telegram Bot!)
4. Нажмите на сервис
5. В разделе **Settings** → **Networking** или в верхней части страницы найдите **Public Domain**
6. Скопируйте URL (пример: `https://your-backend-production.up.railway.app`)
   - Или `https://your-backend.railway.app` если у вас есть кастомный домен

### Шаг 2: Установите BACKEND_URL в Telegram Bot Service

1. В том же проекте Railway найдите **Telegram Bot Service**
2. Откройте сервис
3. Перейдите в **Variables** (или **Settings** → **Variables**)
4. Найдите переменную `BACKEND_URL`
5. Нажмите на неё для редактирования (или создайте новую, если её нет)
6. **Замените значение** на URL из Шага 1
7. **ВАЖНО:** 
   - ✅ Правильно: `https://your-backend-production.up.railway.app`
   - ❌ Неправильно: `https://your-backend.railway.app` (плейсхолдер)
   - ❌ Неправильно: `https://...railway.app/` (со слэшем в конце)

### Шаг 3: Проверьте что backend доступен

Откройте в браузере URL вашего backend:
```
https://your-backend-production.up.railway.app/health
```

Должно вернуть:
```json
{"status": "healthy"}
```

Если не работает - backend не запущен или URL неправильный.

### Шаг 4: Перезапустите Telegram Bot

После изменения переменной:
1. Railway автоматически перезапустит бота
2. Или нажмите **Redeploy** вручную

### Шаг 5: Проверьте логи

После перезапуска в логах бота должно быть:
```
Backend URL configured: https://your-backend-production.up.railway.app
```

**НЕ должно быть:**
- ❌ `⚠️ BACKEND_URL seems to be a placeholder`
- ❌ `404 Not Found` при запросе к `/api/v1/auth/bot-token`

## Пример правильной настройки

### Backend Service (Railway)
```
URL: https://your-backend-production.up.railway.app
Variables:
  - SECRET_KEY=...
  - DATABASE_URL=... (автоматически)
  - CORS_ORIGINS=...
```

### Telegram Bot Service (Railway)
```
Variables:
  - TELEGRAM_BOT_TOKEN=your-token
  - BACKEND_URL=https://your-backend-production.up.railway.app  ← ЭТОТ URL!
```

## Если проблема сохраняется

1. **Проверьте что backend запущен:**
   - Откройте backend URL в браузере
   - Должна открыться API документация

2. **Проверьте что endpoint существует:**
   - Откройте: `https://your-backend-url.railway.app/docs`
   - Найдите endpoint `/api/v1/auth/bot-token`
   - Он должен быть в списке

3. **Проверьте логи backend:**
   - Railway → Backend Service → Deployments → View Logs
   - Ищите запросы к `/api/v1/auth/bot-token`
   - Если запросов нет - бот не может достучаться до backend

4. **Проверьте переменные окружения:**
   - Убедитесь что нет опечаток
   - Убедитесь что URL начинается с `https://`
   - Убедитесь что нет слэша в конце

---

**После исправления BACKEND_URL бот должен работать!** 🎉














