# Настройка ngrok для Telegram Mini App

Инструкция по установке и запуску ngrok на Windows.

## 📥 Шаг 1: Установка ngrok

### Вариант A: Скачать напрямую (рекомендуется)

1. Перейдите на https://ngrok.com/download
2. Скачайте версию для Windows
3. Распакуйте `ngrok.exe` в удобную папку (например, `C:\ngrok\`)

### Вариант B: Через Chocolatey (если установлен)

```powershell
choco install ngrok
```

### Вариант C: Через Scoop (если установлен)

```powershell
scoop install ngrok
```

## 🔑 Шаг 2: Регистрация и получение токена

1. Зарегистрируйтесь на https://ngrok.com (бесплатно)
2. Войдите в аккаунт
3. Перейдите в Dashboard: https://dashboard.ngrok.com/get-started/your-authtoken
4. Скопируйте ваш **authtoken**

## ⚙️ Шаг 3: Настройка ngrok

Откройте PowerShell или Command Prompt и выполните:

```bash
ngrok config add-authtoken ВАШ_АВТОТОКЕН
```

Например:
```bash
ngrok config add-authtoken 2abc123def456ghi789jkl012mno345pqr678
```

**Важно:** Замените `ВАШ_АВТОТОКЕН` на реальный токен из дашборда ngrok.

## 🚀 Шаг 4: Запуск туннеля

### Если ngrok в PATH

Просто запустите:
```bash
ngrok http 8443
```

### Если ngrok не в PATH

Укажите полный путь:
```bash
C:\ngrok\ngrok.exe http 8443
```

Или перейдите в папку с ngrok:
```bash
cd C:\ngrok
ngrok.exe http 8443
```

## 📋 Шаг 5: Получение HTTPS URL

После запуска ngrok вы увидите:

```
Session Status                online
Account                       your-email@example.com
Version                       3.x.x
Region                        Europe (eu)
Latency                       45ms
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://xxxx-xx-xxx-xxx-xx.ngrok-free.app -> http://localhost:8443
```

**Скопируйте HTTPS URL:** `https://xxxx-xx-xxx-xxx-xx.ngrok-free.app`

Этот URL будет указывать на ваш локальный backend на порту 8443.

## 🌐 Веб-интерфейс ngrok

ngrok также запускает веб-интерфейс по адресу: **http://127.0.0.1:4040**

Здесь вы можете:
- Видеть все запросы к вашему туннелю
- Перезапустить туннель
- Скопировать URL

## 📱 Шаг 6: Использование для Telegram Mini App

1. **Запустите backend на HTTPS:**
   ```bash
   cd backend
   python run_https.py
   ```

2. **В отдельном терминале запустите ngrok:**
   ```bash
   ngrok http 8443
   ```

3. **Скопируйте HTTPS URL** из ngrok

4. **Настройте Mini App** через @BotFather:
   - Откройте @BotFather в Telegram
   - Отправьте `/mybots`
   - Выберите вашего бота
   - Выберите **"Bot Settings"** → **"Menu Button"**
   - Введите URL вашего фронтенда через ngrok (или создайте Mini App через `/newapp`)

## 💡 Полезные команды

### Запуск с указанием региона

```bash
ngrok http 8443 --region=eu
```

Регионы: `us` (США), `eu` (Европа), `ap` (Азия), `au` (Австралия), `sa` (Южная Америка), `jp` (Япония), `in` (Индия)

### Запуск с кастомным доменом (требуется платная подписка)

```bash
ngrok http 8443 --domain=your-custom-domain.ngrok.io
```

### Просмотр конфигурации

```bash
ngrok config check
```

## 🔧 Автоматизация

### Создайте batch файл для быстрого запуска

Создайте файл `start-ngrok.bat`:

```batch
@echo off
echo Starting ngrok tunnel for HTTPS backend...
ngrok http 8443
pause
```

Или PowerShell скрипт `start-ngrok.ps1`:

```powershell
Write-Host "Starting ngrok tunnel for HTTPS backend..." -ForegroundColor Green
Write-Host "Backend should be running on https://localhost:8443" -ForegroundColor Yellow
Write-Host ""
ngrok http 8443
```

## ⚠️ Важные замечания

1. **ngrok туннель работает пока открыт терминал** - не закрывайте его
2. **URL меняется при перезапуске** (на бесплатном плане) - нужно обновлять в @BotFather
3. **Ограничения бесплатного плана:**
   - 1 одновременный туннель
   - URL меняется при перезапуске
   - Ограничение на количество запросов

## 🐛 Решение проблем

### Ошибка "command not found"

Убедитесь, что ngrok в PATH или используйте полный путь к `ngrok.exe`.

### Ошибка "authtoken not configured"

Выполните:
```bash
ngrok config add-authtoken ВАШ_ТОКЕН
```

### Портал ngrok (проверка подлинности)

При первом запуске ngrok может показать страницу проверки. Просто нажмите кнопку "Visit Site" в браузере.

## 📚 Дополнительная информация

- [ngrok Documentation](https://ngrok.com/docs)
- [ngrok Dashboard](https://dashboard.ngrok.com/)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)

## ✅ Быстрый чек-лист

- [ ] Установлен ngrok
- [ ] Зарегистрирован аккаунт на ngrok.com
- [ ] Настроен authtoken
- [ ] Backend запущен на https://localhost:8443
- [ ] ngrok туннель запущен
- [ ] HTTPS URL скопирован
- [ ] Mini App настроен через @BotFather

Удачи! 🚀

