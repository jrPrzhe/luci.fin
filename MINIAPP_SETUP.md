# Настройка Telegram Mini App

## ✅ Текущий статус

- ✅ Backend на HTTP: `http://localhost:8000` (работает)
- ✅ ngrok туннель: `https://galleylike-nydia-however.ngrok-free.dev` → `http://localhost:8000`
- ✅ Frontend: `http://localhost:5173`
- ✅ Telegram Bot: работает

## 🎯 Ваш ngrok URL (Backend)

```
https://galleylike-nydia-however.ngrok-free.dev
```

Этот URL указывает на ваш backend API.

## 📱 Настройка Mini App в Telegram

### ⚠️ ВАЖНО: Mini App требует HTTPS URL для фронтенда!

Для работы Mini App нужен HTTPS URL, который указывает на **фронтенд приложения**.

### Вариант 1: Использовать ngrok для фронтенда (рекомендуется)

#### Шаг 1: Запустите отдельный ngrok для фронтенда

В новом терминале:

```powershell
# Запустите ngrok на порт 5173 (frontend)
.\ngrok.exe http 5173
```

Вы получите новый URL, например: `https://xxxx-yyyy-zzzz.ngrok-free.app`

#### Шаг 2: Настройте frontend для работы с backend через ngrok

Создайте файл `.env` в папке `frontend/`:

```env
VITE_API_URL=https://galleylike-nydia-however.ngrok-free.dev
```

Или обновите `vite.config.ts` для проксирования API запросов.

#### Шаг 3: Настройте Mini App в @BotFather

1. **Откройте @BotFather в Telegram**

2. **Отправьте:**
   ```
   /mybots
   ```

3. **Выберите вашего бота**

4. **Выберите "Bot Settings"**

5. **Выберите "Menu Button"**

6. **Введите URL вашего фронтенда:**
   ```
   https://xxxx-yyyy-zzzz.ngrok-free.app
   ```
   (Используйте URL, который вы получили для фронтенда на порту 5173)

### Вариант 2: Использовать один ngrok с проксированием

Если у вас nginx, можно настроить проксирование через один ngrok URL.

## 🔧 Настройка backend для работы с Mini App

Backend уже настроен:
- ✅ Эндпоинт `/api/v1/auth/telegram` для авторизации через Telegram
- ✅ Автоматическая авторизация при открытии Mini App
- ✅ CORS настроен для ngrok доменов

## 🌐 Проверка работы

### 1. Проверьте backend через ngrok

Откройте в браузере:
```
https://galleylike-nydia-however.ngrok-free.dev/docs
```

Должна открыться Swagger документация API.

### 2. Проверьте frontend через ngrok

Откройте в браузере:
```
https://xxxx-yyyy-zzzz.ngrok-free.app
```

Должно открыться ваше приложение.

### 3. Проверьте в Telegram

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (обычно внизу экрана)
3. Должно открыться ваше приложение!
4. При первом открытии произойдет автоматическая авторизация через Telegram

## 🔐 Автоматическая авторизация

Приложение автоматически авторизует пользователя при открытии в Telegram:

1. Frontend определяет, что запущен в Telegram
2. Получает `initData` от Telegram Web App
3. Отправляет его на backend (`/api/v1/auth/telegram`)
4. Backend создает или находит пользователя по `telegram_id`
5. Возвращает JWT токен
6. Пользователь автоматически авторизован!

## 📝 Примечания

### URL меняется при перезапуске ngrok

⚠️ **Важно:** На бесплатном плане ngrok URL меняется при каждом перезапуске.

Если перезапустили ngrok:
1. Скопируйте новый URL
2. Обновите URL в настройках бота через @BotFather:
   ```
   /mybots → Ваш бот → Bot Settings → Menu Button → Введите новый URL
   ```

### CORS настройки

Backend автоматически добавляет ngrok домены в CORS, если они не указаны явно в `.env`.

### Переменные окружения для frontend

Создайте `frontend/.env`:
```env
VITE_API_URL=https://galleylike-nydia-however.ngrok-free.dev
```

Это нужно, чтобы frontend знал, куда отправлять API запросы.

## 🐛 Решение проблем

### Mini App не открывается

1. Проверьте, что frontend доступен через ngrok:
   ```
   https://xxxx-yyyy-zzzz.ngrok-free.app
   ```
   Должно открыться ваше приложение.

2. Проверьте, что backend доступен через ngrok:
   ```
   https://galleylike-nydia-however.ngrok-free.dev/health
   ```
   Должно вернуть: `{"status": "healthy"}`

3. Проверьте, что URL в @BotFather правильный (должен быть URL фронтенда, не backend!)

4. Откройте консоль разработчика в Telegram (если доступна)

### Авторизация не работает

1. Проверьте, что backend запущен
2. Проверьте, что ngrok туннель к backend активен
3. Проверьте CORS настройки в backend
4. Посмотрите логи backend для ошибок

### Frontend не может подключиться к backend

1. Убедитесь, что `VITE_API_URL` в `.env` указывает на правильный ngrok URL backend
2. Проверьте, что ngrok туннель к backend активен
3. Проверьте CORS настройки

## ✅ Чек-лист

- [ ] Backend запущен на `http://localhost:8000`
- [ ] ngrok туннель для backend активен (`https://galleylike-nydia-however.ngrok-free.dev`)
- [ ] Frontend запущен на `http://localhost:5173`
- [ ] ngrok туннель для frontend запущен (новый URL)
- [ ] `VITE_API_URL` в `frontend/.env` указывает на ngrok URL backend
- [ ] Mini App настроен через @BotFather с URL фронтенда
- [ ] Приложение открывается в Telegram
- [ ] Автоматическая авторизация работает

## 📚 Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Web App SDK](https://core.telegram.org/bots/webapps#initializing-mini-apps)
- [ngrok Documentation](https://ngrok.com/docs)

Удачи! 🚀
