# 🔍 Диагностика VK бота

## Проблема: Бот не отвечает на команды

### Шаг 1: Проверка настроек

Запустите скрипт диагностики:

```bash
cd vk-bot
python check_bot.py
```

Этот скрипт проверит:
- ✅ Наличие VK_BOT_TOKEN
- ✅ Подключение к VK API
- ✅ Подключение к бэкенду
- ✅ Настройки Long Poll API

### Шаг 2: Проверка логов бота

Запустите бота и смотрите логи:

```bash
cd vk-bot
python bot.py
```

Вы должны увидеть:
```
============================================================
Starting VK bot...
============================================================
VK_BOT_TOKEN: ✅ Set
BACKEND_URL: http://...
VK_GROUP_ID: ...
============================================================
Bot is ready to receive messages!
```

### Шаг 3: Проверка настроек VK сообщества

**КРИТИЧЕСКИ ВАЖНО:**

1. **Откройте настройки сообщества ВК:**
   - Управление → Настройки → Работа с API

2. **Включите Long Poll API:**
   - ✅ Включите переключатель "Long Poll API"
   - ✅ Включите событие "Входящие сообщения"

3. **Проверьте токен:**
   - Токен должен быть **токеном сообщества**, а не пользовательским
   - Получить можно: Управление → Настройки → Работа с API → Создать ключ
   - Права: `messages`, `groups`

### Шаг 4: Проверка переменных окружения

Создайте файл `.env` в папке `vk-bot`:

```env
VK_BOT_TOKEN=ваш_токен_сообщества
BACKEND_URL=https://ваш-бэкенд.railway.app
VK_GROUP_ID=ваш_id_группы
```

**Важно:**
- `VK_BOT_TOKEN` - это токен СООБЩЕСТВА (community token)
- `BACKEND_URL` - URL вашего бэкенда (Railway, Render и т.д.)
- `VK_GROUP_ID` - ID вашего сообщества (можно найти в настройках)

### Шаг 5: Проверка работы бота

1. **Отправьте боту команду `/start`**

2. **Проверьте логи:**
   - Должно появиться: `✅ Received /start command from user ...`
   - Должно появиться: `📨 Received message from ...`

3. **Если в логах нет сообщений:**
   - Long Poll API не настроен правильно
   - Токен неправильный
   - Бот не запущен

### Шаг 6: Частые проблемы

#### Проблема: "VK_BOT_TOKEN is not set"
**Решение:** Проверьте файл `.env` и переменные окружения

#### Проблема: "Failed to get Long Poll server"
**Решение:** 
- Включите Long Poll API в настройках сообщества
- Проверьте, что токен имеет права `messages`

#### Проблема: "Bot error: ..."
**Решение:** 
- Проверьте логи на наличие ошибок
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

#### Проблема: Бот запущен, но не отвечает
**Решение:**
- Проверьте, что Long Poll API включен
- Проверьте, что событие "Входящие сообщения" включено
- Проверьте логи - должны быть сообщения `📨 Received message`

### Шаг 7: Тестирование локально

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Создайте .env файл
# (см. Шаг 4)

# 3. Запустите бота
python bot.py

# 4. Отправьте боту /start в ВК
# 5. Проверьте логи
```

### Шаг 8: Деплой на Railway

Если бот работает локально, но не работает на Railway:

1. **Проверьте переменные окружения в Railway:**
   - Settings → Variables
   - Убедитесь, что все переменные установлены

2. **Проверьте логи Railway:**
   - Deployments → View Logs
   - Ищите ошибки при запуске

3. **Проверьте Dockerfile:**
   - Убедитесь, что Dockerfile правильно настроен
   - Убедитесь, что рабочая директория правильная

## 📋 Чек-лист

- [ ] VK_BOT_TOKEN установлен и правильный (токен сообщества)
- [ ] BACKEND_URL установлен и доступен
- [ ] Long Poll API включен в настройках сообщества
- [ ] Событие "Входящие сообщения" включено
- [ ] Бот запущен и работает
- [ ] В логах видны входящие сообщения
- [ ] Команда `/start` обрабатывается

## 🆘 Если ничего не помогает

1. Проверьте логи бота на наличие ошибок
2. Проверьте настройки сообщества ВК
3. Попробуйте пересоздать токен сообщества
4. Убедитесь, что бот добавлен в сообщество как администратор

