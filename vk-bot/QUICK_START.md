# 🚀 Быстрый старт VK бота

## Проблема: Бот не отвечает

### Шаг 1: Установите зависимости

```bash
cd vk-bot
pip install -r requirements.txt
```

### Шаг 2: Настройте переменные окружения

**Для локального тестирования:**

1. Создайте файл `.env` в папке `vk-bot`:
```bash
cp .env.example .env
```

2. Заполните `.env`:
```env
VK_BOT_TOKEN=ваш_токен_сообщества
BACKEND_URL=https://ваш-бэкенд.railway.app
VK_GROUP_ID=ваш_id_группы
```

**Для Railway (продакшн):**

1. Откройте Railway → VK Bot Service → Settings → Variables
2. Добавьте переменные:
   - `VK_BOT_TOKEN` = ваш токен сообщества
   - `BACKEND_URL` = https://ваш-бэкенд.railway.app
   - `VK_GROUP_ID` = ваш ID группы

### Шаг 3: Получите токен сообщества ВК

1. Откройте ваше сообщество ВК
2. Управление → Настройки → Работа с API
3. Нажмите "Создать ключ"
4. Выберите права:
   - ✅ Управление сообществом
   - ✅ Сообщения сообщества
5. Скопируйте токен

**⚠️ ВАЖНО:** Нужен токен СООБЩЕСТВА, а не пользовательский!

### Шаг 4: Включите Long Poll API

1. Управление → Настройки → Работа с API → Long Poll API
2. Включите "Long Poll API"
3. Включите событие "Входящие сообщения"
4. Сохраните

### Шаг 5: Проверьте настройки

```bash
python check_bot.py
```

Должно показать:
- ✅ VK_BOT_TOKEN: Установлен
- ✅ Подключение к VK API успешно
- ✅ Бэкенд доступен
- ✅ Long Poll сервер получен

### Шаг 6: Запустите бота

**Локально:**
```bash
python bot.py
```

**На Railway:**
- Бот запускается автоматически после деплоя
- Проверьте логи: Railway → VK Bot Service → Deployments → View Logs

### Шаг 7: Протестируйте

1. Откройте диалог с ботом в ВК
2. Отправьте `/start`
3. Должны появиться кнопки и ответ

## 🔍 Диагностика

### Если бот не отвечает:

1. **Проверьте логи:**
   - Локально: смотрите вывод `python bot.py`
   - Railway: Deployments → View Logs

2. **Проверьте Long Poll:**
   - Убедитесь, что Long Poll API включен
   - Убедитесь, что событие "Входящие сообщения" включено

3. **Проверьте токен:**
   - Должен быть токен сообщества (не пользовательский)
   - Должен иметь права `messages`

4. **Проверьте переменные окружения:**
   - `VK_BOT_TOKEN` установлен
   - `BACKEND_URL` правильный (без слэша в конце)
   - `VK_GROUP_ID` установлен

### В логах должно быть:

```
============================================================
Starting VK bot...
============================================================
VK_BOT_TOKEN: ✅ Set
BACKEND_URL: https://...
VK_GROUP_ID: ...
============================================================
```

При отправке `/start`:
```
✅ Received /start command from user ...
📨 Received message from ...
```

## 📋 Чек-лист

- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `.env` файл создан и заполнен (локально) ИЛИ переменные установлены в Railway
- [ ] Токен сообщества получен и установлен
- [ ] Long Poll API включен в настройках сообщества
- [ ] Событие "Входящие сообщения" включено
- [ ] `check_bot.py` показывает все ✅
- [ ] Бот запущен
- [ ] В логах видны входящие сообщения





