# 📦 Сводка по деплою проекта

## 🎯 Структура деплоя

Проект состоит из 3 компонентов, которые нужно задеплоить отдельно:

```
┌─────────────────┐
│   Frontend      │  →  Vercel (React + Vite)
│   (React)       │
└─────────────────┘
         │
         │ HTTP запросы
         ▼
┌─────────────────┐
│   Backend       │  →  Railway (FastAPI + PostgreSQL)
│   (FastAPI)     │
└─────────────────┘
         ▲
         │ HTTP запросы
         │
┌─────────────────┐
│  Telegram Bot   │  →  Railway (Python)
│   (Polling)     │
└─────────────────┘
```

## 📋 Чек-лист деплоя

### ✅ Шаг 1: Backend на Railway

📖 **Инструкция:** `BACKEND_DEPLOY.md`

**Что нужно сделать:**
1. ✅ Создать проект на Railway
2. ✅ Создать Backend сервис (Root Directory: `backend`)
3. ✅ Добавить PostgreSQL базу данных
4. ✅ Настроить переменные окружения:
   - `SECRET_KEY`
   - `DATABASE_URL` (автоматически от Railway)
   - `CORS_ORIGINS` (URL вашего фронтенда на Vercel)
5. ✅ Получить URL backend (например: `https://your-backend.railway.app`)

**Проверка:**
- Откройте `https://your-backend.railway.app/docs` - должна открыться API документация
- Откройте `https://your-backend.railway.app/health` - должно вернуть `{"status": "healthy"}`

---

### ✅ Шаг 2: Frontend на Vercel

📖 **Инструкция:** `VERCEL_DEPLOY.md`

**Что нужно сделать:**
1. ✅ Создать проект на Vercel
2. ✅ Указать Root Directory: `frontend`
3. ✅ Добавить переменную окружения:
   - `VITE_API_URL` = URL вашего backend (например: `https://your-backend.railway.app`)
4. ✅ Получить URL фронтенда (например: `https://your-app.vercel.app`)

**Проверка:**
- Откройте URL фронтенда - должно открыться приложение
- Проверьте что запросы к API работают (откройте DevTools → Network)

---

### ✅ Шаг 3: Telegram Bot на Railway

📖 **Инструкция:** `TELEGRAM_BOT_DEPLOY.md`

**Что нужно сделать:**
1. ✅ Создать новый сервис на Railway (в том же проекте)
2. ✅ Указать Root Directory: `telegram-bot`
3. ✅ Настроить Builder: Dockerfile
4. ✅ Добавить переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - токен от @BotFather
   - `BACKEND_URL` - URL вашего backend (например: `https://your-backend.railway.app`)
5. ✅ Проверить работу бота

**Как получить TELEGRAM_BOT_TOKEN:**
1. Найдите @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Перейдите в **API Token**
5. Скопируйте токен

**Проверка:**
- Откройте Telegram
- Найдите вашего бота
- Отправьте `/start`
- Бот должен ответить

---

## 🔗 Связывание компонентов

### Backend → Frontend

**На Frontend (Vercel):**
- Переменная: `VITE_API_URL=https://your-backend.railway.app`

**На Backend (Railway):**
- Переменная: `CORS_ORIGINS=https://your-app.vercel.app`

### Backend → Telegram Bot

**На Telegram Bot (Railway):**
- Переменная: `BACKEND_URL=https://your-backend.railway.app`

---

## 🔧 Переменные окружения - Сводка

### Backend (Railway)

```env
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://... (автоматически от Railway)
CORS_ORIGINS=https://your-app.vercel.app
```

### Frontend (Vercel)

```env
VITE_API_URL=https://your-backend.railway.app
```

### Telegram Bot (Railway)

```env
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
BACKEND_URL=https://your-backend.railway.app
```

---

## 🐛 Решение проблем

### Backend не запускается

📖 См. `BACKEND_DEPLOY.md` → раздел "Решение проблем"

**Частые проблемы:**
- ❌ `DATABASE_URL` не установлен → добавьте PostgreSQL базу данных
- ❌ `requirements.txt not found` → проверьте Root Directory
- ❌ `400 Bad Request` для OPTIONS → проверьте `CORS_ORIGINS`

### Frontend не может подключиться к Backend

📖 См. `VERCEL_DEPLOY.md` → раздел "Решение проблем"

**Частые проблемы:**
- ❌ CORS ошибки → проверьте `CORS_ORIGINS` на backend
- ❌ `VITE_API_URL` не установлен → добавьте переменную на Vercel
- ❌ Backend недоступен → проверьте что backend запущен

### Telegram Bot не работает

📖 См. `TELEGRAM_BOT_DEPLOY.md` → раздел "Решение проблем"

**Частые проблемы:**
- ❌ `TELEGRAM_BOT_TOKEN is not set!` → добавьте переменную на Railway
- ❌ Не может подключиться к backend → проверьте `BACKEND_URL`
- ❌ Бот не отвечает → проверьте логи на Railway

---

## 📊 Итоговая структура на Railway

После деплоя у вас будет проект на Railway с 2 сервисами:

```
Railway Project: luci.fin
│
├── Backend Service
│   ├── Root Directory: backend
│   ├── Builder: Dockerfile
│   ├── URL: https://your-backend.railway.app
│   ├── Variables:
│   │   ├── SECRET_KEY
│   │   ├── DATABASE_URL (от PostgreSQL)
│   │   └── CORS_ORIGINS
│   └── Database: PostgreSQL
│
└── Telegram Bot Service
    ├── Root Directory: telegram-bot
    ├── Builder: Dockerfile
    ├── URL: не требуется (polling)
    └── Variables:
        ├── TELEGRAM_BOT_TOKEN
        └── BACKEND_URL
```

---

## ✅ Финальная проверка

После деплоя всех компонентов проверьте:

1. **Backend:**
   - [ ] Открывается `https://your-backend.railway.app/docs`
   - [ ] Health check работает: `https://your-backend.railway.app/health`

2. **Frontend:**
   - [ ] Открывается `https://your-app.vercel.app`
   - [ ] Запросы к API работают (проверьте DevTools)

3. **Telegram Bot:**
   - [ ] Бот отвечает на `/start`
   - [ ] Бот отвечает на `/help`
   - [ ] Бот может получить баланс (если пользователь авторизован)

---

## 🎉 Готово!

После завершения всех шагов у вас будет полностью рабочий проект:
- ✅ Frontend на Vercel
- ✅ Backend на Railway
- ✅ PostgreSQL база данных на Railway
- ✅ Telegram бот на Railway
- ✅ Все компоненты связаны и работают вместе

---

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Vercel Documentation](https://vercel.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [FastAPI Documentation](https://fastapi.tiangolo.com)

---

**Нужна помощь?** Проверьте соответствующую инструкцию для каждого компонента или логи на Railway/Vercel.

















