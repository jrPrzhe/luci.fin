# 🚀 Создание админа в Railway - Пошаговая инструкция

## Проблема: "no such table: users"

Это означает, что таблицы еще не созданы в продакшн базе данных Railway.

## ✅ Решение: Применить миграции, затем создать админа

### Шаг 1: Применить миграции базы данных

**РЕКОМЕНДУЕТСЯ: Использовать скрипт run_migrations.py**
```bash
railway run python backend/run_migrations.py
```

Этот скрипт автоматически найдет `alembic.ini` и применит миграции.

**Альтернатива: Если миграции применяются автоматически при старте**

Проверьте логи Railway при старте backend - должны быть строки:
```
Running database migrations...
Database migrations applied successfully
```

Если миграции применяются автоматически, можно пропустить этот шаг и сразу создать админа.

### Шаг 2: Проверить, что таблицы созданы

```bash
railway run python -c "from app.core.database import engine; from sqlalchemy import inspect; inspector = inspect(engine); print('Tables:', inspector.get_table_names())"
```

Должны появиться таблицы: `users`, `accounts`, `transactions`, и т.д.

### Шаг 3: Создать админа

```bash
railway run python backend/create_admin_user.py --email admin@test.com --password admin123456
```

## 🔄 Альтернативный способ: Через Swagger (если миграции уже применены)

Если таблицы уже существуют (миграции применены автоматически при старте):

1. **Откройте Swagger:** `https://lucifin-production.up.railway.app/docs`
2. **Найдите эндпоинт:** `POST /api/v1/auth/register`
3. **Создайте пользователя:**
   ```json
   {
     "email": "admin@test.com",
     "password": "admin123456",
     "first_name": "Admin",
     "last_name": "User"
   }
   ```
4. **Сделайте его админом через SQL** (если есть доступ к БД):
   ```sql
   UPDATE users SET is_admin = 1 WHERE email = 'admin@test.com';
   ```

## 📝 Проверка после создания

1. **Войдите через Swagger:**
   - `POST /api/v1/auth/login` с email и паролем
   - Скопируйте токен

2. **Проверьте статус:**
   - Авторизуйтесь в Swagger
   - Вызовите `GET /api/v1/admin/check-status`
   - Должно вернуть `"is_admin": true`

3. **Проверьте список пользователей:**
   - Вызовите `GET /api/v1/admin/users`
   - Должен вернуть список всех пользователей

## ⚠️ Важно

- Миграции применяются автоматически при старте backend (если не установлена переменная `SKIP_MIGRATIONS`)
- Если таблицы не созданы, сначала примените миграции вручную
- Скрипт `create_admin_user.py` теперь проверяет существование таблиц и выводит понятное сообщение об ошибке

