# Руководство по контрибьюции

Спасибо за интерес к проекту! Мы рады вашему участию.

## Как внести вклад

### 1. Форк и клонирование

```bash
# Форкните репозиторий на GitHub
# Затем клонируйте свой форк
git clone https://github.com/YOUR_USERNAME/finance-manager.git
cd finance-manager
```

### 2. Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b bugfix/issue-description
```

### 3. Внесите изменения

- Следуйте стилю кода проекта
- Добавляйте комментарии к коду
- Пишите тесты для новой функциональности

### 4. Коммиты

```bash
git commit -m "feat: добавить новую функцию"
```

Используйте conventional commits:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - рутинные задачи

### 5. Запуште изменения

```bash
git push origin feature/your-feature-name
```

### 6. Создайте Pull Request

На GitHub создайте Pull Request с описанием изменений.

## Стиль кода

### Python (Backend)

- Используйте `black` для форматирования
- Максимальная длина строки: 100 символов
- Используйте type hints
- Документируйте функции docstrings

```bash
# Установите pre-commit hooks
pip install pre-commit
pre-commit install
```

### TypeScript (Frontend)

- Используйте ESLint
- Максимальная длина строки: 100 символов
- Используйте async/await вместо промисов
- Компоненты пишите в функциональном стиле

```bash
# Проверка линтера
npm run lint

# Автоисправление
npm run lint -- --fix
```

## Тестирование

### Backend тесты

```bash
cd backend
pytest
pytest --cov=app tests/
```

### Frontend тесты

```bash
cd frontend
npm test
```

Покрытие кода тестами должно быть не менее 70%.

## Документация

- Обновляйте документацию при добавлении новой функции
- Добавляйте примеры использования
- Оставляйте комментарии к сложной логике

## Вопросы?

Создайте Issue на GitHub или свяжитесь с maintainers.

Спасибо за вклад! 🎉

