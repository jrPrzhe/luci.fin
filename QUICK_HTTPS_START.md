# Быстрый запуск HTTPS Backend

## ✅ Сертификат готов!

Файлы `localhost.crt` и `localhost.key` созданы успешно.

## 🚀 Запуск

### Вариант 1: Используя скрипт

```bash
# Из корня проекта:
.\start-backend-https.bat

# Или в PowerShell:
.\start-backend-https.ps1
```

### Вариант 2: Вручную

```bash
cd backend
python run_https.py
```

## 🌐 Доступ

- **Backend API**: https://localhost:8443
- **API Docs**: https://localhost:8443/docs

## ⚠️ Важно

При первом открытии браузер покажет предупреждение о безопасности - это нормально для самоподписанного сертификата!

**Как продолжить:**
1. Нажмите "Дополнительно" / "Advanced"
2. Нажмите "Перейти на localhost (небезопасно)" / "Proceed to localhost"

## 🔗 Для Telegram Mini App

После запуска backend на HTTPS:

1. **Запустите ngrok** (в отдельном терминале):
   ```bash
   ngrok http 8443
   ```

2. **Скопируйте HTTPS URL** из ngrok (например: `https://xxxx-xx-xxx-xxx-xx.ngrok-free.app`)

3. **Настройте Mini App** через @BotFather:
   ```
   /mybots -> Ваш бот -> Bot Settings -> Menu Button
   ```
   Укажите URL вашего фронтенда через ngrok

## 📝 Удаление .pfx файла

`.pfx` файл больше не нужен, можно удалить:
```bash
cd backend
del localhost.pfx
```

Или скрипт удалит его автоматически при следующем запуске.

