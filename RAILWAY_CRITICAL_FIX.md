# 🚨 КРИТИЧЕСКОЕ РЕШЕНИЕ: Railway Build Context

## Проблема

Railway находит Dockerfile (`load build definition from /backend/Dockerfile`), но не может найти `requirements.txt` при выполнении `COPY requirements.txt .`

## Причина

**Root Directory не установлен или не используется как build context!**

Когда Root Directory установлен в `backend`, Railway должен:
1. Использовать `backend/` как build context
2. Искать Dockerfile в `backend/Dockerfile` (относительно Root Directory)
3. Выполнять команды COPY относительно `backend/`

## ✅ РЕШЕНИЕ: Установить Root Directory

### Шаг 1: В Railway UI

1. **Откройте Settings сервиса `luci.fin`**
2. **Найдите раздел Source:**
   - В правом меню **Quick Links** → **Source**
   - ИЛИ прокрутите вниз до раздела **Source**
3. **Установите Root Directory:**
   - Поле **Root Directory** → введите: `backend`
   - Нажмите **Save** или **Apply changes**

### Шаг 2: Если Root Directory не видно

**Пересоздайте сервис:**

1. **Удалите текущий сервис:**
   - Settings → Danger Zone → Delete Service

2. **Создайте новый:**
   - **+ New** → **GitHub Repo**
   - Выберите `luci.fin`
   - **КРИТИЧЕСКИ ВАЖНО:** При настройке в поле **Root Directory** укажите: `backend`
   - Нажмите **Deploy**

### Шаг 3: Проверка

После установки Root Directory:

1. **Settings → Source:**
   - Root Directory должен быть: `backend`

2. **Settings → Build:**
   - Builder должен быть: `Dockerfile`
   - dockerfilePath должен быть: `Dockerfile` (относительно Root Directory)

3. **Логи сборки:**
   - Railway должен найти все файлы
   - `COPY requirements.txt .` должен работать

## 🔍 Как это работает

Когда Root Directory = `backend`:

```
Корень репозитория (luci.fin/)
└── backend/  ← Root Directory (build context)
    ├── Dockerfile  ← Railway ищет здесь
    ├── requirements.txt  ← COPY находит здесь
    └── app/
```

Railway использует `backend/` как build context, поэтому:
- `COPY requirements.txt .` находит `backend/requirements.txt`
- `COPY . .` копирует все из `backend/`

## ⚠️ БЕЗ Root Directory Railway:

- Использует корень репозитория как build context
- Ищет `requirements.txt` в корне (не находит)
- Ищет `Dockerfile` в корне (не находит, т.к. он в `backend/`)

## 📝 После исправления

1. Railway автоматически перезапустит деплой
2. Сборка должна найти все файлы
3. Dockerfile должен успешно собраться

## 🆘 Если все еще не работает

Проверьте что файлы в репозитории:
```bash
git ls-files backend/requirements.txt
git ls-files backend/Dockerfile
```

Если файлов нет - добавьте их в git и запушьте.





