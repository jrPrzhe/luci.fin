# ⚡ Быстрое исправление ошибки сборки на Railway

## 🎯 Рекомендуемое решение: Использовать Dockerfile

**Проблема:** Nixpacks не может правильно собрать проект с ML библиотеками (scikit-learn, pandas, numpy).

**Решение:** Переключитесь на Dockerfile - он уже настроен правильно.

### Шаги:

1. **В Railway:**
   - Откройте ваш сервис `luci.fin`
   - Settings → Build → Builder
   - Измените с **"Railpack Default"** на **"Dockerfile"**
   - Нажмите **Save** или **Apply changes**

2. **Проверьте Root Directory:**
   - Settings → Source → Root Directory должен быть: `backend`
   - Если нет поля Source, Railway должен автоматически определить по Dockerfile

3. **Нажмите Deploy:**
   - Railway автоматически пересоберет проект используя Dockerfile
   - Сборка должна пройти успешно

## ✅ Почему Dockerfile лучше

- ✅ Все системные зависимости включены (gcc, g++, libpq-dev, libffi-dev, libssl-dev)
- ✅ Правильно настроен для ML библиотек
- ✅ Поддержка переменной PORT от Railway
- ✅ Более надежный и предсказуемый процесс сборки

## 📝 Что было обновлено

- `backend/Dockerfile` - обновлен для Railway (поддержка $PORT и дополнительные зависимости)
- `backend/nixpacks.toml` - обновлен с дополнительными пакетами (на случай если захотите использовать Nixpacks)

## 🔍 Если ошибка сохраняется

Проверьте логи:
1. Railway → Deployments → View Logs
2. Найдите конкретную ошибку
3. Возможно потребуются дополнительные системные пакеты

## ⚠️ Важно

После переключения на Dockerfile убедитесь что:
- Root Directory установлен в `backend`
- Dockerfile находится в `backend/Dockerfile`
- Все файлы закоммичены и запушены в репозиторий










