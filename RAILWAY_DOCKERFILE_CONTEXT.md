# Исправление ошибки: "requirements.txt: not found" в Dockerfile

## 🐛 Проблема

Railway не может найти `requirements.txt` при сборке Docker образа:
```
ERROR: "/requirements.txt": not found
```

## ✅ Решение

Проблема возникает из-за того, что Railway использует Root Directory `backend`, но Dockerfile пытается копировать файлы относительно корня репозитория.

### Вариант 1: Проверьте Root Directory (Рекомендуется)

1. **В Railway Settings → Source:**
   - Убедитесь что **Root Directory** установлен в `backend`
   - Если Root Directory = `backend`, то все команды COPY в Dockerfile должны работать относительно папки `backend/`

2. **Проверьте структуру репозитория:**
   ```
   luci.fin/
   └── backend/
       ├── Dockerfile  ← Railway использует этот
       ├── requirements.txt  ← должен быть здесь
       ├── app/
       └── ...
   ```

### Вариант 2: Обновите Dockerfile

Dockerfile уже обновлен для правильной работы с Root Directory = `backend`.

### Вариант 3: Если Root Directory не установлен

Если Root Directory не установлен или установлен в корень:

1. **Установите Root Directory:**
   - Settings → Source → Root Directory: `backend`

2. **Или измените структуру проекта (не рекомендуется):**
   - Переместите Dockerfile в корень проекта
   - Измените пути в Dockerfile

## 🔍 Проверка

1. **Убедитесь что файлы существуют:**
   ```bash
   ls backend/requirements.txt
   ls backend/Dockerfile
   ```

2. **Проверьте что файлы в git:**
   ```bash
   git ls-files backend/requirements.txt
   git ls-files backend/Dockerfile
   ```

3. **Проверьте Root Directory в Railway:**
   - Settings → Source → Root Directory должен быть `backend`

## 📝 После исправления

1. Добавьте изменения:
   ```bash
   git add backend/Dockerfile
   git commit -m "Fix Dockerfile paths for Railway build context"
   git push origin main
   ```

2. Railway автоматически перезапустит деплой
3. Проверьте логи - сборка должна найти requirements.txt

## ⚠️ Важно

- Root Directory должен быть `backend`
- Dockerfile должен использовать относительные пути (COPY requirements.txt .)
- Все файлы должны быть в папке `backend/`





