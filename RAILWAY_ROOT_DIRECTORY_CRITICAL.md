# 🚨 КРИТИЧЕСКИ ВАЖНО: Root Directory в Railway

## Проблема

Railway не может найти `requirements.txt` и другие файлы, потому что **Root Directory не установлен или установлен неправильно**.

## ✅ РЕШЕНИЕ: Установить Root Directory

### Способ 1: Через UI Railway (Попробуйте сначала)

1. **Откройте Railway:**
   - Зайдите в ваш проект
   - Откройте сервис `luci.fin`

2. **Найдите Root Directory:**
   - Settings → **Source** (в правом меню Quick Links)
   - ИЛИ Settings → прокрутите вниз до раздела **Source**
   - Найдите поле **Root Directory**

3. **Установите значение:**
   - Введите: `backend`
   - Нажмите **Save** или **Apply changes**

4. **Проверьте:**
   - После сохранения Railway должен автоматически перезапустить деплой
   - Или нажмите **Deploy** вручную

### Способ 2: Пересоздать сервис (Если способ 1 не работает)

**Это самый надежный способ:**

1. **Удалите текущий сервис:**
   - Settings → прокрутите вниз до **Danger Zone**
   - Нажмите **Delete Service**
   - Подтвердите удаление

2. **Создайте новый сервис:**
   - В вашем проекте Railway нажмите **+ New**
   - Выберите **GitHub Repo**
   - Выберите репозиторий `luci.fin`
   - **ВАЖНО:** После выбора репозитория Railway покажет форму настройки
   - В этой форме найдите поле **Root Directory** или **Root Path**
   - Введите: `backend`
   - Нажмите **Deploy**

3. **Проверьте после создания:**
   - Settings → Source → Root Directory должен быть `backend`
   - Settings → Build → Builder должен быть `Dockerfile`

## 🔍 Как проверить что Root Directory установлен

После настройки проверьте:

1. **В Railway Settings → Source:**
   - Поле **Root Directory** должно содержать: `backend`

2. **В логах сборки:**
   - Railway должен искать файлы в `backend/`
   - Dockerfile должен найти `requirements.txt`

## 📝 Структура должна быть

```
luci.fin/ (корень репозитория на GitHub)
└── backend/
    ├── Dockerfile  ← Railway использует этот
    ├── requirements.txt  ← Railway должен найти этот
    ├── railway.json
    └── app/
```

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Без правильного Root Directory Railway НЕ сможет найти файлы!**

- Если Root Directory не установлен → Railway ищет файлы в корне репозитория
- Если Root Directory = `backend` → Railway ищет файлы в `backend/`
- Dockerfile находится в `backend/Dockerfile` → нужен Root Directory = `backend`

## 🎯 После установки Root Directory

1. Railway автоматически перезапустит деплой
2. Сборка должна найти все файлы
3. Dockerfile должен успешно собраться

## 🆘 Если ничего не помогает

1. Убедитесь что файлы закоммичены в git:
   ```bash
   git ls-files backend/requirements.txt
   git ls-files backend/Dockerfile
   ```

2. Проверьте что файлы есть в репозитории на GitHub

3. Пересоздайте сервис с Root Directory = `backend`





