# 🚀 Быстрая настройка администратора для тестирования

## Вариант 1: Создать админа через Railway CLI (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создайте админа через email

```bash
railway run python backend/create_admin_user.py --email admin@test.com --password admin123456
```

**Учетные данные для входа:**
- Email: `admin@test.com`
- Password: `admin123456`

### Шаг 2: Получите токен через Swagger

1. Откройте Swagger UI: `https://lucifin-production.up.railway.app/docs`
2. Найдите эндпоинт `POST /api/v1/auth/login`
3. Нажмите "Try it out"
4. Введите:
   ```json
   {
     "email": "admin@test.com",
     "password": "admin123456"
   }
   ```
5. Нажмите "Execute"
6. Скопируйте `access_token` из ответа

### Шаг 3: Авторизуйтесь в Swagger

1. Нажмите кнопку **"Authorize"** (вверху справа)
2. В поле `Value` вставьте: `Bearer <ваш_токен>`
3. Нажмите "Authorize" и "Close"

### Шаг 4: Проверьте админские эндпоинты

Теперь вы можете вызвать:
- `GET /api/v1/admin/check-status` - проверить свой статус
- `GET /api/v1/admin/users` - получить список всех пользователей
- `GET /api/v1/admin/sync-admin-status` - синхронизировать статус

## Вариант 2: Использовать стандартного тестового пользователя

Если тестовый пользователь уже создан:

```bash
# 1. Создайте пользователя (если еще не создан)
railway run python backend/create_test_user.py

# 2. Сделайте его админом через SQL (если есть доступ к БД)
# Или обновите через скрипт:
railway run python backend/create_admin_user.py --email test@example.com --password test123456
```

## Вариант 3: Обновить существующего пользователя

Если у вас уже есть пользователь в базе:

```bash
# Обновить существующего пользователя до админа
railway run python backend/create_admin_user.py --email ваш-email@example.com --password ваш-пароль
```

Скрипт автоматически обновит статус админа, если пользователь уже существует.

## 🔍 Проверка через эндпоинты

### Проверить свой статус:
```
GET /api/v1/admin/check-status
```

### Синхронизировать статус (для Telegram пользователей):
```
GET /api/v1/admin/sync-admin-status
```

### Получить список всех пользователей:
```
GET /api/v1/admin/users
```

## ⚠️ Важно

- Email пользователи **НЕ** получают автоматически статус админа из `ADMIN_TELEGRAM_IDS`
- `ADMIN_TELEGRAM_IDS` работает только для Telegram пользователей
- Для email пользователей нужно вручную установить `is_admin = True` через скрипт или SQL












