# 🚀 Руководство по настройке VK Mini App

## ✅ Что уже сделано

- ✅ Добавлена поддержка VK Mini Apps в код
- ✅ Создана миграция для поля `vk_id` в базе данных
- ✅ Миграция применена локально
- ✅ Изменения запушены в репозиторий

## 📋 Следующие шаги

### 1. Проверить деплой на сервер

После push в GitHub, Railway (или ваш хостинг) должен автоматически:
- Задеплоить изменения
- Применить миграцию при старте backend
- Добавить колонку `vk_id` в таблицу `users`

**Как проверить:**
1. Откройте логи Railway/хостинга
2. Найдите строки:
   ```
   [STARTUP] Running migrations...
   [STARTUP] Running alembic upgrade head...
   [STARTUP] Migrations completed
   ```
3. Или проверьте вручную:
   ```sql
   -- Подключитесь к базе данных
   SELECT column_name, data_type 
   FROM information_schema.columns 
   WHERE table_name = 'users' AND column_name = 'vk_id';
   ```

### 2. Создать VK Mini App

1. Перейдите на [VK Developers](https://dev.vk.com/)
2. Войдите в свой аккаунт VK
3. Создайте новое приложение:
   - Нажмите "Создать приложение"
   - Выберите тип: **Mini App**
   - Укажите название приложения

### 3. Настроить приложение

В настройках приложения укажите:

**Основные настройки:**
- **Название:** Finance Manager (или ваше название)
- **Описание:** Управление личными финансами

**URL приложения:**
- **URL приложения:** `https://ваш-домен.com` (URL вашего фронтенда)
- **URL для тестирования:** `https://ваш-домен.com` (можно тот же)

**Важно:** URL должен быть доступен по HTTPS!

### 4. Получить App ID и Secret Key

После создания приложения вы получите:
- **App ID** (например: `12345678`)
- **Secret Key** (например: `AbCdEfGhIjKlMnOpQrStUvWxYz`)

**Сохраните их!** Они понадобятся для:
- Проверки подписи launch params (опционально, для безопасности)
- Настройки приложения в VK

### 5. Настроить домен (если нужно)

Если ваш фронтенд еще не задеплоен:

**Вариант A: Использовать существующий домен**
- Убедитесь, что домен доступен по HTTPS
- Укажите этот домен в настройках VK Mini App

**Вариант B: Использовать Vercel/Netlify для фронтенда**
- Задеплойте фронтенд на Vercel или Netlify
- Получите домен (например: `finance-manager.vercel.app`)
- Укажите этот домен в настройках VK Mini App

### 6. Протестировать приложение

1. **Откройте приложение в VK:**
   - В мобильном приложении VK: найдите ваше приложение в разделе "Мини-приложения"
   - Или откройте ссылку: `https://vk.com/app12345678` (где `12345678` - ваш App ID)

2. **Проверьте авторизацию:**
   - Приложение должно автоматически определить, что оно запущено в VK
   - Должна произойти автоматическая авторизация через VK Bridge
   - Пользователь должен быть создан автоматически

3. **Проверьте функциональность:**
   - Создайте транзакцию
   - Проверьте, что данные сохраняются
   - Убедитесь, что данные изолированы по `vk_id`

### 7. (Опционально) Добавить проверку подписи

Для безопасности можно добавить проверку подписи launch params:

1. Добавьте `VK_APP_SECRET` в переменные окружения backend
2. Раскомментируйте и добавьте проверку подписи в `backend/app/api/v1/auth.py`:

```python
import hmac
import hashlib
from app.core.config import settings

def verify_vk_sign(params: dict) -> bool:
    """Verify VK launch params signature"""
    secret = settings.VK_APP_SECRET
    if not secret:
        return True  # Skip verification if secret not set
    
    sign = params.pop('sign', '')
    if not sign:
        return False
    
    # Sort params and create query string
    sorted_params = sorted([(k, v) for k, v in params.items() if k != 'sign'])
    query_string = '&'.join([f"{k}={v}" for k, v in sorted_params])
    
    # Calculate hash
    calculated_sign = hmac.new(
        secret.encode(),
        query_string.encode(),
        hashlib.md5
    ).hexdigest()
    
    return calculated_sign == sign
```

## 🔍 Отладка

### Проблема: Авторизация не работает

**Проверьте:**
1. Откройте консоль браузера (F12) в VK Mini App
2. Найдите логи:
   ```
   VK auto-auth check: { hasLaunchParams: true, ... }
   Attempting automatic VK login...
   ```
3. Если есть ошибки - проверьте:
   - Доступен ли backend по HTTPS
   - Правильно ли указан URL в настройках VK Mini App
   - Есть ли `vk_user_id` в URL параметрах

### Проблема: Колонка vk_id не создана

**Решение:**
1. Проверьте логи backend при старте
2. Должна быть строка: `[STARTUP] Adding vk_id to users...`
3. Если нет - миграция не применилась, примените вручную:
   ```bash
   railway run alembic upgrade head
   ```

### Проблема: Приложение не открывается в VK

**Проверьте:**
1. URL приложения указан правильно в настройках VK
2. URL доступен по HTTPS
3. CORS настроен правильно на backend
4. Backend отвечает на запросы

## 📚 Полезные ссылки

- [VK Mini Apps Documentation](https://dev.vk.com/mini-apps)
- [VK Bridge API](https://dev.vk.com/mini-apps/development/launch-params)
- [VK Developers Console](https://dev.vk.com/apps)

## ✅ Чеклист готовности

- [ ] Миграция применена на сервере
- [ ] VK Mini App создан в VK Developers
- [ ] URL приложения указан в настройках VK
- [ ] App ID и Secret Key сохранены
- [ ] Фронтенд задеплоен и доступен по HTTPS
- [ ] Приложение протестировано в VK
- [ ] Авторизация работает
- [ ] Данные сохраняются корректно

---

**Готово!** После выполнения всех шагов ваше приложение будет работать в VK Mini Apps! 🎉




