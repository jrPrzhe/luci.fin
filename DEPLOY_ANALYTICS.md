# 🚀 Деплой системы аналитики в продакшн

## Шаги для деплоя

### 1. Подготовка к деплою

Убедитесь, что все изменения закоммичены:

```bash
git add .
git commit -m "Add analytics system: tracking for bot and miniapp"
git push
```

### 2. Применение миграций на проде

#### Вариант A: Через Railway CLI (рекомендуется)

```bash
# Установите Railway CLI, если еще не установлен
npm i -g @railway/cli

# Авторизуйтесь
railway login

# Подключитесь к проекту
railway link

# Примените миграции (используем готовый скрипт - РЕКОМЕНДУЕТСЯ)
railway run python backend/run_migrations.py

# Или альтернативные варианты:
railway run --directory backend alembic upgrade head
railway run alembic -c backend/alembic.ini upgrade head
```

#### Вариант B: Через Railway Dashboard

1. Откройте ваш Backend Service в Railway
2. Перейдите в **Deployments** → последний деплой
3. Откройте **Shell/Console**
4. Выполните:
   ```bash
   cd backend
   alembic upgrade head
   ```

#### Вариант C: Автоматическое применение (если настроено)

Если у вас настроено автоматическое применение миграций при старте (в `main.py`), миграции применятся автоматически при следующем деплое.

### 3. Проверка миграции

После применения миграции проверьте, что таблица создана:

```bash
# Через Railway CLI
railway run python -c "
from app.core.database import engine
from sqlalchemy import inspect
inspector = inspect(engine)
tables = inspector.get_table_names()
print('Tables:', tables)
if 'analytics_events' in tables:
    print('✅ analytics_events table exists!')
    columns = [col['name'] for col in inspector.get_columns('analytics_events')]
    print('Columns:', columns)
else:
    print('❌ analytics_events table not found!')
"
```

### 4. Проверка работы системы

1. **Проверьте API endpoint**:
   ```bash
   curl -X POST https://your-backend-url/api/v1/analytics/track \
     -H "Content-Type: application/json" \
     -d '{
       "event_type": "test",
       "event_name": "test_event",
       "platform": "web"
     }'
   ```

2. **Проверьте статистику** (требуется админ-токен):
   ```bash
   curl -X GET "https://your-backend-url/api/v1/analytics/stats" \
     -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
   ```

3. **Откройте страницу аналитики**:
   - Войдите как администратор
   - Перейдите на `/analytics`
   - Убедитесь, что данные отображаются

### 5. Мониторинг

После деплоя следите за:
- Логами бэкенда на наличие ошибок
- Логами VK бота на наличие ошибок отслеживания
- Ростом количества событий в таблице `analytics_events`

## Откат миграции (если нужно)

Если что-то пошло не так, можно откатить миграцию:

```bash
railway run alembic downgrade -1
```

Это удалит таблицу `analytics_events` и все данные в ней.

## Важные замечания

1. **Миграция безопасна**: Создание новой таблицы не влияет на существующие данные
2. **Производительность**: Индексы созданы для быстрого поиска
3. **Масштабируемость**: Таблица может расти быстро, рассмотрите архивирование старых данных

## Структура миграции

Миграция `9c8be056d531_add_analytics_events_table.py` создает:
- Таблицу `analytics_events` с полями:
  - `id` (primary key)
  - `user_id`, `vk_id`, `telegram_id` (идентификация пользователя)
  - `event_type`, `event_name` (тип и название события)
  - `platform` (платформа)
  - `metadata` (JSON с дополнительными данными)
  - `user_agent`, `referrer` (для веб-версии)
  - `created_at` (время создания)
- Индексы на всех ключевых полях для быстрого поиска

## После успешного деплоя

Система начнет автоматически собирать данные:
- ✅ События из VK бота
- ✅ События из VK мини-приложения
- ✅ Все взаимодействия пользователей

Данные будут доступны в разделе "Аналитика" для администраторов.

