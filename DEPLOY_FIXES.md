# 🚀 Деплой исправлений на прод

## ✅ Что было исправлено

1. **Создание транзакций** - исправлена проблема с enum (INCOME/EXPENSE/TRANSFER → income/expense/transfer)
   - Улучшен `TransactionTypeEnum` TypeDecorator для правильной конвертации
   - Добавлена установка значения через `__dict__`, `setattr` и SQLAlchemy inspect API
   - Для переводов используются отдельные коммиты вместо bulk insert
   - Улучшены `__init__` и `@validates` методы в модели Transaction
2. **Эндпоинт категорий** - исправлена обработка параметра transaction_type
3. **TypeDecorator** - улучшена конвертация enum значений в lowercase

## 📋 Как задеплоить изменения на прод

### Вариант 1: Автоматический деплой (рекомендуется)

Если Railway настроен на автоматический деплой из GitHub:

1. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Fix enum issues for transactions and categories"
   git push origin main
   ```

2. **Railway автоматически задеплоит:**
   - Railway обнаружит push в main ветку
   - Автоматически запустит сборку
   - Применит миграции при старте (если есть)
   - Перезапустит backend

3. **Проверьте деплой:**
   - Откройте Railway Dashboard
   - Перейдите в **Deployments**
   - Убедитесь что последний деплой успешен (зеленый статус)

### Вариант 2: Ручной деплой через Railway Dashboard

1. **Откройте Railway Dashboard:**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите в свой проект

2. **Запустите деплой:**
   - Откройте ваш Backend сервис
   - Нажмите **Deploy** или **Redeploy**
   - Railway загрузит последний код из GitHub и задеплоит

3. **Проверьте логи:**
   - После деплоя откройте **Logs**
   - Убедитесь что нет ошибок
   - Должны быть строки:
     - `[STARTUP] Starting application import...`
     - `[STARTUP] Migrations completed`
     - `[STARTUP] All routers included, application ready!`

## 🔍 Проверка после деплоя

### 1. Проверьте что backend работает:

```bash
# Откройте в браузере или через curl:
https://ВАШ_БЭКЕНД_URL/docs
```

Должна открыться Swagger документация API.

### 2. Проверьте создание транзакций:

**Тест дохода:**
```bash
curl -X POST https://ВАШ_БЭКЕНД_URL/api/v1/transactions/ \
  -H "Authorization: Bearer ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "account_id": 1,
    "transaction_type": "income",
    "amount": 100.0,
    "currency": "RUB"
  }'
```

**Тест расхода:**
```bash
curl -X POST https://ВАШ_БЭКЕНД_URL/api/v1/transactions/ \
  -H "Authorization: Bearer ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "account_id": 1,
    "transaction_type": "expense",
    "amount": 50.0,
    "currency": "RUB",
    "category_id": 1
  }'
```

**Тест перевода:**
```bash
curl -X POST https://ВАШ_БЭКЕНД_URL/api/v1/transactions/ \
  -H "Authorization: Bearer ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "account_id": 1,
    "transaction_type": "transfer",
    "amount": 100.0,
    "currency": "RUB",
    "to_account_id": 2
  }'
```

### 3. Проверьте категории:

```bash
curl https://ВАШ_БЭКЕНД_URL/api/v1/categories/?transaction_type=income \
  -H "Authorization: Bearer ВАШ_ТОКЕН"
```

### 4. Проверьте отчеты:

```bash
curl https://ВАШ_БЭКЕНД_URL/api/v1/reports/analytics?period=month \
  -H "Authorization: Bearer ВАШ_ТОКЕН"
```

## ⚠️ Если что-то не работает

### Проблема: Ошибка "invalid input value for enum transactiontype: 'INCOME'"

**Решение:**
1. Проверьте что изменения задеплоены (последний деплой в Railway)
2. Проверьте логи Railway - должны быть строки о применении миграций
3. Если миграции не применились автоматически, примените вручную:

```bash
# Через Railway CLI
railway run python backend/run_migrations.py

# Или через Railway Dashboard Shell
cd backend
python run_migrations.py
```

### Проблема: Backend не запускается

**Решение:**
1. Проверьте логи Railway - там будет видна ошибка
2. Убедитесь что все переменные окружения настроены:
   - `DATABASE_URL`
   - `SECRET_KEY`
   - `CORS_ORIGINS`
3. Проверьте что база данных доступна

### Проблема: Изменения не применились

**Решение:**
1. Убедитесь что изменения закоммичены и запушены в GitHub
2. Проверьте что Railway подключен к правильной ветке (обычно `main`)
3. Принудительно перезапустите деплой в Railway Dashboard

## 📝 Измененные файлы

Следующие файлы были изменены и должны быть задеплоены:

1. `backend/app/models/transaction.py` - улучшен TransactionTypeEnum
2. `backend/app/api/v1/transactions.py` - исправлено создание транзакций
3. `backend/app/api/v1/categories.py` - исправлен эндпоинт категорий

## ✅ Чек-лист после деплоя

- [ ] Backend успешно задеплоен (зеленый статус в Railway)
- [ ] Нет ошибок в логах Railway
- [ ] API документация открывается (`/docs`)
- [ ] Создание дохода работает
- [ ] Создание расхода работает
- [ ] Переводы работают
- [ ] Категории загружаются
- [ ] Отчеты работают

## 🎯 Быстрая команда для деплоя

Если у вас настроен git и Railway:

```bash
# 1. Закоммитьте изменения
git add .
git commit -m "Fix enum issues for transactions"
git push origin main

# 2. Railway автоматически задеплоит
# 3. Проверьте статус в Railway Dashboard
```

---

**Важно:** После деплоя подождите 1-2 минуты пока Railway перезапустит backend, затем проверьте что все работает.

