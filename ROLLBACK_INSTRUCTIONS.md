# 🔄 Инструкция по откату изменений на проде

## ⚠️ ВАЖНО: Откат изменений

Если нужно откатить все изменения, связанные с исправлением enum проблем, выполните следующие шаги:

## Вариант 1: Откат до коммита до всех enum исправлений (РЕКОМЕНДУЕТСЯ)

Коммит `b5ff227d` был до начала всех исправлений enum:

```bash
# 1. Перейдите в директорию проекта
cd finance-manager

# 2. Проверьте текущее состояние
git status

# 3. Создайте резервную ветку на случай если что-то пойдет не так
git branch backup-before-rollback

# 4. Откатитесь до коммита до всех enum исправлений
git reset --hard b5ff227d

# 5. Принудительно запушьте изменения (ОСТОРОЖНО!)
git push origin main --force
```

## Вариант 2: Откат до конкретного коммита

Если вы знаете конкретный коммит, который работал:

```bash
# 1. Найдите нужный коммит
git log --oneline -30

# 2. Создайте резервную ветку
git branch backup-before-rollback

# 3. Откатитесь до нужного коммита (замените COMMIT_HASH на нужный)
git reset --hard COMMIT_HASH

# 4. Принудительно запушьте
git push origin main --force
```

## Вариант 3: Откат только измененных файлов

Если нужно откатить только конкретные файлы:

```bash
# 1. Посмотрите какие файлы были изменены
git diff HEAD~5 --name-only

# 2. Откатите конкретные файлы (замените FILE_PATH на путь к файлу)
git checkout b5ff227d -- FILE_PATH

# 3. Закоммитьте изменения
git commit -m "Rollback: Revert FILE_PATH to working version"

# 4. Запушьте
git push origin main
```

## 📋 Список файлов, которые были изменены для enum исправлений

Основные файлы, которые нужно откатить:

1. `backend/app/models/transaction.py` - TypeDecorator и валидация
2. `backend/app/api/v1/transactions.py` - создание транзакций
3. `backend/app/api/v1/categories.py` - эндпоинт категорий

## 🔍 Проверка перед откатом

Перед откатом проверьте:

```bash
# 1. Посмотрите какие изменения будут откачены
git diff b5ff227d HEAD -- backend/app/models/transaction.py
git diff b5ff227d HEAD -- backend/app/api/v1/transactions.py
git diff b5ff227d HEAD -- backend/app/api/v1/categories.py

# 2. Убедитесь что это именно те изменения, которые нужно откатить
```

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ

1. **Принудительный push (`--force`)** перезапишет историю на удаленном репозитории
2. **Создайте резервную ветку** перед откатом на случай если что-то пойдет не так
3. **Railway автоматически задеплоит** после push в main ветку
4. **Проверьте логи Railway** после деплоя чтобы убедиться что все работает

## 🔄 Восстановление после отката

Если нужно вернуть изменения обратно:

```bash
# 1. Переключитесь на резервную ветку
git checkout backup-before-rollback

# 2. Создайте новую ветку или слейте изменения
git checkout -b restore-enum-fixes
# или
git merge backup-before-rollback
```

## 📝 После отката

После отката и деплоя:

1. Проверьте что backend запустился без ошибок
2. Проверьте создание транзакций (доходы, расходы, переводы)
3. Проверьте загрузку категорий
4. Проверьте отчеты

## 🆘 Если что-то пошло не так

Если после отката что-то не работает:

1. **НЕ ПАНИКУЙТЕ** - у вас есть резервная ветка
2. Проверьте логи Railway - там будет видна ошибка
3. Восстановите из резервной ветки (см. выше)
4. Или откатитесь еще дальше к более раннему коммиту

---

**Рекомендуемый коммит для отката:** `b5ff227d` (до всех enum исправлений)





