# Руководство по нагрузочному тестированию Finance Manager

Эта директория содержит скрипты для нагрузочного тестирования, которые симулируют высокую нагрузку и проверяют стабильность системы.

## 📋 Обзор

Настройка нагрузочного тестирования использует **Locust** — инструмент для нагрузочного тестирования на Python, который позволяет:
- Симулировать тысячи одновременных пользователей
- Тестировать различные сценарии нагрузки
- Мониторить время отклика и частоту ошибок
- Выявлять узкие места и проблемы производительности

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd load_testing
pip install -r requirements.txt
```

### 2. Настройка целевого URL

Установите целевой URL в переменной окружения или измените `locustfile.py`:

```bash
# Windows PowerShell
$env:LOCUST_HOST="http://localhost:8000"

# Linux/Mac
export LOCUST_HOST="http://localhost:8000"
```

Или используйте параметр `--host` при запуске Locust.

### 3. Запуск нагрузочных тестов

#### Базовый тест (Web UI)
```bash
locust -f locustfile.py --host=http://localhost:8000
```

Затем откройте браузер на `http://localhost:8089` для настройки и запуска теста.

#### Тест из командной строки (без UI)
```bash
# Запуск с 100 пользователями, скорость создания 10 пользователей/сек, на 5 минут
locust -f locustfile.py --host=http://localhost:8000 --headless -u 100 -r 10 -t 5m
```

#### Разные типы пользователей
```bash
# Только обычные пользователи
locust -f locustfile.py --host=http://localhost:8000 -u 50 -r 5

# Только пользователи только для чтения
locust -f locustfile.py:ReadOnlyUser --host=http://localhost:8000 -u 30 -r 3
```

## 📊 Сценарии тестирования

### Сценарий 1: Легкая нагрузка (Текущее использование)
Симулирует ~10-20 одновременных пользователей:
```bash
locust -f locustfile.py --host=http://localhost:8000 --headless -u 20 -r 2 -t 10m
```

### Сценарий 2: Средняя нагрузка
Симулирует ~50-100 одновременных пользователей:
```bash
locust -f locustfile.py --host=http://localhost:8000 --headless -u 100 -r 10 -t 10m
```

### Сценарий 3: Высокая нагрузка
Симулирует ~200-500 одновременных пользователей:
```bash
locust -f locustfile.py --host=http://localhost:8000 --headless -u 500 -r 20 -t 15m
```

### Сценарий 4: Стресс-тест
Симулирует ~1000+ одновременных пользователей для поиска точки отказа:
```bash
locust -f locustfile.py --host=http://localhost:8000 --headless -u 1000 -r 50 -t 20m
```

### Сценарий 5: Тест на скачок нагрузки
Симулирует внезапный скачок трафика:
```bash
locust -f locustfile.py --host=http://localhost:8000 --headless -u 200 -r 200 -t 2m
```

## 📈 Мониторинг результатов

### Веб-интерфейс Dashboard
При запуске Locust с UI (по умолчанию) вы увидите:
- **Статистику в реальном времени**: Запросов в секунду, время отклика, частота ошибок
- **Графики**: Распределение времени отклика, запросы во времени
- **Ошибки**: Список неудачных запросов с деталями ошибок
- **Экспорт отчетов**: CSV/HTML отчеты

### Вывод в командной строке
При запуске в headless режиме Locust выводит:
- Текущую статистику каждые 10 секунд
- Итоговую сводку в конце

### Экспорт результатов
```bash
# Генерация HTML отчета
locust -f locustfile.py --host=http://localhost:8000 --headless -u 100 -r 10 -t 5m --html report.html

# Генерация CSV отчетов
locust -f locustfile.py --host=http://localhost:8000 --headless -u 100 -r 10 -t 5m --csv results
```

## 🎯 Что тестируется

Нагрузочный тест симулирует реалистичное поведение пользователей:

1. **Аутентификация** (Регистрация/Вход)
2. **Просмотр дашборда** - Самое частое действие
3. **Просмотр счетов** - Проверка балансов счетов
4. **Просмотр категорий** - Просмотр категорий расходов
5. **Создание транзакций** - Добавление доходов/расходов
6. **Просмотр аналитики** - Проверка отчетов и статистики
7. **Просмотр целей** - Проверка финансовых целей
8. **Создание счетов** - Добавление новых счетов
9. **Просмотр совместных бюджетов** - Проверка совместных бюджетов
10. **AI-ассистент** - Взаимодействие с AI функциями
11. **Проверка здоровья** - Мониторинг состояния системы

## ⚙️ Конфигурация

### Настройка поведения пользователей

Отредактируйте `locustfile.py` для изменения:
- **Время ожидания**: `wait_time = between(1, 3)` - Время между запросами
- **Вес задач**: `@task(5)` - Большее число = более частое выполнение
- **Типы пользователей**: Создайте пользовательские классы для разного поведения

### Тестирование разных эндпоинтов

Добавьте новые методы `@task` для тестирования конкретных эндпоинтов:

```python
@task(2)
def my_custom_endpoint(self):
    self.client.get(
        "/api/v1/my-endpoint",
        headers=self.get_headers(),
        name="My Custom Endpoint"
    )
```

## 🔍 Интерпретация результатов

### Ключевые метрики для отслеживания

1. **Время отклика (мс)**
   - < 200мс: Отлично
   - 200-500мс: Хорошо
   - 500-1000мс: Приемлемо
   - > 1000мс: Требуется оптимизация

2. **Запросов в секунду (RPS)**
   - Чем выше, тем лучше
   - Должно оставаться стабильным под нагрузкой

3. **Частота ошибок**
   - Должна быть < 1%
   - Мониторьте ошибки 5xx (ошибки сервера)
   - Мониторьте ошибки 429 (ограничение скорости)

4. **Процентили времени отклика**
   - 50-й процентиль (медиана): Типичный опыт пользователя
   - 95-й процентиль: Большинство пользователей испытывают
   - 99-й процентиль: Худший случай для большинства пользователей

### Красные флаги

- **Увеличивающееся время отклика** при росте нагрузки
- **Высокая частота ошибок** (> 5%)
- **Ошибки подключения к базе данных**
- **Утечки памяти** (мониторьте ресурсы сервера)
- **Ограничение скорости** (ошибки 429) - может потребоваться настройка лимитов

## 🛠️ Устранение неполадок

### Отказ в подключении
- Убедитесь, что backend запущен
- Проверьте правильность URL хоста
- Проверьте настройки файрвола

### Ошибки аутентификации
- Проверьте, правильно ли создаются тестовые пользователи
- Убедитесь, что генерация JWT токенов работает
- Проверьте подключение к базе данных

### Высокая частота ошибок
- Проверьте логи backend на наличие ошибок
- Мониторьте производительность базы данных
- Проверьте подключение к Redis (если используется)
- Убедитесь в настройках ограничения скорости

### Медленное время отклика
- Проверьте производительность запросов к базе данных
- Мониторьте использование CPU и памяти
- Проверьте на наличие проблем N+1 запросов
- Убедитесь, что кэширование работает

## 📝 Лучшие практики

1. **Начните с малого**: Начните с легкой нагрузки и постепенно увеличивайте
2. **Мониторьте ресурсы**: Следите за CPU, памятью и базой данных во время тестов
3. **Тестируйте реалистичные сценарии**: Симулируйте реальные паттерны поведения пользователей
4. **Запускайте в нерабочее время**: Не тестируйте на production в рабочее время
5. **Документируйте результаты**: Ведите записи результатов тестов для сравнения
6. **Тестируйте регулярно**: Запускайте нагрузочные тесты после крупных изменений

## 🔗 Дополнительные ресурсы

- [Документация Locust](https://docs.locust.io/)
- [Лучшие практики тестирования производительности](https://k6.io/docs/test-types/load-testing/)
- [Советы по производительности FastAPI](https://fastapi.tiangolo.com/deployment/performance/)

## 📊 Пример результатов теста

После запуска теста вы должны увидеть вывод вида:

```
Type     Name                          # reqs      # fails  |     Avg     Min     Max  |  Median   req/s
--------|------------------------------|-------|-------------|-------|-------|-------|-------|----------
GET      View Dashboard                5000    0(0.00%)      |     45     12    234  |     38   50.2
POST     Create Transaction            1000    2(0.20%)      |     78     23    456  |     65   10.1
GET      View Accounts                 2000    0(0.00%)      |     32     10    189  |     28   20.1
--------|------------------------------|-------|-------------|-------|-------|-------|-------|----------
         Aggregated                    8000    2(0.03%)      |     48     10    456  |     42   80.4
```

## 🚨 Важные замечания

- **Не запускайте нагрузочные тесты на production** без надлежащего мониторинга и одобрения
- **Начните с меньшего количества пользователей** и постепенно увеличивайте
- **Мониторьте вашу базу данных** - высокая нагрузка может повлиять на производительность БД
- **Проверьте ограничение скорости** - вы можете достичь лимитов скорости во время тестирования
- **Используйте отдельные тестовые данные** - не загрязняйте production данные







