# Руководство по быстрому старту - Нагрузочное тестирование

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Установка Locust
```bash
cd load_testing
pip install -r requirements.txt
```

### Шаг 2: Запустите ваш Backend
Убедитесь, что ваш Finance Manager backend запущен:
```bash
# В директории backend
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### Шаг 3: Запустите ваш первый тест

**Windows (PowerShell):**
```powershell
.\run_tests.ps1 light
```

**Linux/Mac:**
```bash
chmod +x run_tests.sh
./run_tests.sh light
```

**Или используйте Locust напрямую:**
```bash
locust -f locustfile.py --host=http://localhost:8000
```
Затем откройте http://localhost:8089 в браузере.

## 📊 Сценарии тестирования

### 1. Легкая нагрузка (Начните отсюда!)
Симулирует 20 одновременных пользователей - безопасно для тестирования:
```bash
# Windows
.\run_tests.ps1 light

# Linux/Mac
./run_tests.sh light
```

### 2. Средняя нагрузка
Симулирует 100 одновременных пользователей:
```bash
.\run_tests.ps1 medium
```

### 3. Высокая нагрузка
Симулирует 500 одновременных пользователей:
```bash
.\run_tests.ps1 high
```

### 4. Интерактивный режим (Рекомендуется для первого раза)
Используйте веб-интерфейс для управления тестом:
```bash
.\run_tests.ps1 interactive
# Затем откройте http://localhost:8089
```

## 📈 Понимание результатов

После запуска теста проверьте директорию `reports/`:
- **HTML файл**: Визуальный отчет с графиками
- **CSV файлы**: Сырые данные для анализа

### Ключевые метрики:
- **Время отклика**: Должно быть < 500мс для большинства запросов
- **Частота ошибок**: Должна быть < 1%
- **RPS (Запросов в секунду)**: Чем выше, тем лучше

## ⚠️ Важные замечания

1. **Начните с легкой нагрузки** - Не перегружайте вашу систему
2. **Мониторьте ваш backend** - Следите за CPU, памятью, базой данных
3. **Проверяйте логи** - Ищите ошибки в логах backend
4. **Тестируйте постепенно** - Постепенно увеличивайте нагрузку

## 🆘 Устранение неполадок

**Отказ в подключении?**
- Убедитесь, что backend запущен на порту 8000
- Проверьте URL хоста в команде

**Все запросы не удаются?**
- Проверьте логи backend на наличие ошибок
- Убедитесь, что база данных запущена
- Проверьте, что аутентификация работает

**Слишком медленно?**
- Начните с меньшего количества пользователей
- Проверьте производительность базы данных
- Мониторьте ресурсы сервера

## 📝 Следующие шаги

1. Сначала запустите тест с легкой нагрузкой
2. Просмотрите результаты
3. Постепенно увеличивайте нагрузку
4. Мониторьте ресурсы системы
5. Выявите узкие места
6. Оптимизируйте на основе находок

Для подробной документации см. [README_RU.md](README_RU.md)




