# Быстрый деплой на production с новой БД

Краткая инструкция для безопасного переключения на новую БД (195.43.142.121) в production.

## ⚠️ Перед началом

1. **Сделайте бэкап текущей production БД** (если есть данные)
2. **Убедитесь, что новая БД готова** (миграции применены, таблицы созданы)

## Быстрый план (5 шагов)

### Шаг 1: Проверка новой БД

```bash
# Проверка подключения
psql -h 195.43.142.121 -U finance_user -d finance_db -c "SELECT COUNT(*) FROM users;"

# Проверка таблиц
psql -h 195.43.142.121 -U finance_user -d finance_db -c "\dt"
```

### Шаг 2: Миграция данных (если нужно)

Если в текущей production БД есть данные:

```bash
# Экспорт из старой БД
pg_dump -h старая_бд_хост -U пользователь -d база > backup.sql

# Импорт в новую БД (только данные, структура уже есть)
psql -h 195.43.142.121 -U finance_user -d finance_db < backup.sql
```

### Шаг 3: Обновление переменных в Railway

1. Откройте ваш проект на **Railway**
2. Перейдите в ваш **Backend сервис**
3. Откройте **Variables**
4. Найдите `DATABASE_URL`
5. Измените значение на:
   ```
   postgresql://finance_user:ваш_пароль@195.43.142.121:5432/finance_db
   ```
6. Сохраните изменения

⚠️ **Railway автоматически перезапустит сервис** после изменения переменных.

### Шаг 4: Проверка после переключения

```bash
# Проверка через API
curl https://your-backend.railway.app/docs

# Проверка health (если есть)
curl https://your-backend.railway.app/health
```

### Шаг 5: Функциональное тестирование

1. Откройте https://your-backend.railway.app/docs
2. Попробуйте авторизоваться
3. Проверьте основные функции

## Откат (если что-то пошло не так)

1. В Railway → Variables
2. Верните старую `DATABASE_URL`
3. Railway перезапустит сервис

## Чек-лист

- [ ] Новая БД настроена и доступна
- [ ] Миграции применены на новой БД
- [ ] Данные мигрированы (если нужно)
- [ ] Бэкап старой БД создан
- [ ] `DATABASE_URL` обновлен в Railway
- [ ] Приложение перезапущено
- [ ] Функциональность проверена
- [ ] Логи проверены на ошибки

## Важные моменты

1. **Время деплоя**: Railway перезапускает сервис автоматически (~1-2 минуты простоя)
2. **Миграции**: Применяются автоматически при старте (см. `app/main.py`)
3. **Данные**: Если не мигрировали данные - новая БД будет пустой
4. **Откат**: Всегда можно вернуть старую БД через Variables

---

📖 **Подробная инструкция**: См. `PRODUCTION_DB_MIGRATION.md`






