# 🔧 Альтернативное решение: Dockerfile в корне

## Проблема

Railway не может найти файлы при использовании Root Directory = `backend`. Это может быть связано с тем, как Railway обрабатывает build context.

## ✅ Решение: Dockerfile в корне репозитория

Создан Dockerfile в корне репозитория, который:
- Работает с build context = корень репозитория (по умолчанию в Railway)
- Копирует файлы из `backend/` явно: `COPY backend/requirements.txt .`

### Что было сделано:

1. Создан `Dockerfile` в корне репозитория
2. Dockerfile использует пути `backend/` для копирования файлов
3. Railway.json обновлен для использования корневого Dockerfile

### Настройка Railway:

1. **Удалите Root Directory:**
   - Settings → Source → Root Directory: оставьте пустым (или удалите значение)

2. **Или пересоздайте сервис:**
   - Удалите текущий сервис
   - Создайте новый через + New → GitHub Repo
   - **НЕ устанавливайте Root Directory** (оставьте пустым)
   - Railway будет использовать корень репозитория как build context

3. **Проверьте Builder:**
   - Settings → Build → Builder должен быть: `Dockerfile`
   - dockerfilePath должен быть: `Dockerfile` (в корне)

## 📝 Структура

```
luci.fin/ (корень репозитория = build context)
├── Dockerfile  ← Railway использует этот (НОВЫЙ)
├── backend/
│   ├── Dockerfile  ← старый (можно оставить)
│   ├── requirements.txt
│   └── app/
└── frontend/
```

## ⚠️ Важно

- Root Directory должен быть **пустым** (не `backend`)
- Dockerfile в корне использует пути `backend/` для копирования
- Build context = корень репозитория

## 🎯 После настройки

1. Railway автоматически перезапустит деплой
2. Dockerfile в корне найдет все файлы через `backend/` пути
3. Сборка должна пройти успешно

## 🔄 Если хотите вернуться к Root Directory

Если позже захотите использовать Root Directory = `backend`:
1. Удалите Dockerfile из корня
2. Установите Root Directory = `backend`
3. Railway будет использовать `backend/Dockerfile`















