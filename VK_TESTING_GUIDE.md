# 🧪 Руководство по тестированию VK Mini App

## 📋 Что у вас уже есть

- ✅ VK Mini App создан
- ✅ Защищённый ключ (Secret Key) получен
- ✅ Сервисный ключ получен
- ✅ Код приложения готов
- ✅ Миграция применена

## 🚀 Шаг 1: Настроить URL приложения в VK

1. Откройте [VK Developers](https://dev.vk.com/apps)
2. Выберите ваше приложение
3. Перейдите в раздел **"Настройки"** → **"Основные"**
4. Найдите поле **"URL приложения"**
5. Укажите URL вашего фронтенда:
   ```
   https://luci-fin-kb8p.vercel.app
   ```
   (или ваш реальный URL, если он другой)

6. **Важно:** URL должен быть доступен по **HTTPS** (не HTTP!)

7. Сохраните изменения

## 🔧 Шаг 2: Проверить CORS на backend

Backend должен разрешать запросы с вашего фронтенда и с VK.

1. Откройте [Railway](https://railway.app)
2. Перейдите в ваш Backend сервис
3. Откройте **Settings** → **Variables**
4. Проверьте переменную `CORS_ORIGINS`

Должна быть строка вида:
```
https://luci-fin-kb8p.vercel.app,https://vk.com,https://m.vk.com
```

Или в JSON формате:
```json
["https://luci-fin-kb8p.vercel.app", "https://vk.com", "https://m.vk.com"]
```

**Если переменной нет или она не содержит VK домены:**
- Добавьте или обновите `CORS_ORIGINS`
- Включите: `https://vk.com`, `https://m.vk.com` (мобильная версия)
- Railway автоматически перезапустит сервис

## 📱 Шаг 3: Открыть приложение в VK

### Вариант A: Через мобильное приложение VK

1. Откройте приложение VK на телефоне
2. Перейдите в раздел **"Мини-приложения"** (или **"Сервисы"**)
3. Найдите ваше приложение по названию
4. Или откройте ссылку: `https://vk.com/appXXXXX` (где `XXXXX` - ваш App ID)

### Вариант B: Через веб-версию VK

1. Откройте [vk.com](https://vk.com) в браузере
2. В адресной строке введите: `https://vk.com/appXXXXX` (где `XXXXX` - ваш App ID)
3. Или найдите приложение через поиск

### Вариант C: Прямая ссылка для тестирования

В настройках VK Mini App найдите раздел **"Ссылка для тестирования"** или создайте ссылку:
```
https://vk.com/appXXXXX?vk_user_id=YOUR_VK_ID
```

Где:
- `XXXXX` - ваш App ID
- `YOUR_VK_ID` - ваш ID в VK (можно найти в настройках профиля VK)

## 🔍 Шаг 4: Проверить работу приложения

### 4.1. Откройте DevTools (инструменты разработчика)

**В браузере (если тестируете через веб-версию VK):**
- Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
- Перейдите на вкладку **Console**

**В мобильном приложении:**
- Используйте [VK Mini Apps Debugger](https://dev.vk.com/mini-apps/development/tools/debugger)
- Или подключите телефон к компьютеру и используйте Chrome DevTools

### 4.2. Проверьте логи авторизации

В консоли должны появиться логи:

```javascript
VK auto-auth check: { 
  hasLaunchParams: true, 
  launchParamsLength: 123,
  isMiniApp: true,
  currentPath: "/"
}
Attempting automatic VK login...
VK auto-login successful: { 
  userId: 123,
  hasAccessToken: true,
  accessTokenLength: 200
}
[VKAuthHandler] Token saved successfully
```

### 4.3. Проверьте запросы к API

1. Откройте вкладку **Network** в DevTools
2. Найдите запрос к `/api/v1/auth/vk`
3. Проверьте:
   - **Status**: должен быть `200` (успешно)
   - **Response**: должен содержать `access_token` и `refresh_token`

### 4.4. Проверьте создание пользователя

1. После успешной авторизации вы должны быть автоматически перенаправлены на главную страницу
2. Попробуйте создать транзакцию или счёт
3. Данные должны сохраняться

## 🐛 Решение проблем

### Проблема: "Приложение не открывается"

**Решение:**
1. Проверьте, что URL указан правильно в настройках VK
2. Убедитесь, что URL доступен по HTTPS
3. Проверьте, что фронтенд задеплоен и работает:
   ```
   https://luci-fin-kb8p.vercel.app
   ```

### Проблема: "CORS ошибка"

**Решение:**
1. Проверьте `CORS_ORIGINS` на Railway
2. Убедитесь, что включены домены VK:
   ```
   https://vk.com,https://m.vk.com
   ```
3. Перезапустите backend на Railway

### Проблема: "Авторизация не работает"

**Проверьте в консоли:**
1. Есть ли `vk_user_id` в URL параметрах?
2. Есть ли ошибки в запросе к `/api/v1/auth/vk`?
3. Доступен ли backend по HTTPS?

**Решение:**
1. Проверьте логи backend на Railway
2. Убедитесь, что миграция применена (колонка `vk_id` существует)
3. Проверьте, что backend отвечает:
   ```
   https://lucifin-production.up.railway.app/docs
   ```

### Проблема: "Пользователь не создается"

**Проверьте:**
1. Логи backend на Railway
2. Должны быть строки:
   ```
   Received VK login request, launch_params length: ...
   VK Mini App login - vk_id: '...'
   User processed: id=..., vk_id=..., is_new=True
   ```

**Решение:**
1. Проверьте, что миграция применена
2. Проверьте, что в базе данных есть колонка `vk_id`:
   ```sql
   SELECT column_name FROM information_schema.columns 
   WHERE table_name = 'users' AND column_name = 'vk_id';
   ```

## 📊 Чеклист тестирования

- [ ] URL приложения указан в настройках VK
- [ ] CORS настроен на backend (включены домены VK)
- [ ] Приложение открывается в VK
- [ ] В консоли видны логи авторизации
- [ ] Запрос к `/api/v1/auth/vk` успешен (Status 200)
- [ ] Пользователь создан автоматически
- [ ] Токены сохранены в localStorage
- [ ] Можно создавать транзакции и счета
- [ ] Данные сохраняются корректно

## 🎯 Быстрая проверка

Выполните эти команды для быстрой проверки:

1. **Проверьте доступность фронтенда:**
   ```bash
   curl https://luci-fin-kb8p.vercel.app
   ```
   Должен вернуть HTML страницу

2. **Проверьте доступность backend:**
   ```bash
   curl https://lucifin-production.up.railway.app/docs
   ```
   Должна открыться документация API

3. **Проверьте endpoint авторизации VK:**
   ```bash
   curl -X POST https://lucifin-production.up.railway.app/api/v1/auth/vk \
     -H "Content-Type: application/json" \
     -d '{"launch_params": "vk_user_id=123&vk_app_id=XXXXX"}'
   ```
   Должен вернуть ошибку валидации (это нормально, нужны реальные параметры)

## 📝 Полезные ссылки

- [VK Mini Apps Debugger](https://dev.vk.com/mini-apps/development/tools/debugger)
- [VK Developers Console](https://dev.vk.com/apps)
- [VK Bridge API](https://dev.vk.com/mini-apps/development/bridge)

---

**Готово!** После выполнения всех шагов ваше приложение должно работать в VK! 🎉




