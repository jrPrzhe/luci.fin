# Исправление ошибки сборки на Railway

## 🐛 Ошибка: "Failed to build an image"

Если Railway не может собрать образ, выполните следующие шаги:

## ✅ Решение 1: Использовать Dockerfile (Рекомендуется)

1. **В Railway Settings → Build → Builder:**
   - Измените с **"Railpack Default"** на **"Dockerfile"**
   - Railway будет использовать `backend/Dockerfile`

2. **Убедитесь что Root Directory установлен:**
   - Settings → Source → Root Directory: `backend`

3. **Перезапустите деплой:**
   - Нажмите **Deploy** в Railway

## ✅ Решение 2: Исправить Nixpacks конфигурацию

Если хотите использовать Nixpacks:

1. **Проверьте файлы:**
   - `backend/runtime.txt` - должен содержать `python-3.11.0`
   - `backend/Procfile` - должен содержать команду запуска
   - `backend/nixpacks.toml` - конфигурация Nixpacks (новый файл)

2. **Убедитесь что Root Directory установлен:**
   - Settings → Source → Root Directory: `backend`

3. **Перезапустите деплой**

## ✅ Решение 3: Пересоздать сервис с правильными настройками

1. **Удалите текущий сервис:**
   - Settings → Danger Zone → Delete Service

2. **Создайте новый сервис:**
   - **+ New** → **GitHub Repo**
   - Выберите репозиторий `luci.fin`
   - **Важно:** При настройке укажите **Root Directory: `backend`**

3. **Используйте Dockerfile:**
   - После создания сервиса
   - Settings → Build → Builder → выберите **"Dockerfile"**

## 🔍 Проверка

После применения решения:

1. Проверьте логи деплоя (Deployments → View Logs)
2. Убедитесь что сборка проходит успешно
3. Проверьте что приложение запускается

## 📝 Примечание

Dockerfile - более надежный способ для сложных проектов с зависимостями (scikit-learn, pandas, numpy), которые требуют компиляции.

















