# Настройка Railway Scheduler для ежедневных напоминаний

## Что такое Railway Scheduler?

Railway Scheduler — это сервис в Railway, который позволяет запускать команды по расписанию (аналог cron). Он идеально подходит для отправки ежедневных напоминаний о заданиях.

## Пошаговая инструкция

### Шаг 1: Создание нового сервиса

1. Откройте ваш проект в Railway Dashboard
2. Нажмите кнопку **"+ New"** (или **"New Service"**)
3. Выберите **"Empty Service"** или **"GitHub Repo"** (если хотите подключить репозиторий)
4. Назовите сервис: `daily-reminders` или `scheduler`

### Шаг 2: Настройка типа сервиса

1. В настройках сервиса найдите раздел **"Settings"**
2. Найдите опцию **"Service Type"** или **"Deploy Type"**
3. Выберите **"Scheduler"** или **"Cron Job"**
   - Если такой опции нет, используйте **"Shell"** или **"Command"**

### Шаг 3: Настройка расписания

1. В настройках сервиса найдите поле **"Schedule"** или **"Cron Schedule"**
2. Введите расписание в формате cron:
   ```
   0 9 * * *    # Каждый день в 9:00 UTC
   ```
   
   **Примеры расписаний:**
   - `0 9 * * *` - каждый день в 9:00 UTC
   - `0 10 * * *` - каждый день в 10:00 UTC
   - `0 9 * * 1-5` - каждый будний день в 9:00 UTC
   - `0 12 * * *` - каждый день в 12:00 UTC (полдень)
   - `30 8 * * *` - каждый день в 8:30 UTC

### Шаг 4: Настройка команды

1. В настройках сервиса найдите поле **"Command"** или **"Start Command"**
2. Введите команду:
   ```bash
   python backend/scripts/send_daily_reminders.py
   ```
   
   Или если используете виртуальное окружение:
   ```bash
   python3 backend/scripts/send_daily_reminders.py
   ```

### Шаг 5: Настройка переменных окружения

1. В настройках сервиса перейдите в раздел **"Variables"**
2. Добавьте все необходимые переменные окружения из вашего Backend сервиса:
   - `DATABASE_URL` - URL базы данных
   - `TELEGRAM_BOT_TOKEN` - токен Telegram бота
   - `VK_BOT_TOKEN` - токен VK бота (опционально)
   - `GOOGLE_AI_API_KEY` - ключ для AI сообщений (опционально)
   - Любые другие переменные, которые использует скрипт

   **Важно:** Можно использовать **"Reference"** для ссылки на переменные из другого сервиса (Backend)

### Шаг 6: Настройка рабочей директории

1. Если скрипт не находится в корне проекта, укажите **"Root Directory"**:
   - Оставьте пустым, если скрипт в корне
   - Или укажите путь, если проект в поддиректории

### Шаг 7: Настройка Python окружения

1. Railway автоматически определит Python, если есть `requirements.txt`
2. Убедитесь, что в корне проекта есть `requirements.txt` с зависимостями
3. Или создайте `Procfile` с командой:
   ```
   scheduler: python backend/scripts/send_daily_reminders.py
   ```

### Шаг 8: Сохранение и активация

1. Нажмите **"Save"** или **"Deploy"**
2. Scheduler автоматически запустится по расписанию
3. Проверьте логи в разделе **"Logs"** после первого запуска

## Альтернативный способ: через Railway CLI

Если у вас установлен Railway CLI, можно создать scheduler через команды:

```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Создайте новый сервис
railway init

# Настройте переменные окружения
railway variables set DATABASE_URL="your-database-url"
railway variables set TELEGRAM_BOT_TOKEN="your-token"

# Создайте railway.toml для scheduler
```

Создайте файл `railway.toml` в корне проекта:

```toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "python backend/scripts/send_daily_reminders.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[scheduler]
schedule = "0 9 * * *"  # Каждый день в 9:00 UTC
```

## Проверка работы

### Способ 1: Через логи

1. Откройте ваш Scheduler сервис в Railway
2. Перейдите в раздел **"Logs"**
3. После времени запуска (9:00 UTC) должны появиться логи:
   ```
   🚀 Sending daily reminders to all users...
   ✅ Daily reminders sent to 5 users
   ```

### Способ 2: Ручной запуск

1. В Railway Dashboard откройте ваш Scheduler сервис
2. Найдите кнопку **"Deploy"** или **"Run"**
3. Нажмите для ручного запуска (для тестирования)

### Способ 3: Через API

Можно вызвать эндпоинт напрямую для тестирования:
```bash
POST /api/v1/gamification/send-daily-reminders
Authorization: Bearer <admin_token>
```

## Устранение проблем

### Проблема: Scheduler не запускается

**Решение:**
1. Проверьте формат cron расписания
2. Убедитесь, что команда указана правильно
3. Проверьте логи на наличие ошибок
4. Убедитесь, что все переменные окружения установлены

### Проблема: Скрипт не находит модули

**Решение:**
1. Убедитесь, что `PYTHONPATH` установлен:
   ```bash
   PYTHONPATH=/app python backend/scripts/send_daily_reminders.py
   ```
2. Или используйте абсолютный путь к скрипту

### Проблема: Ошибка подключения к базе данных

**Решение:**
1. Проверьте, что `DATABASE_URL` установлен правильно
2. Убедитесь, что база данных доступна из Railway
3. Проверьте настройки firewall/безопасности базы данных

### Проблема: Не отправляются сообщения в Telegram

**Решение:**
1. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен
2. Убедитесь, что токен действителен
3. Проверьте логи на ошибки API Telegram

## Рекомендации

1. **Время запуска:** Выберите время, когда пользователи активны (например, 9:00 по их часовому поясу)
2. **Мониторинг:** Регулярно проверяйте логи, чтобы убедиться, что напоминания отправляются
3. **Обработка ошибок:** Скрипт уже содержит обработку ошибок, но можно добавить уведомления в случае критических ошибок
4. **Тестирование:** Сначала протестируйте на небольшой группе пользователей или в тестовом окружении

## Дополнительные настройки

### Отправка в определенное время по часовому поясу

Если нужно отправлять в определенное время по часовому поясу пользователя, можно:
1. Модифицировать скрипт для определения часового пояса пользователя
2. Или создать несколько scheduler'ов для разных часовых поясов

### Логирование

Все логи автоматически сохраняются в Railway. Можно настроить интеграцию с внешними сервисами мониторинга (например, Sentry) для отслеживания ошибок.

## Пример конфигурации Railway.toml

Полный пример `railway.toml` для scheduler:

```toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "python backend/scripts/send_daily_reminders.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[scheduler]
schedule = "0 9 * * *"  # Каждый день в 9:00 UTC

[env]
PYTHONPATH = "/app"
```

## Готово! 🎉

После настройки Scheduler будет автоматически отправлять ежедневные напоминания всем пользователям в указанное время.

