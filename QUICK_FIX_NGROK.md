# Быстрое исправление: ngrok подключается к неправильному порту

## 🔴 Проблема

ngrok пытается подключиться к `http://localhost:8443`, но backend работает на `http://localhost:8000`

## ✅ Решение

### Шаг 1: Остановите ngrok

В терминале с ngrok нажмите **Ctrl+C**

### Шаг 2: Перезапустите ngrok на порт 8000

```powershell
# Из корня проекта:
.\ngrok.exe http 8000
```

Или используйте обновленный скрипт (создам его).

### Шаг 3: Проверка

После перезапуска ngrok покажет:
```
Forwarding    https://xxxx-xx-xxx-xxx-xx.ngrok-free.app -> http://localhost:8000
```

Теперь URL будет работать!

## 🎯 Важно

- **Backend должен работать на порту 8000** (как сейчас)
- **ngrok должен туннелировать на порт 8000**
- Внешний доступ будет через HTTPS (ngrok предоставляет автоматически)

## 📋 Итоговая конфигурация

1. ✅ **Backend**: `http://localhost:8000` (запущен)
2. 🔄 **ngrok**: нужно перезапустить на порт 8000
3. ✅ **Frontend**: `http://localhost:5173`
4. ✅ **Telegram Bot**: работает

Перезапустите ngrok на порт 8000 и всё заработает!

