# Исправления API

## Что было сделано:

### Backend:
1. ✅ Созданы API endpoints:
   - `/api/v1/auth/register` - регистрация
   - `/api/v1/auth/login` - вход
   - `/api/v1/auth/me` - текущий пользователь
   - `/api/v1/accounts/` - список счетов
   - `/api/v1/accounts/balance` - баланс
   - `/api/v1/transactions/` - транзакции

2. ✅ Подключены роутеры в `main.py`

### Frontend:
1. ✅ Создан API клиент (`src/services/api.ts`)
2. ✅ Обновлены страницы:
   - Login - теперь работает с реальным API
   - Register - теперь работает с реальным API
   - Dashboard - загружает баланс и транзакции

### Telegram Bot:
1. ✅ Обновлена команда `/balance` - теперь пытается подключиться к backend

## Что нужно сделать:

1. **Перезапустить backend** - чтобы применить изменения в API
2. **Проверить CORS** - убедиться, что frontend может обращаться к backend
3. **Создать тестовый аккаунт** через регистрацию на фронте
4. **Для бота** - нужна дополнительная настройка аутентификации через Telegram

## Проверка работы:

1. Откройте http://localhost:5173
2. Зарегистрируйте новый аккаунт
3. Войдите в систему
4. На дашборде должны отображаться баланс (0) и пустой список транзакций

Если возникают ошибки:
- Проверьте консоль браузера (F12)
- Проверьте логи backend
- Убедитесь, что backend запущен на http://localhost:8000

