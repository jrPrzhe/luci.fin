# 🚨 СРОЧНО: Настройка Railway для корневого Dockerfile

## Проблема

Railway все еще ищет Dockerfile в `/backend/Dockerfile`, но теперь у нас Dockerfile в корне репозитория.

## ✅ РЕШЕНИЕ: Убрать Root Directory

### В Railway UI:

1. **Откройте Settings сервиса `luci.fin`**
2. **Settings → Source:**
   - Поле **Root Directory** → **ОСТАВЬТЕ ПУСТЫМ** (удалите значение `backend`)
   - Нажмите **Save**

3. **Settings → Build:**
   - Builder должен быть: `Dockerfile`
   - dockerfilePath должен быть: `Dockerfile` (или `./Dockerfile`)

### Или пересоздайте сервис БЕЗ Root Directory:

1. **Удалите сервис:**
   - Settings → Danger Zone → Delete Service

2. **Создайте новый:**
   - **+ New** → **GitHub Repo**
   - Выберите `luci.fin`
   - **НЕ устанавливайте Root Directory** (оставьте пустым)
   - Railway автоматически найдет `Dockerfile` в корне

## 📝 Структура

```
luci.fin/ (build context = корень)
├── Dockerfile  ← Railway использует этот
├── backend/
│   ├── requirements.txt
│   └── app/
└── railway.json (в корне, если нужно)
```

## ⚠️ КРИТИЧЕСКИ ВАЖНО

- **Root Directory должен быть ПУСТЫМ** (не `backend`!)
- Dockerfile в корне репозитория
- Railway.json обновлен для использования корневого Dockerfile

## 🎯 После настройки

1. Railway автоматически перезапустит деплой
2. Dockerfile в корне найдет файлы через `backend/` пути
3. Сборка должна пройти успешно

## 🔍 Если ошибка сохраняется

Проверьте что Dockerfile в git:
```bash
git ls-files Dockerfile
```

Если нет - добавьте:
```bash
git add Dockerfile
git commit -m "Add root Dockerfile"
git push origin main
```





