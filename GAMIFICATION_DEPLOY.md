# Деплой системы геймификации

## ✅ Что было добавлено

1. **Модели данных**:
   - `UserGamificationProfile` - профиль пользователя (уровень, XP, страйк, сердце)
   - `DailyQuest` - шаблоны ежедневных квестов
   - `UserDailyQuest` - персональные квесты пользователя
   - `Achievement` - описания достижений
   - `UserAchievement` - история разблокированных достижений

2. **API эндпоинты**:
   - `GET /api/v1/gamification/status` - полный статус геймификации
   - `GET /api/v1/gamification/quests` - ежедневные квесты
   - `POST /api/v1/gamification/quests/{id}/complete` - выполнение квеста
   - `GET /api/v1/gamification/achievements` - все достижения
   - `POST /api/v1/ai/gamification-message` - сообщения от Люси

3. **Frontend компоненты**:
   - `GamificationStatus` - отображение уровня, страйка, сердца
   - `DailyQuests` - список ежедневных квестов
   - `LucyAvatar` - аватар Люси с эмоциями
   - `Achievements` - страница достижений

## 🚀 Деплой на Railway

### Автоматический деплой

Railway автоматически задеплоит изменения после пуша в `main` ветку.

### Применение миграций

Миграции должны примениться автоматически при старте приложения (см. `backend/app/main.py`).

Если миграции не применились автоматически, выполните вручную:

```bash
# Через Railway CLI
railway run alembic upgrade head

# Или через Railway Dashboard -> Service -> Deployments -> View Logs
```

### Инициализация начальных данных

После применения миграций нужно инициализировать базовые квесты и достижения:

```bash
# Через Railway CLI
railway run python backend/scripts/init_gamification_data.py

# Или через Railway Dashboard -> Service -> Deployments -> Shell
cd backend
python scripts/init_gamification_data.py
```

## 📋 Проверка после деплоя

1. **Проверьте миграции**:
   - Убедитесь, что таблицы созданы: `user_gamification_profiles`, `daily_quests`, `user_daily_quests`, `achievements`, `user_achievements`

2. **Проверьте API**:
   - `GET /api/v1/gamification/status` - должен вернуть статус геймификации
   - `GET /api/v1/gamification/quests` - должен вернуть список квестов

3. **Проверьте Frontend**:
   - Откройте Dashboard - должны отображаться компоненты геймификации
   - Откройте страницу Achievements - должна отображаться страница достижений

4. **Проверьте интеграцию**:
   - Создайте транзакцию - должен обновиться страйк и начислиться XP
   - Выполните квест - должен обновиться прогресс

## 🔧 Ручное применение миграций (если нужно)

Если автоматическое применение не сработало:

```bash
# Подключитесь к Railway через CLI
railway link

# Примените миграции
railway run alembic upgrade head

# Инициализируйте данные
railway run python backend/scripts/init_gamification_data.py
```

## 📝 Важные замечания

1. **Миграция**: `3f8ef46fad30_add_gamification_tables.py` создаст все необходимые таблицы
2. **Инициализация данных**: Скрипт `init_gamification_data.py` создаст базовые квесты и достижения
3. **Автоматическое обновление**: При создании транзакций автоматически обновляется страйк и начисляется XP
4. **Профили пользователей**: Профили геймификации создаются автоматически при первом обращении к API

## 🐛 Troubleshooting

### Миграции не применяются

Проверьте логи Railway:
```bash
railway logs
```

Ищите строки:
- `[STARTUP] Running migrations...`
- `[STARTUP] Migrations completed`

### Ошибки при инициализации данных

Убедитесь, что:
1. Миграции применены успешно
2. Подключение к БД работает
3. Все таблицы созданы

### API возвращает ошибки

Проверьте:
1. Роутер `gamification` подключен в `main.py`
2. Модели импортированы в `models/__init__.py`
3. Нет синтаксических ошибок в коде

## ✅ Готово!

После успешного деплоя система геймификации будет работать автоматически:
- Пользователи будут получать XP за транзакции
- Страйк будет обновляться автоматически
- Квесты будут генерироваться ежедневно
- Достижения будут разблокироваться при выполнении условий







