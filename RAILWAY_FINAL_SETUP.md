# ✅ Финальная настройка Railway

## Текущая ситуация

Создан Dockerfile в корне репозитория, который работает с build context = корень репозитория.

## ⚠️ ВАЖНО: Настройки в Railway

### Вариант 1: Убрать Root Directory (РЕКОМЕНДУЕТСЯ)

1. **В Railway Settings → Source:**
   - Поле **Root Directory** → **оставьте пустым** или удалите значение
   - Нажмите **Save**

2. **В Railway Settings → Build:**
   - Builder должен быть: `Dockerfile`
   - dockerfilePath должен указывать на корневой Dockerfile

### Вариант 2: Пересоздать сервис БЕЗ Root Directory

1. **Удалите текущий сервис:**
   - Settings → Danger Zone → Delete Service

2. **Создайте новый:**
   - **+ New** → **GitHub Repo**
   - Выберите `luci.fin`
   - **НЕ устанавливайте Root Directory** (оставьте пустым)
   - Railway автоматически найдет Dockerfile в корне

## 📝 Структура

```
luci.fin/ (корень = build context)
├── Dockerfile  ← Railway использует этот (в корне)
├── backend/
│   ├── requirements.txt
│   └── app/
└── frontend/
```

## 🔍 Как работает новый Dockerfile

Dockerfile в корне использует пути `backend/`:
- `COPY backend/requirements.txt .` - копирует из backend/
- `COPY backend/ .` - копирует все из backend/

## ✅ После настройки

1. Railway автоматически перезапустит деплой
2. Dockerfile в корне найдет все файлы
3. Сборка должна пройти успешно

## 🎯 Ключевые моменты

- **Root Directory должен быть ПУСТЫМ** (не `backend`)
- Dockerfile находится в корне репозитория
- Railway использует корень как build context
- Dockerfile копирует файлы из `backend/` явно





