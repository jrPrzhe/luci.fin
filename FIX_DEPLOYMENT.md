# 🔧 Исправление ошибки "Request failed" при авторизации

## Проблема

При попытке авторизации на фронтенде возникает ошибка "Request failed" из-за:
1. Не настроен `VITE_API_URL` на Vercel
2. Не настроен `CORS_ORIGINS` на Railway

## ✅ Решение

### Шаг 1: Настройка Vercel (Frontend)

1. Откройте проект на [vercel.com](https://vercel.com)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте переменную:
   - **Key**: `VITE_API_URL`
   - **Value**: `https://lucifin-production.up.railway.app`
   - **Environment**: Production, Preview, Development (выберите все)
4. Нажмите **Save**
5. **Перезапустите деплой** (Deployments → последний деплой → ... → Redeploy)

### Шаг 2: Настройка Railway (Backend)

1. Откройте проект на [railway.app](https://railway.app)
2. Перейдите в ваш backend сервис
3. Откройте **Settings** → **Variables**
4. Добавьте или обновите переменную:
   - **Key**: `CORS_ORIGINS`
   - **Value**: `https://luci-fin-kb8p.vercel.app`
   - Если уже есть другие origins, добавьте через запятую: `https://luci-fin-kb8p.vercel.app,http://localhost:5173`
   - ⚠️ **Важно**: Можно использовать либо JSON формат `["https://luci-fin-kb8p.vercel.app"]`, либо просто строку через запятую `https://luci-fin-kb8p.vercel.app`
5. Railway автоматически перезапустит сервис

### Шаг 3: Проверка

После применения изменений:

1. **Frontend**: Откройте `https://luci-fin-kb8p.vercel.app`
2. Попробуйте авторизоваться
3. Откройте **DevTools** (F12) → **Console** и **Network**
4. Проверьте:
   - Запросы должны идти на `https://lucifin-production.up.railway.app`
   - Не должно быть CORS ошибок

## 🔍 Отладка

Если ошибка сохраняется:

### Проверьте в браузере (DevTools → Network):

1. Найдите запрос к `/api/v1/auth/login`
2. Проверьте:
   - **Request URL**: должен быть `https://lucifin-production.up.railway.app/api/v1/auth/login`
   - **Status**: должен быть 200 (не CORS ошибка)
   - **Response**: должен содержать токены

### Проверьте переменные окружения:

**Vercel:**
```bash
# В Vercel Dashboard → Settings → Environment Variables
VITE_API_URL=https://lucifin-production.up.railway.app
```

**Railway:**
```bash
# В Railway Dashboard → Settings → Variables
CORS_ORIGINS=https://luci-fin-kb8p.vercel.app
```

### Проверьте CORS на backend:

Откройте в браузере:
```
https://lucifin-production.up.railway.app/docs
```

Проверьте, что API доступен.

## 📝 Важные замечания

1. **После изменения переменных окружения на Vercel** нужно **перезапустить деплой** (Redeploy)
2. **Railway перезапускает автоматически** после изменения переменных
3. Если используете **Preview deployments** на Vercel, добавьте туда тоже `VITE_API_URL`

## 🎯 После исправления

1. Frontend должен отправлять запросы на правильный backend URL
2. Backend должен разрешать запросы с фронтенда
3. Авторизация должна работать

