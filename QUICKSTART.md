# Быстрый старт

## Подготовка

### 1. Установите необходимые инструменты

- **Docker** и **Docker Compose**: [Установка Docker](https://docs.docker.com/get-docker/)
- **Git**: [Установка Git](https://git-scm.com/downloads)

### 2. Получите API ключи

Для полной функциональности вам понадобятся:

1. **Google AI Studio API Key** - для ИИ-ассистента (Gemini)
   - Регистрация: https://aistudio.google.com
   - Создание ключа: нажмите "Get API key" или "Создать API-ключ"
   - **Бесплатно с щедрыми лимитами!** 🎉

2. **Telegram Bot Token** - для Telegram бота
   - Создайте бота через @BotFather в Telegram
   - Получите токен

3. **Exchange Rate API Key** (опционально)
   - Для конвертации валют
   - Множество бесплатных API доступны

## Запуск проекта

### Вариант 1: Docker (Рекомендуется)

1. **Клонируйте репозиторий**:
```bash
git clone <repository-url>
cd finance-manager
```

2. **Настройте переменные окружения**:
```bash
cp .env.example .env
```

3. **Отредактируйте `.env` файл** и добавьте свои ключи:
```env
SECRET_KEY=your-secret-key-here-generate-with-openssl
DATABASE_URL=postgresql://finance_user:finance_password@postgres:5432/finance_db
GOOGLE_AI_API_KEY=your-google-ai-api-key
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
```

4. **Запустите проект**:
```bash
docker-compose up -d
```

5. **Проверьте статус**:
```bash
docker-compose ps
```

6. **Откройте в браузере**:
- Frontend: http://localhost
- API Docs: http://localhost/api/docs

### Вариант 2: Локальная разработка

#### Backend

1. **Установите Python 3.11+**:
```bash
python --version  # Проверьте версию
```

2. **Создайте виртуальное окружение**:
```bash
cd backend
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Настройте базу данных**:
```bash
# Создайте .env файл или экспортируйте переменные
export DATABASE_URL=postgresql://user:password@localhost:5432/finance_db
```

5. **Запустите миграции**:
```bash
alembic upgrade head
```

6. **Запустите сервер**:
```bash
uvicorn app.main:app --reload
```

Backend будет доступен на http://localhost:8000

#### Frontend

1. **Установите Node.js 18+**:
```bash
node --version  # Проверьте версию
```

2. **Установите зависимости**:
```bash
cd frontend
npm install
```

3. **Запустите dev-сервер**:
```bash
npm run dev
```

Frontend будет доступен на http://localhost:5173

#### Telegram Bot

1. **В отдельном терминале**:
```bash
cd telegram-bot
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

2. **Создайте .env файл**:
```env
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
BACKEND_URL=http://localhost:8000
```

3. **Запустите бота**:
```bash
python bot.py
```

## Первый запуск

### 1. Создайте аккаунт

Перейдите на http://localhost и зарегистрируйтесь:
- Email: ваш@email.com
- Пароль: (не менее 8 символов)

Или войдите через Telegram-бота.

### 2. Создайте первый счёт

1. Нажмите "➕ Добавить счёт"
2. Выберите тип счёта (Наличные, Банковская карта, и т.д.)
3. Укажите начальный баланс
4. Выберите валюту

### 3. Добавьте первую транзакцию

1. Нажмите "➕ Добавить расход" или "➕ Добавить доход"
2. Заполните форму:
   - Сумма
   - Категория
   - Дата
   - Описание
3. Сохраните

### 4. Попробуйте Telegram-бота

В Telegram отправьте боту:
- `/start` - начать работу
- `/balance` - проверить баланс
- `/add_expense` - добавить расход

## Полезные команды

### Docker

```bash
# Просмотр логов
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f telegram-bot

# Остановка
docker-compose down

# Пересоздание контейнеров
docker-compose up -d --force-recreate

# Очистка (включая данные)
docker-compose down -v
```

### Backend

```bash
# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1

# Запустить тесты
pytest

# Форматирование кода
black app/
```

### Frontend

```bash
# Сборка для продакшена
npm run build

# Просмотр продакшен сборки
npm run preview

# Линтинг
npm run lint
```

## Решение проблем

### База данных не подключается

1. Проверьте, что PostgreSQL запущен:
```bash
docker-compose ps postgres
```

2. Проверьте DATABASE_URL в .env

3. Пересоздайте базу:
```bash
docker-compose down -v
docker-compose up -d postgres
```

### Telegram-бот не отвечает

1. Проверьте токен в .env
2. Убедитесь, что бот запущен:
```bash
docker-compose logs telegram-bot
```

### ИИ-ассистент не работает

1. Проверьте OPENAI_API_KEY в .env
2. Убедитесь, что на балансе есть средства
3. Проверьте логи:
```bash
docker-compose logs backend | grep ai
```

## Следующие шаги

1. Изучите [API Документацию](http://localhost:8000/docs)
2. Настройте совместные бюджеты
3. Создайте финансовые цели
4. Настройте уведомления
5. Попробуйте ИИ-ассистента

## Дополнительная документация

- [README.md](README.md) - Общая информация
- [API Documentation](http://localhost:8000/docs) - Swagger UI
- [Architecture Guide](docs/architecture.md) - Архитектура системы (в разработке)

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Создайте Issue на GitHub
3. Обратитесь к документации

Удачи! 🚀

